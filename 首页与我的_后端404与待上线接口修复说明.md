# 「首页 / 我的」后端：404 与待上线接口修复说明

> **用途**：交给后端/运维对照修复。  
> **环境**：2026-04 对 `https://jshsp1.eds-tech.cn/api/v1` 实测；同域下部分接口正常，说明 TLS、域名、主进程可达，**缺路由或未反代到正确服务**。  
> **产品契约全文**：`doc/首页与我的_服务域前后端联调需求.md`（尤其 **11.10**、第十章订单、**第十四章**）；前端行为见 `doc/首页与我的_前端配合及测试说明.md`。

---

## 1. 现象归纳

| 路径 | 实测 HTTP | 含义 |
|------|-----------|------|
| `GET /api/v1/core/community/hot` | **404**（`Cannot GET ...`） | 未注册或未部署 |
| `GET /api/v1/service-orders/my` | **404** | 未注册或未部署 |
| `GET /api/v1/neighbor-assist/orders/my` | **404** | 未注册或未部署 |

**对照（同基座已通）**：

- `GET /api/v1/core/services/hot` → 200  
- `GET /api/v1/core/workers` → 200  
- `GET /api/v1/core/goods/featured` → 200  
- `GET /api/v1/orders/my` → **401**（无 Token），说明路由存在、需登录  

**结论**：404 **不是**小程序合法域名问题，而是 **线上 Node/网关未提供对应 `app.get`/`router` 或未发布含该路由的构建**；若存在多服务（多端口），还需核对 **Nginx `location` 是否转发到挂载了这些路由的进程**。

---

## 2. 待修复接口（按优先级）

### P0 — `GET /api/v1/core/community/hot`

**用途**：首页「小区热卖榜」；与全站 `core/services/hot` 区分，按 **小区维度** 聚合订单统计排序。

**Query（与需求文档 11.10 一致）**

- `community_id`：建议支持；若登录用户有默认小区，可从 JWT 解析，与 Query 二选一或合并策略由后端定。  
- `limit`：可选，默认与上限与文档一致。  
- `days`：可选，统计时间窗。  
- `type`：可选，`service` | `shop` | `all`。

**响应 `data` 建议形态**

```json
{
  "services": [
    {
      "item_type": "service",
      "id": 1,
      "title": "服务标题",
      "price": "99.00",
      "cover_image": "/或https",
      "order_count_in_community": 120,
      "rank": 1
    }
  ],
  "shops": []
}
```

- 若一期仅做服务榜，`shops` 可为 `[]`。  
- **统一包装**：与现有 Core 接口一致，建议 `{ "errno": 0, "data": { ... } }` 或项目已约定的 `{ "message":"ok", "data": { ... } }`（**无 `errno` 时 HTTP 须 200 且业务成功**，前端以无 `errno` + 200 视为成功）。

**验收**

```bash
curl -sS "https://jshsp1.eds-tech.cn/api/v1/core/community/hot?limit=5&community_id=1"
# 期望：HTTP 200，body 含 services（或文档约定字段），非 404
```

---

### P0 — `GET /api/v1/service-orders/my`

**用途**：小程序「我的 → 到家订单」列表（JWT）。

**Query**

- `page`、`limit`（或 `page_size`），与 `GET core/workers` 分页风格对齐。

**响应 `data` 建议**

- 列表形态与文档一致：**`{ "list": [ ... ], "total", "page", "limit" }`**，前端用 `data.list`。  
- 列表项至少便于展示：`id`、`status`（或 `status_text`）、服务名称摘要、`created_at`、`amount` / `pay_amount` 等。

**与现有 `GET /api/v1/orders/my` 的关系**

- 线上 **`orders/my` 已存在**（无 Token 为 401）。若业务上「到家订单」与「通用订单」共用一张表，可二选一：  
  - **方案 A**：实现独立 `service-orders/my`，仅返回到家类型（推荐，与联调文档一致）。  
  - **方案 B**：不新增路径，在文档中**正式声明**「到家订单 = `GET /orders/my?type=service`（或等价筛选）」，并保证筛选参数稳定；前端可改为只调 `orders/my`。  
- 在未发布 `service-orders/my` 前，前端已做 **404 时回退 `orders/my`**，后端上线本接口后应返回 200 + 列表，避免长期依赖回退。

**验收**

```bash
curl -sS -H "Authorization: Bearer <用户JWT>" \
  "https://jshsp1.eds-tech.cn/api/v1/service-orders/my?page=1&limit=10"
# 期望：HTTP 200，非 404
```

---

### P1 — `GET /api/v1/neighbor-assist/orders/my`

**用途**：小程序「我的 → 帮帮订单」列表（JWT）。

**Query / 响应**：与需求文档 **11.10 邻里帮帮**、**第十四章** 一致；`data` 建议同样为 **`{ list, total, page, limit }`**。

**验收**

```bash
curl -sS -H "Authorization: Bearer <用户JWT>" \
  "https://jshsp1.eds-tech.cn/api/v1/neighbor-assist/orders/my?page=1&limit=10"
# 期望：HTTP 200，非 404
```

---

## 3. 错误码与 HTTP 约定（避免联调歧义）

- **业务失败**：优先 **`errno !== 0`** + `errmsg`；HTTP 可为 200（与当前 `utils/util.js` 一致）。  
- **未登录**：**401** + 明确 `errmsg`。  
- **路由不存在**：**404** 仅用于「路径未实现」；实现后不应再对正确路径返回 404。

---

## 4. 部署与网关自检（运维）

- [ ] 确认当前线上跑的是 **含上述路由** 的构建（`git` 提交 / 镜像 tag）。  
- [ ] 若 **主 API 与惠民卡等分进程**：确认 `jshsp1.eds-tech.cn` 的 `/api/v1/core/*`、`/api/v1/service-orders/*`、`/api/v1/neighbor-assist/*` 均反代到 **挂载 Core + 订单路由** 的 upstream，而不是仅反代旧进程。  
- [ ] 发布后复测本节 **curl** 与小程序「首页热卖 / 我的订单」页。

---

## 5. 修订记录

| 日期 | 说明 |
|------|------|
| 2026-04-16 | 初版：基于线上探测的 404 清单 + 契约摘要 + 验收命令 |
