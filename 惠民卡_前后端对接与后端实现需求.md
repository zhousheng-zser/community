# 惠民卡 · 前后端对接与后端实现需求

> **目的**：把首页「惠民卡」Tab 及关联入口的前端行为，与后端需落地的**数据库、服务进程、HTTP 接口**对齐；标明当前小程序仍以**本地死数据**为主时的切换条件与缺口功能。  
> **已有详设**：表结构、种子、素材同步见 `doc/惠民卡_联盟数据与同步.md`；线上网关与进程拆分见 `doc/惠民卡_线上对接.md`；接口清单见 `doc/backend/API_DOC.md` §7.1。

---

## 1. 前端功能与数据来源（现状）

| 前端位置 | 用户可见能力 | 当前主要数据来源 | 说明 |
|----------|--------------|------------------|------|
| `pages/index/index.wxml` · Tab「惠民卡」 | 子 Tab：京东联盟 / 拼多多 | `index.js` 的 `benefitAllianceTabs`（写死两项） | 非接口 |
| 同上 · 顶栏头图 + 标题/副标题 | 各平台一块 hero 区 | **默认**：`utils/benefitAllianceLocal.js` 内标题文案；头图初值 `utils/images.js`（`benefitJdAllianceHero` / `benefitPddAllianceHero`） | `config.benefitAlliancePreferLocal === true` 时，本地有商品则**不请求** `benefit/display` 覆盖京东/拼多多 hero |
| 同上 · 商品网格 | 主图、标题、价/券后价、可选返利角标 | **默认**：`benefitAllianceLocal.js` 中 `JD_ITEMS` / `PDD_ITEMS` + 本地图路径 `/img/jd_benefit/*.png`、`/img/pdd_benefit/*.jpeg` | 与「流量联盟」文档对齐的**死数据** |
| 同上 · 底部 GO 卡片 | 京东：跳转联盟小程序 proxy；拼多多：小程序 path 或复制 H5 链 | 本地：`jdEntry` / `pddEntry` 来自首条商品；接口模式：列表首条 | 见 `index.js` `goToJDMiniprogram` / `goToPddBenefit` |
| `utils/config.js` | 京东/拼多多目标小程序 AppId | `benefitAlliance.jdUnionAppId`、`pddMiniAppId` | 后端可在进宝返回中约束 path 对应 AppId，前端需一致 |
| 首页顶栏搜索框 | 占位文案含「惠民卡」场景 | 仅 `placeholder`，**无**提交检索逻辑 | 若产品要做联盟内搜索，需新增接口与前端事件 |
| `pages/user/user` · 订单区「惠民卡订单」 | 点击 | `showToastWait`（敬请期待） | **无**订单列表页、**无**后端订单域 |

**结论**：业务上「惠民卡」联盟商品与顶栏应以**后端库表 + 只读 API** 为权威；当前通过 `benefitAlliancePreferLocal: true` 优先走本地包，线上社区主 API（常见 3000）往往**未挂载**惠民路由，需按 `惠民卡_线上对接.md` 合并进程或反代到本仓库 `backend`（如 3001）。

---

## 2. 后端必须交付项（与前端一一对应）

### 2.1 数据库（MySQL）

| 表名 | 用途 | 前端消费场景 | 建表/种子 |
|------|------|--------------|-----------|
| `jd_benefit_goods` | 京东联盟商品行 | `GET .../jd/benefit/goods` → 网格、`jdEntry` | `backend/sql/jd_benefit_goods.sql`、`seed_benefit_bootstrap.sql` / `npm run seed:jd-benefit` |
| `pdd_benefit_goods` | 拼多多进宝商品行 | `GET .../pdd/benefit/goods` → 网格、`pddEntry` | `backend/sql/pdd_benefit_goods.sql`、`npm run seed:pdd-benefit` |
| `benefit_alliance_config` | 按 `scene` + `platform` 的顶栏图与文案 | `GET .../benefit/display`（在关闭本地优先或本地无列表时生效） | `backend/sql/benefit_alliance_config.sql`、`npm run seed:benefit-alliance-config` |

**字段与前端映射要点**（详细列定义见联盟同步文档）：

- 京东：`sku_id` ↔ 前端 `skuId`（`u.jd.com/{sku_id}` 路径段）；`image_url` → 列表图（支持 `/img/...` 或 `https://`）；`spread_url` → 跳转用；`price`、`rebate_amount`、`sort_order`、`status`。
- 拼多多：`link_key` 运维对照；`goods_id`、`mini_path` 供 `navigateToMiniProgram`；`coupon_price` / `price` 对应券后/原价展示；`spread_url` 作 H5 兜底复制。
- 顶栏：`hero_image_url`、`hero_title`、`hero_subtitle`，接口输出驼峰 `heroImage`、`heroTitle`、`heroSubtitle`。

### 2.2 HTTP 接口（统一前缀 `https://<域名>/api/v1`，与 `utils/config.js` 的 `baseUrl` 一致）

小程序 `util.get` 成功时取 `res.data.data || res.data`，下列「业务体」指**解析后**对象。

| 方法 | 路径 | Query | 业务体（errno=0 时） | 前端使用位置 |
|------|------|-------|----------------------|--------------|
| GET | `/benefit/display` | `scene=benefit_card`（可扩展） | `{ jd: { heroImage, heroTitle, heroSubtitle }, pdd: { ... } }` | `index.js` `init`：覆盖 `jdBanner`/`pddBanner` 与标题（本地优先关闭或对应侧无本地列表时） |
| GET | `/jd/benefit/goods` | `scene=benefit_card` | `{ list: [{ id, skuId, title, image, price, rebateAmount, spreadUrl }] }` | 商品列表、`jdEntry` 取首条 |
| GET | `/jd/promotion/spread-url` | `sku_id`、`scene` | `{ spreadUrl }` | `goToJDMiniprogram`：本地优先关闭且需查库转链时 |
| GET | `/pdd/benefit/goods` | `scene=benefit_card` | `{ list: [{ id, goodsId, title, image, price, couponPrice, rebateAmount, spreadUrl, miniPath }] }` | 商品列表、`pddEntry` |
| GET | `/pdd/promotion/spread-url` | `goods_id`、`scene` | `{ spreadUrl, miniPath }` | `goToPddBenefit`：有 `goodsId` 且无本地 `miniPath` 时 |

**实现参考**：本仓库 `backend/src/controllers/benefitDisplayController.js`、`jdBenefitController.js`、`pddBenefitController.js`；挂载方式 `backend/src/mountBenefitAlliance.js`。

**管理端（运营配置，非小程序直连）**：`GET/POST/PUT/DELETE /api/v1/admin/jd-benefit-goods` 等（见 `backend/src/routes/admin.js`），用于替代手工改库；拼多多对称 CRUD 可按 `后台管理界面_方案草案.md` 二期补。

### 2.3 服务与静态资源

| 项 | 要求 |
|----|------|
| 进程 | 惠民路由必须在小程序可达的 `baseUrl` 同源路径下；若主社区 API 无此路由，需 Nginx 拆分或 `mountBenefitAlliance` 并入主 `app`（见 `惠民卡_线上对接.md`） |
| 图片 | 库中 `image_url` / `hero_image_url` 若为 `/img/jd_benefit` 等，需与 `config.uploadsImageSubdirs` 及服务器 `uploads` 目录一致，否则小程序只显示包内路径或需改为完整 URL |
| 鉴权 | 当前小程序调用上述 GET **无 token 硬性要求**；管理端接口需独立鉴权 |

---

## 3. 前端切换为「以后端为准」的检查清单

1. 生产库执行建表 + 种子（或运营后台录入），保证 `status=1` 且有排序。
2. 确认 `GET /api/v1/benefit/display`、`/jd/benefit/goods`、`/pdd/benefit/goods` 在 `config.baseUrl` 下可访问。
3. 将 `utils/config.js` 中 **`benefitAlliancePreferLocal` 设为 `false`**，避免本地清单短路接口。
4. 验证京东：点击商品与 GO → 联盟小程序 `proxy` 正常；拼多多：有 `miniPath` 时直跳，仅有 H5 时复制链行为符合预期。
5. 可选：关闭本地优先后，京东侧会请求 `jd/promotion/spread-url`（当前后端从表读 `spread_url`，与列表一致即可）。

---

## 4. 当前未覆盖 / 待产品决策的后端需求

| 模块 | 现状 | 若要做，后端建议 |
|------|------|------------------|
| 惠民卡顶栏搜索 | 无提交逻辑 | `GET /jd/benefit/goods?q=&scene=` 等分页检索，或独立搜索聚合接口 |
| 我的 → 惠民卡订单 | 仅占位 Toast | 订单主表 + 与联盟 PID/用户 openid 关联 + 列表/详情 API；依赖京东/拼多多订单回传或手动导入策略 |
| 动态转链 | `spread-url` 现从表读取 | 对接京东联盟 / 多多进宝开放平台，在 `getSpreadUrl` 内实时转链并落库或缓存 |

---

## 5. 文档维护

- 接口变更时同步更新 `doc/backend/API_DOC.md` §7.1。  
- 表结构或种子变更时同步更新 `doc/惠民卡_联盟数据与同步.md`。
