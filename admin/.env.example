# 开发：复制为 .env.development，使用相对路径走 Vite 代理（推荐）
# VITE_API_BASE=/api/v1
# VITE_PROXY_TARGET=http://127.0.0.1:3000
# 若代理目标端口只接受 HTTPS，必须写 https://（写 http 会得到 nginx 400 HTML，前端常显示为「HTTP 400 错误请求」）

# 生产构建：
# · 若 Nginx 与页面同域反代 /api（见 deploy/nginx-community-admin-31443.conf），用相对路径：
#   VITE_API_BASE=/api/v1
# · 若 API 在另一域名/端口，写完整 URL，例如
#   VITE_API_BASE=https://你的域名:31443/api/v1
