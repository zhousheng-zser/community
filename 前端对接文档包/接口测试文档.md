# 接口测试文档

> **版本**: v1.0  
> **基础URL**: `http://localhost:3001/api/v1`  
> **更新日期**: 2026-04-22

---

## 测试环境准备

### 1. 启动后端服务

```bash
cd /root/community-backend/backend
npm run dev
```

### 2. 确认服务运行

```bash
curl http://localhost:3001/api/v1/health
```

---

## 一、认证模块测试

### 1.1 用户登录测试

```bash
# 账号密码登录
curl -X POST http://localhost:3001/api/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{"phone":"13800138000","password":"password123"}'
```

**预期响应**：
```json
{
  "code": 0,
  "data": {
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "user": {...}
  }
}
```

**验证命令**：
```bash
# 保存token到变量
TOKEN=$(curl -s -X POST http://localhost:3001/api/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{"phone":"13800138000","password":"password123"}' | jq -r '.data.token')
echo "Token: $TOKEN"
```

### 1.2 管理员登录测试

```bash
curl -X POST http://localhost:3001/api/v1/auth/admin/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123"}'
```

### 1.3 服务商后台登录测试

```bash
curl -X POST http://localhost:3001/api/v1/service-provider-portal/login \
  -H "Content-Type: application/json" \
  -d '{"phone":"13900139000","password":"password123"}'
```

### 1.4 商家后台登录测试

```bash
curl -X POST http://localhost:3001/api/v1/merchant-portal/login \
  -H "Content-Type: application/json" \
  -d '{"phone":"13700137000","password":"password123"}'
```

---

## 二、用户模块测试

### 2.1 获取用户信息

```bash
curl -X GET http://localhost:3001/api/v1/user/profile \
  -H "Authorization: Bearer $TOKEN"
```

### 2.2 更新用户信息

```bash
curl -X PATCH http://localhost:3001/api/v1/user/profile \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"nickname":"测试用户"}'
```

### 2.3 获取用户地址列表

```bash
curl -X GET http://localhost:3001/api/v1/user/addresses \
  -H "Authorization: Bearer $TOKEN"
```

### 2.4 添加用户地址

```bash
curl -X POST http://localhost:3001/api/v1/user/addresses \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "contact":"张三",
    "phone":"13800138000",
    "province":"北京市",
    "city":"北京市",
    "district":"朝阳区",
    "detail":"1号楼101室",
    "is_default":true
  }'
```

---

## 三、核心数据模块测试

### 3.1 获取轮播图

```bash
curl -X GET http://localhost:3001/api/v1/core/banners
```

### 3.2 获取服务分类

```bash
curl -X GET http://localhost:3001/api/v1/core/categories
```

### 3.3 获取热门服务

```bash
curl -X GET http://localhost:3001/api/v1/core/hot-services
```

### 3.4 获取服务列表

```bash
curl -X GET "http://localhost:3001/api/v1/core/services?community_id=1"
```

### 3.5 获取服务详情

```bash
curl -X GET http://localhost:3001/api/v1/core/services/1
```

### 3.6 获取技工列表

```bash
curl -X GET "http://localhost:3001/api/v1/core/workers?community_id=1"
```

### 3.7 获取技工详情

```bash
curl -X GET http://localhost:3001/api/v1/core/workers/1
```

---

## 四、服务订单模块测试

### 4.1 创建服务订单

```bash
curl -X POST http://localhost:3001/api/v1/service-orders \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "service_id":1,
    "worker_id":1,
    "community_id":1,
    "address_snapshot":{
      "contact":"张三",
      "contact_phone":"13800138000",
      "detail":"1号楼101室"
    },
    "appointment_time":"2026-04-23 10:00:00",
    "remark":"请准时上门",
    "qty":1
  }'
```

### 4.2 获取我的订单列表

```bash
curl -X GET http://localhost:3001/api/v1/service-orders/my \
  -H "Authorization: Bearer $TOKEN"
```

### 4.3 模拟支付

```bash
curl -X POST http://localhost:3001/api/v1/service-orders/1/mock-pay \
  -H "Authorization: Bearer $TOKEN"
```

### 4.4 确认完成

```bash
curl -X POST http://localhost:3001/api/v1/service-orders/1/confirm \
  -H "Authorization: Bearer $TOKEN"
```

---

## 五、邻里帮帮模块测试

### 5.1 创建帮帮订单

```bash
curl -X POST http://localhost:3001/api/v1/neighbor-assist \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "assist_type":"take",
    "title":"帮忙取快递",
    "description":"帮忙去菜鸟驿站取快递",
    "address_snapshot":{
      "contact":"张三",
      "contact_phone":"13800138000",
      "detail":"1号楼101室"
    },
    "reward_amount":5.00,
    "community_id":1
  }'
```

### 5.2 获取我的帮帮订单

```bash
curl -X GET http://localhost:3001/api/v1/neighbor-assist/my \
  -H "Authorization: Bearer $TOKEN"
```

### 5.3 获取待接单池（技工）

```bash
# 先获取技工token
WORKER_TOKEN=$(curl -s -X POST http://localhost:3001/api/v1/worker-portal/login \
  -H "Content-Type: application/json" \
  -d '{"phone":"13900139001","password":"password123"}' | jq -r '.data.token')

curl -X GET "http://localhost:3001/api/v1/neighbor-assist/pool?community_id=1&limit=50" \
  -H "Authorization: Bearer $WORKER_TOKEN"
```

### 5.4 抢单

```bash
curl -X POST http://localhost:3001/api/v1/neighbor-assist/1/grab \
  -H "Authorization: Bearer $WORKER_TOKEN"
```

### 5.5 完成订单

```bash
curl -X POST http://localhost:3001/api/v1/neighbor-assist/1/complete \
  -H "Authorization: Bearer $WORKER_TOKEN"
```

---

## 六、本地集市模块测试

### 6.1 获取店铺列表

```bash
curl -X GET http://localhost:3001/api/v1/market/shops
```

### 6.2 获取店铺详情

```bash
curl -X GET http://localhost:3001/api/v1/market/shops/1
```

### 6.3 获取店铺商品

```bash
curl -X GET "http://localhost:3001/api/v1/market/shops/1/goods"
```

### 6.4 获取商品详情

```bash
curl -X GET http://localhost:3001/api/v1/market/goods/1
```

### 6.5 购物车操作

```bash
# 获取购物车
curl -X GET http://localhost:3001/api/v1/market/cart \
  -H "Authorization: Bearer $TOKEN"

# 添加购物车
curl -X POST http://localhost:3001/api/v1/market/cart/items \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"goods_id":1,"sku_id":1,"quantity":2}'

# 更新购物车
curl -X PUT http://localhost:3001/api/v1/market/cart/items/1 \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"quantity":3}'

# 删除购物车商品
curl -X DELETE http://localhost:3001/api/v1/market/cart/items/1 \
  -H "Authorization: Bearer $TOKEN"
```

### 6.6 订单操作

```bash
# 订单预览
curl -X POST http://localhost:3001/api/v1/market/orders/preview \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"cart_item_ids":[1,2],"address_id":1}'

# 创建订单
curl -X POST http://localhost:3001/api/v1/market/orders \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"cart_item_ids":[1,2],"address_id":1,"remark":"尽快配送"}'

# 获取我的订单
curl -X GET http://localhost:3001/api/v1/market/orders \
  -H "Authorization: Bearer $TOKEN"

# 获取订单详情
curl -X GET http://localhost:3001/api/v1/market/orders/MO202604220001 \
  -H "Authorization: Bearer $TOKEN"

# 取消订单
curl -X POST http://localhost:3001/api/v1/market/orders/MO202604220001/cancel \
  -H "Authorization: Bearer $TOKEN"
```

### 6.7 支付操作

```bash
# 创建支付
curl -X POST http://localhost:3001/api/v1/market/payments/create \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"order_no":"MO202604220001","pay_method":"wechat"}'

# 查询支付状态
curl -X GET "http://localhost:3001/api/v1/market/payments/status?order_no=MO202604220001" \
  -H "Authorization: Bearer $TOKEN"

# 模拟支付成功
curl -X POST http://localhost:3001/api/v1/market/payments/mock-success \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"order_no":"MO202604220001"}'
```

---

## 七、九州中台管理模块测试

### 7.1 管理员登录

```bash
ADMIN_TOKEN=$(curl -s -X POST http://localhost:3001/api/v1/auth/admin/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123"}' | jq -r '.data.token')
echo "Admin Token: $ADMIN_TOKEN"
```

### 7.2 统计概览

```bash
curl -X GET http://localhost:3001/api/v1/admin/stats/overview \
  -H "Authorization: Bearer $ADMIN_TOKEN"
```

### 7.3 用户管理

```bash
# 获取用户列表
curl -X GET "http://localhost:3001/api/v1/admin/users?page=1&limit=20" \
  -H "Authorization: Bearer $ADMIN_TOKEN"

# 获取用户详情
curl -X GET http://localhost:3001/api/v1/admin/users/1 \
  -H "Authorization: Bearer $ADMIN_TOKEN"
```

### 7.4 技工申请管理

```bash
# 获取技工申请列表
curl -X GET "http://localhost:3001/api/v1/admin/worker-applications?status=pending" \
  -H "Authorization: Bearer $ADMIN_TOKEN"

# 审核技工申请
curl -X PUT http://localhost:3001/api/v1/admin/worker-applications/1 \
  -H "Authorization: Bearer $ADMIN_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"status":"approved","remark":"审核通过"}'
```

### 7.5 派单管理

```bash
# 获取派单队列
curl -X GET http://localhost:3001/api/v1/admin/dispatch-queue \
  -H "Authorization: Bearer $ADMIN_TOKEN"

# 分配服务订单
curl -X POST http://localhost:3001/api/v1/admin/service-orders/1/assign \
  -H "Authorization: Bearer $ADMIN_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"worker_id":1}'

# 分配邻里帮帮订单
curl -X POST http://localhost:3001/api/v1/admin/neighbor-assist/orders/1/assign \
  -H "Authorization: Bearer $ADMIN_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"worker_id":1}'
```

### 7.6 本地集市管理

```bash
# 获取店铺列表
curl -X GET http://localhost:3001/api/v1/admin/market-shops \
  -H "Authorization: Bearer $ADMIN_TOKEN"

# 创建店铺
curl -X POST http://localhost:3001/api/v1/admin/market-shops \
  -H "Authorization: Bearer $ADMIN_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "name":"测试店铺",
    "contact_name":"张三",
    "phone":"13800138000",
    "address":"测试地址",
    "community_id":1
  }'

# 获取商品列表
curl -X GET "http://localhost:3001/api/v1/admin/market-goods?shop_id=1" \
  -H "Authorization: Bearer $ADMIN_TOKEN"

# 创建商品
curl -X POST http://localhost:3001/api/v1/admin/market-goods \
  -H "Authorization: Bearer $ADMIN_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "shop_id":1,
    "name":"测试商品",
    "category_id":1,
    "price":10.00,
    "stock":100
  }'
```

### 7.7 退款管理

```bash
# 获取退款列表
curl -X GET http://localhost:3001/api/v1/admin/refunds \
  -H "Authorization: Bearer $ADMIN_TOKEN"

# 审核退款
curl -X POST http://localhost:3001/api/v1/admin/refunds/1/review \
  -H "Authorization: Bearer $ADMIN_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"status":"approved","remark":"同意退款"}'

# 执行退款
curl -X POST http://localhost:3001/api/v1/admin/refunds/1/execute \
  -H "Authorization: Bearer $ADMIN_TOKEN"
```

### 7.8 社区管理

```bash
# 获取社区列表
curl -X GET "http://localhost:3001/api/v1/admin/communities/list?page=1&limit=20" \
  -H "Authorization: Bearer $ADMIN_TOKEN"

# 创建社区
curl -X POST http://localhost:3001/api/v1/admin/communities \
  -H "Authorization: Bearer $ADMIN_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "name":"测试社区",
    "address":"北京市朝阳区测试路1号",
    "contact_phone":"010-12345678",
    "longitude":116.4074,
    "latitude":39.9042,
    "service_radius":3000
  }'

# 获取社区统计
curl -X GET http://localhost:3001/api/v1/admin/communities/1/stats \
  -H "Authorization: Bearer $ADMIN_TOKEN"
```

---

## 八、服务商后台模块测试

### 8.1 服务商登录

```bash
PROVIDER_TOKEN=$(curl -s -X POST http://localhost:3001/api/v1/service-provider-portal/login \
  -H "Content-Type: application/json" \
  -d '{"phone":"13900139000","password":"password123"}' | jq -r '.data.token')
echo "Provider Token: $PROVIDER_TOKEN"
```

### 8.2 仪表盘

```bash
curl -X GET http://localhost:3001/api/v1/service-provider-portal/dashboard \
  -H "Authorization: Bearer $PROVIDER_TOKEN"
```

### 8.3 订单管理

```bash
# 获取订单列表
curl -X GET http://localhost:3001/api/v1/service-provider-portal/orders \
  -H "Authorization: Bearer $PROVIDER_TOKEN"

# 接单
curl -X POST http://localhost:3001/api/v1/service-provider-portal/orders/1/accept \
  -H "Authorization: Bearer $PROVIDER_TOKEN"

# 打卡
curl -X POST http://localhost:3001/api/v1/service-provider-portal/orders/1/check-in \
  -H "Authorization: Bearer $PROVIDER_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"latitude":39.9042,"longitude":116.4074,"photos":["https://..."]}'

# 完成订单
curl -X POST http://localhost:3001/api/v1/service-provider-portal/orders/1/complete \
  -H "Authorization: Bearer $PROVIDER_TOKEN"
```

---

## 九、商家后台模块测试

### 9.1 商家登录

```bash
MERCHANT_TOKEN=$(curl -s -X POST http://localhost:3001/api/v1/merchant-portal/login \
  -H "Content-Type: application/json" \
  -d '{"phone":"13700137000","password":"password123"}' | jq -r '.data.token')
echo "Merchant Token: $MERCHANT_TOKEN"
```

### 9.2 仪表盘

```bash
curl -X GET http://localhost:3001/api/v1/market/merchant/dashboard \
  -H "Authorization: Bearer $MERCHANT_TOKEN"
```

### 9.3 商品管理

```bash
# 获取商品列表
curl -X GET http://localhost:3001/api/v1/market/merchant/goods \
  -H "Authorization: Bearer $MERCHANT_TOKEN"

# 创建商品
curl -X POST http://localhost:3001/api/v1/market/merchant/goods \
  -H "Authorization: Bearer $MERCHANT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "name":"测试商品",
    "category_id":1,
    "price":10.00,
    "stock":100,
    "image_urls":["https://..."],
    "description":"测试商品描述"
  }'

# 补货
curl -X POST http://localhost:3001/api/v1/market/merchant/goods/1/restock \
  -H "Authorization: Bearer $MERCHANT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"quantity":50}'

# 上下架
curl -X POST http://localhost:3001/api/v1/market/merchant/goods/1/shelf \
  -H "Authorization: Bearer $MERCHANT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"status":"active"}'
```

### 9.4 订单管理

```bash
# 获取订单列表
curl -X GET http://localhost:3001/api/v1/market/merchant/orders \
  -H "Authorization: Bearer $MERCHANT_TOKEN"

# 订单操作（发货）
curl -X POST http://localhost:3001/api/v1/market/merchant/orders/MO202604220001/action \
  -H "Authorization: Bearer $MERCHANT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"action":"ship","tracking_no":"SF1234567890"}'
```

---

## 十、技工端模块测试

### 10.1 技工登录

```bash
WORKER_TOKEN=$(curl -s -X POST http://localhost:3001/api/v1/worker-portal/login \
  -H "Content-Type: application/json" \
  -d '{"phone":"13900139001","password":"password123"}' | jq -r '.data.token')
echo "Worker Token: $WORKER_TOKEN"
```

### 10.2 订单操作

```bash
# 获取订单列表
curl -X GET http://localhost:3001/api/v1/worker/orders \
  -H "Authorization: Bearer $WORKER_TOKEN"

# 接单
curl -X POST http://localhost:3001/api/v1/worker/orders/1/accept \
  -H "Authorization: Bearer $WORKER_TOKEN"

# 打卡
curl -X POST http://localhost:3001/api/v1/worker/orders/1/check-in \
  -H "Authorization: Bearer $WORKER_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"latitude":39.9042,"longitude":116.4074,"photos":["https://..."]}'

# 上传凭证
curl -X POST http://localhost:3001/api/v1/worker/orders/1/evidence \
  -H "Authorization: Bearer $WORKER_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"photos":["https://..."],"description":"服务完成照片"}'

# 完成订单
curl -X POST http://localhost:3001/api/v1/worker/orders/1/complete \
  -H "Authorization: Bearer $WORKER_TOKEN"
```

---

## 十一、数据库验证命令

### 11.1 连接数据库

```bash
mysql -u root -p'CommunityPwd123!' community_db
```

### 11.2 验证用户数据

```sql
-- 查看用户列表
SELECT id, nickname, phone, community_id, created_at FROM users ORDER BY id DESC LIMIT 10;

-- 查看用户地址
SELECT * FROM user_addresses WHERE user_id = 1;
```

### 11.3 验证订单数据

```sql
-- 查看服务订单
SELECT id, order_no, user_id, worker_id, status, total_amount, created_at 
FROM service_orders ORDER BY id DESC LIMIT 10;

-- 查看集市订单
SELECT id, order_no, user_id, shop_id, status, total_amount, created_at 
FROM market_orders ORDER BY id DESC LIMIT 10;

-- 查看邻里帮帮订单
SELECT id, user_id, worker_id, assist_type, status, reward_amount, created_at 
FROM neighbor_assist_orders ORDER BY id DESC LIMIT 10;
```

### 11.4 验证技工数据

```sql
-- 查看技工列表
SELECT wp.id, wp.user_id, u.nickname, u.phone, wp.community_id, wp.status, wp.rating
FROM worker_profiles wp
JOIN users u ON wp.user_id = u.id
ORDER BY wp.id DESC LIMIT 10;
```

### 11.5 验证支付数据

```sql
-- 查看支付记录
SELECT * FROM payments ORDER BY id DESC LIMIT 10;

-- 查看退款记录
SELECT * FROM refunds ORDER BY id DESC LIMIT 10;
```

---

## 十二、自动化测试脚本

### 12.1 完整业务链测试脚本

```bash
#!/bin/bash
# 文件: /root/community-backend/backend/test-api.sh

BASE_URL="http://localhost:3001/api/v1"

echo "===== 1. 用户登录 ====="
TOKEN=$(curl -s -X POST $BASE_URL/auth/login \
  -H "Content-Type: application/json" \
  -d '{"phone":"13800138000","password":"password123"}' | jq -r '.data.token')
echo "Token: $TOKEN"

echo ""
echo "===== 2. 获取用户信息 ====="
curl -s -X GET $BASE_URL/user/profile \
  -H "Authorization: Bearer $TOKEN" | jq .

echo ""
echo "===== 3. 获取服务列表 ====="
curl -s -X GET "$BASE_URL/core/services?community_id=1" | jq .

echo ""
echo "===== 4. 创建服务订单 ====="
ORDER_ID=$(curl -s -X POST $BASE_URL/service-orders \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "service_id":1,
    "worker_id":1,
    "community_id":1,
    "address_snapshot":{"contact":"张三","contact_phone":"13800138000","detail":"1号楼101室"},
    "appointment_time":"2026-04-23 10:00:00",
    "qty":1
  }' | jq -r '.data.id')
echo "Order ID: $ORDER_ID"

echo ""
echo "===== 5. 模拟支付 ====="
curl -s -X POST $BASE_URL/service-orders/$ORDER_ID/mock-pay \
  -H "Authorization: Bearer $TOKEN" | jq .

echo ""
echo "===== 测试完成 ====="
```

### 12.2 运行测试脚本

```bash
chmod +x /root/community-backend/backend/test-api.sh
./test-api.sh
```

---

**文档版本**: v1.0  
**最后更新**: 2026-04-22
