# 惠民卡接口反代：主社区 API 仍监听 3000，本仓库 backend 单独监听 3001（见 ecosystem.benefit.pm2.cjs）
# 将下列 location 并入现有 server { ... }，并放在「通用 /api/v1/」规则之前，避免被其它 location 抢走。
#
#   upstream community_benefit { server 127.0.0.1:3001; }
#
#   location ~ ^/api/v1/(jd|pdd|benefit)(/|$) {
#       proxy_http_version 1.1;
#       proxy_set_header Host $host;
#       proxy_set_header X-Real-IP $remote_addr;
#       proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#       proxy_set_header X-Forwarded-Proto $scheme;
#       proxy_pass http://community_benefit;
#   }
