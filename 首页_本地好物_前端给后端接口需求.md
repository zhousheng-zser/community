# 首页「本地商城」接口需求（前端 -> 后端）

## 1. 背景与目标

前端已切换为“纯真实商品模式”，不再使用任何兜底/模拟商品。  
因此需要后端按“模块化商品池”提供接口数据，且商品必须来源于真实店铺商品。

另外，前端会基于用户坐标做二次过滤：**过滤掉 5km 以外店铺的商品**。  
为避免前后不一致，建议后端也按同样规则过滤并返回距离字段，前端再做一次保护性过滤。

## 2. 本地商城全量模块清单（含首页与子页）

以下模块**要么在首页直接展示商品**，要么**点击进入后展示商品列表**，均需要后端提供真实店铺商品数据（或挂载关系 + JOIN 出商品）。

### 2.1 首页内嵌商品区

1. `每日上新`（短列表）
2. `热卖TOP榜`（短列表）
3. `周期榜单`（多 Tab，每个 Tab 一组商品）
4. `底部分类 Feed`（多 Tab，支持分页）

### 2.2 金刚区四个专区（点击进入 `push-goods-list`）

| 前端展示名 | `zone_id` | 说明 |
|-----------|-----------|------|
| 爆款专区 | `1` | 列表页默认瀑布流布局 |
| 礼物专区 | `2` | 可选横向子分类（如「送长辈」），见专区接口 `sub_categories` |
| 本地商城甄选 | `3` | 可选左侧类目栏，见 `sidebar_categories`；切换类目时带 `sidebar_category` 重拉 |
| 高佣专区 | `4` | 高佣单列布局 |

### 2.3 三花聚顶导购窗（点击进入 `push-channel`）

首页三个入口与后端 `channel_key` 约定如下（与 URL 参数 `title` 对应）：

| 入口文案 | `channel_key` |
|---------|----------------|
| 品牌好货 | `brand_goods` |
| 寻找九州好物 | `jiuzhou_haowu` |
| 秋冬好物 | `autumn_winter` |

### 2.4 仅展示素材、不要求商品列表接口的模块

- 顶部 Banner 轮播：可为运营图；若也要跳转到某商品池，可再单独约定。

### 2.5 说明（与历史文档的差异）

此前一版文档曾把「分类金刚、导购窗」排除在接口范围外，导致 **`push-goods-list` / `push-channel` 仍使用前端假数据**，出现「点进去商品与图片不对」的现象。  
当前前端已改为调用 **专区接口** 与 **频道接口**；请后端按 **第 5.3、5.4 节** 实现并联调。

## 3. 数据来源原则（必须）

- 商品必须来自真实店铺商品主表（本地集市：`market_goods` 及关联 `market_shops`）。
- 模块表只做“挂载关系”，不要复制一份商品详情，避免多份数据不一致。
- 每条商品需能追溯到 `shop_id` 与 `product_id`。

## 4. 建议表设计（按模块分表）

> 我们前端按“每个模块一个商品池”理解，后端可按以下方案实现（命名可调整，但语义请保持一致）

### 4.1 每日上新

- 表：`lg_home_daily_news_products`
- 字段建议：
  - `id` 主键
  - `product_id` 商品ID（关联真实商品）
  - `shop_id` 店铺ID（可冗余，便于查询）
  - `sort` 排序
  - `status` 上下架状态（1启用/0停用）
  - `start_at` / `end_at` 生效时间（可选）
  - `created_at` / `updated_at`

### 4.2 热卖TOP榜

- 表：`lg_home_top_sales_products`
- 字段建议同上，另可加：
  - `rank_no` 榜单位次（可选，若不传前端按顺序展示）

### 4.3 周期榜单（多 Tab）

- 模块定义表：`lg_home_periodic_modules`
  - `id`, `module_name`, `sort`, `status`, `created_at`, `updated_at`
- 模块商品关系表：`lg_home_periodic_module_products`
  - `id`, `module_id`, `product_id`, `shop_id`, `sort`, `status`, `created_at`, `updated_at`

### 4.4 底部分类 Feed（多 Tab + 分页）

- 模块定义表：`lg_home_feed_modules`
  - `id`, `module_name`, `sort`, `status`, `created_at`, `updated_at`
- 模块商品关系表：`lg_home_feed_module_products`
  - `id`, `module_id`, `product_id`, `shop_id`, `sort`, `status`, `created_at`, `updated_at`

### 4.5 金刚区「专区」商品池（对应 `zone_id` 1～4）

- 专区定义表：`lg_home_zones`
  - `id`（即前端 `zone_id`）, `zone_code`, `name`, `sort`, `status`, `created_at`, `updated_at`
- 专区商品关系表：`lg_home_zone_products`
  - `id`, `zone_id`, `product_id`, `shop_id`, `sort`, `status`, `created_at`, `updated_at`

礼物专区若需要「子分类」维度，可增加：

- `lg_home_zone_gift_subcategories`：`zone_id`, `sub_code`, `name`, `cover_image`, `sort`  
  商品挂载表可增加 `gift_sub_code` 与 `product_id` 关联，或由后端按规则归类。

本地商城甄选若需要左侧类目：

- `lg_home_zone_sidebar_categories`：`zone_id`, `category_name`, `sort`  
  商品挂载表可增加 `sidebar_category` 字段用于筛选。

### 4.6 导购窗「频道」商品池（对应 `channel_key`）

- 频道定义表：`lg_home_channels`
  - `id`, `channel_key`, `title`, `sort`, `status`, `created_at`, `updated_at`
- 频道商品关系表：`lg_home_channel_products`
  - `id`, `channel_id`, `product_id`, `shop_id`, `sort`, `status`, `created_at`, `updated_at`

若频道内需多 Tab（如「九州好食 / 九州好味 / 九州好物」），可增加：

- `lg_home_channel_tabs`：`channel_id`, `tab_name`, `sort`
- `lg_home_channel_tab_products`：`tab_id`, `product_id`, `shop_id`, `sort`

## 5. 接口约定（请尽量按此实现）

前端已按下面接口接入（`util` 的 `baseUrl` 已含 `/api/v1`，实现路由时请挂载在 **`/api/v1/local-goods-home/...`**）：

## 5.1 首页模块聚合接口

- `GET /api/v1/local-goods-home/modules`
- Query 参数：
  - `user_lat` number（可选）
  - `user_lng` number（可选）
  - `distance_km` number（默认 5，前端会传 5）

### 返回结构（建议）

```json
{
  "code": 0,
  "data": {
    "daily_news": [
      {
        "id": 1001,
        "name": "商品名",
        "pay_price": 99.9,
        "rebate_amount": 12.3,
        "main_image": "https://...",
        "shop_id": 11,
        "distance_km": 2.4
      }
    ],
    "top_sales": [
      {
        "id": 2001,
        "name": "商品名",
        "pay_price": 59.9,
        "rebate_amount": 9.1,
        "main_image": "https://...",
        "shop_id": 12,
        "distance_km": 1.8
      }
    ],
    "periodic_modules": [
      {
        "module_name": "今日主推",
        "goods_list": [
          {
            "id": 3001,
            "name": "商品名",
            "pay_price": 39.9,
            "rebate_amount": 4.6,
            "main_image": "https://...",
            "shop_id": 15,
            "distance_km": 3.1
          }
        ]
      }
    ],
    "feed_modules": [
      {
        "module_name": "高佣推荐",
        "page": 1,
        "has_more": true,
        "goods_list": [
          {
            "id": 4001,
            "name": "商品名",
            "pay_price": 29.9,
            "rebate_amount": 6.2,
            "main_image": "https://...",
            "shop_id": 20,
            "distance_km": 0.9
          }
        ]
      }
    ]
  }
}
```

## 5.2 Feed 分页接口

- `GET /api/v1/local-goods-home/feed-products`
- Query 参数：
  - `module_name` string（必填，对应 Feed Tab 名）
  - `page` number（必填，从 1 开始）
  - `page_size` number（必填，前端默认 10）
  - `user_lat` number（可选）
  - `user_lng` number（可选）
  - `distance_km` number（前端传 5）

### 返回结构（建议）

```json
{
  "code": 0,
  "data": {
    "list": [
      {
        "id": 5001,
        "name": "商品名",
        "pay_price": 45.9,
        "rebate_amount": 7.8,
        "main_image": "https://...",
        "shop_id": 31,
        "distance_km": 4.6
      }
    ],
    "has_more": true
  }
}
```

## 5.3 金刚区专区商品列表

- `GET /api/v1/local-goods-home/zone-products`
- Query 参数：
  - `zone_id` number（必填，`1`～`4`，含义见第 2.2 节）
  - `user_lat` / `user_lng` / `distance_km`（与首页一致，前端默认 `distance_km=5`）
  - `page` / `page_size`（分页，首页外列表建议 `page_size` 30～50）
  - `gift_sub_category` string（可选，礼物专区子分类名，与 `sub_categories[].name` 对齐）
  - `sidebar_category` string（可选，本地商城甄选左侧类目名，与 `sidebar_categories` 对齐）

### 返回结构（建议）

```json
{
  "code": 0,
  "data": {
    "list": [
      {
        "id": 6001,
        "name": "商品名",
        "pay_price": 19.9,
        "rebate_amount": 2.1,
        "main_image": "https://...",
        "shop_id": 9,
        "distance_km": 2.1
      }
    ],
    "sub_categories": [
      { "name": "送长辈", "image": "https://..." }
    ],
    "sidebar_categories": ["家庭清洁", "生鲜水果"]
  }
}
```

说明：`sub_categories` / `sidebar_categories` 可仅在对应专区返回；无侧栏/子分类时返回空数组即可。

## 5.4 导购窗频道商品列表

- `GET /api/v1/local-goods-home/channel-products`
- Query 参数：
  - `channel_key` string（必填：`brand_goods` | `jiuzhou_haowu` | `autumn_winter`）
  - `user_lat` / `user_lng` / `distance_km`（同上）
  - `page` / `page_size`（可选）

### 返回结构 A：多 Tab（推荐「寻找九州好物」类频道）

```json
{
  "code": 0,
  "data": {
    "tab_groups": [
      {
        "tab_name": "九州好食",
        "goods_list": [
          {
            "id": 7001,
            "name": "商品名",
            "pay_price": 19.9,
            "rebate_amount": 2.1,
            "main_image": "https://...",
            "shop_id": 9,
            "distance_km": 2.1
          }
        ]
      }
    ]
  }
}
```

### 返回结构 B：单列表（品牌好货 / 秋冬好物可先用此结构）

```json
{
  "code": 0,
  "data": {
    "list": [
      {
        "id": 8001,
        "name": "商品名",
        "pay_price": 19.9,
        "rebate_amount": 2.1,
        "main_image": "https://...",
        "shop_id": 9,
        "distance_km": 2.1
      }
    ]
  }
}
```

## 6. 前端字段最小依赖

每个商品最少需要：

- `id`
- `name`（或 `title`/`goods_name`，建议统一 `name`）
- `pay_price`（或 `price`）
- `rebate_amount`（或 `comm`）
- `main_image`（或 `image`/`cover_image`）
- `distance_km`（非常关键，用于 5km 过滤）
- `shop_id`

**商品主图补充说明（避免小程序不显示图）：**

- 前端会统一用 `utils/imgUrl` 处理：支持 `http/https` 完整地址，或站点相对路径（如 `/uploads/xxx.jpg`，会拼 `imageBaseUrl`）。
- 若数据库字段名是 `mainPicture` / `main_picture` 等，也会一并兼容；但请至少保证**有一条非空的主图字段**。
- 若只返回 `null`、空字符串或错误路径，小程序里会表现为白块/灰占位。

## 7. 距离过滤规则（请后端与前端统一）

- 目标：仅展示距离用户 **<= 5km** 的店铺商品。
- 建议后端先过滤后返回；前端会再次保护性过滤。
- `distance_km` 建议保留 1~2 位小数。
- 无定位时：
  - 建议返回空列表，或返回 `code + message` 提示“缺少坐标，无法计算距离”；
  - 不建议直接返回全量商品，避免误展示远距离商品。

## 8. 错误码与容错建议

- `code=0` 成功
- 非 0 失败，返回 `message`
- 建议补充：
  - 模块不存在
  - 坐标缺失/非法
  - 分页参数非法
  - 模块无数据（可成功返回空数组）

## 9. 联调检查清单

- [ ] 首页四个内嵌模块（每日上新 / 热卖 / 周期 / Feed）均能返回真实店铺商品
- [ ] `zone-products`：`zone_id` 1～4 列表与主图正确；礼物子分类、甄选侧栏数据与筛选参数对齐
- [ ] `channel-products`：三个 `channel_key` 数据正确；多 Tab 时 `tab_groups` 与单列表两种结构至少实现一种
- [ ] 距离字段稳定返回且单位为 km
- [ ] 前后端对 5km 规则一致
- [ ] Feed 支持分页，`has_more` 正确
- [ ] 下架/禁用商品不会出现在模块结果中
- [ ] 同一商品重复挂载时去重策略明确（建议按 `product_id` 去重）

## 10. 当前前端实现状态说明

前端已完成以下改造：

- 移除本地商城首页兜底商品与假分页数据
- 首页模块依赖 `local-goods-home/modules` + `feed-products`
- **`push-goods-list` 已改为请求 `local-goods-home/zone-products`**（不再使用随机占位商品）
- **`push-channel` 已改为请求 `local-goods-home/channel-products`**（不再使用写死列表）
- 商品主图统一走 `utils/imgUrl` + 多字段兼容；列表与首页均做 5km 保护性过滤

后续只要后端按上述接口返回数据，即可直接联调。
