# 「首页」Tab 内层首页与「我的」：前后端联调需求（详版）

> **范围**  
> 仅 **`pages/index/index` 顶部子 Tab =「首页」** 时的界面（`activeTab === '首页'`，`index.wxml` 中 **`wx:else`**，约 335～446 行）。  
> **不包含**：惠民卡、本地商城、本地集市三个 Tab（见各自文档）。

> **关联代码**：`pages/index/index.js`（`init`）、`pages/index/index.wxml`（内层首页结构）、`pages/tidy-service/tidy-service.js`、`pages/service/service.js`、`pages/worker-detail/worker-detail.js`、`pages/goods-detail/goods-detail.js`、`pages/merchant-detail/merchant-detail.js`、`pages/classify/classify.js`、`pages/user/user.js`。  
> **关联文档**：`doc/API_DOC.md`、`doc/第三阶段_开发文档.md`、`doc/我的_进度文档.md`。  
> **假数据落库与映射**：`doc/内层首页_样例数据包与数据库映射.md`；样例 JSON/SQL 在 `backend/seed-data/home-inner/`，`seed_service_groups.js` 已合并 `service_groups_extra.json`。  
> **接口 + 表结构 + 后端关系（约定全文）**：本文 **第十一～十三章**；**九大类写死 + 下单/派单**：**第二章 2.4**、**第十章 A**；**九州统一收单派单**：**第十章 B**；**小区热卖 / 邻里帮帮双地址**：**第五、六章**。

---

## 一、统一约定（后端与前端共用）

### 1.1 响应格式

- 小程序 `utils/util.js` 约定：HTTP 200 且（`errno == 0` **或** 无 `errno`）时，`resolve(data.data || data)`。
- **后端建议**：统一返回  
  `{ "errno": 0, "data": { ... } }`  
  列表类接口 `data` 可为数组，或 `{ "list": [], "total": n }`（前端需兼容两种形态，见各模块）。

### 1.2 图片 URL

- 相对路径（如 `/uploads/...`）前端经 `imgUrl()` 拼 `config.imageBaseUrl`。
- **后端**：尽量返回**带域名**或**以 `/` 开头的站内路径**；避免仅返回无路径文件名。

### 1.3 鉴权

- 内层首页展示的 **Core 只读接口**（Banner、热门服务、技工列表、精选商品等）产品定位为 **游客可浏览**：**可不鉴权**（与 `API_DOC.md` 第三阶段一致）。
- **入驻类 POST**（`worker/apply` 等）必须 **JWT**。

---

## 二、模块 1：九宫格服务类目（金刚区）

### 2.1 前端现状

| 项目 | 说明 |
|------|------|
| 数据 | `index.js` → `categoryList`，在 `init()` 里写死 9 项，`mapHomeIcon` 处理 `icon` |
| 模板 | `index.wxml`：`navigator` `url="{{item.url}}"`，形如 `../tidy-service/tidy-service?key=tidy` |
| 落地页 | `tidy-service`：`onLoad` 用本地 `getPageConfig(key)`，再 `wx.request` 拉 `GET {baseUrl}/core/service-groups/:key`（**未走** `util.get`，与全局封装不一致） |

### 2.2 后端需要做什么

| 优先级 | 任务 |
|--------|------|
| P0 | 提供 **`GET /api/v1/core/service-groups/:key`**，`:key` 与下表 **slug 完全一致**（区分大小写建议统一小写+下划线）。 |
| P0 | 返回体（建议包在标准 `data` 内）：`{ "categories": [...], "services": [...] }`。 |
| P1 | 管理后台可维护：分组标题、类目 Tab、分组下挂的服务 ID 列表及排序。 |

**`:key` 与首页入口对应表**

| key | 首页展示名 |
|-----|------------|
| `tidy` | 整理收纳 |
| `urgent_fix` | 家修急事 |
| `appliance_clean` | 家电清洗 |
| `pioneer_clean` | 开荒保洁 |
| `mite_remove` | 除螨服务 |
| `furniture_care` | 家具养护 |
| `baby_home` | 宝宝家事 |
| `house_repair` | 房屋修缮 |
| `beauty_home` | 上门美业 |

**`categories` 单项建议字段**

| 字段 | 类型 | 说明 |
|------|------|------|
| `name` | string | 展示名；第一项建议为「热门服务」 |
| `icon_url` | string? | 可选；无则前端可用本地占位 |

**`services` 单项必须与 `GET /api/v1/core/services/:id` 可对齐**

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | int | **必填**，与 `services` 主表一致 |
| `title` | string | 标题（可含「【2小时】」等） |
| `price` | number 或 string | 与详情页一致 |
| `cover_image` | string? | 封面 |
| `sales_count` | int? | 已售，用于展示「已售 n」 |
| `category` | object? | 如 `{ "name": "厨房收纳" }`，用于 Tab 筛选 |

**排序规则**：同一分组内按运营 `sort` 或 `sales_count` 降序，需在接口文档写死一种。

**数据库**：`service_groups(slug, title, …)`、`service_group_items(group_id, service_id, sort)`，或等价实现；保证 `slug` 上表可查。

### 2.3 前端需要调整什么

| 优先级 | 调整项 |
|--------|--------|
| P1 | **`tidy-service.js`**：将 `wx.request` 改为 **`util.get('core/service-groups/' + key)`**，与全局 `baseUrl`、Token、错误处理一致；按 `util` 解析后的结构取 `categories/services`（兼容 `res.data`）。 |
| P2 | **`index.js`**（可选）：若后端增加 **`GET /api/v1/core/home-categories`** 只下发 9 项的 `name/icon/url`，可减少前端写死；否则维持前端静态配置 + 仅落地页走 `service-groups` 也可。 |
| P2 | 九类 URL 与后端 `slug` 变更时同步改 `categoryList` 或改由接口下发。 |

### 2.4 产品约定：「家修急事」～「上门美业」等九类——**展示可写死，下单必须进后台**

| 项 | 约定 |
|----|------|
| **范围** | 九宫格从「整理收纳」到「上门美业」共 9 个入口；其中 **「家修急事」～「上门美业」** 与「整理收纳」一致，均可 **长期用前端 `getPageConfig(key)` / 写死 SKU 列表** 展示，**不强制**依赖 `GET /core/service-groups/:key` 才能发版。 |
| **前端** | 维持 `tidy-service` 本地配置 + 跳转 `service/service?id=`；若采用写死 `id`，须与库内 **`Services.id`（种子导入）一致**，或在首版用「标题 + group_key」下单由后端映射（不推荐，易错）。 |
| **后端（必选）** | 用户点击「下单」后，须有 **统一的到家服务订单** 落库、状态流转、**平台可见的待派单队列**，以及 **将订单指派给实际技工** 的能力（人工派单或规则派单，见 **第十章 A**）。 |
| **`service-groups` 接口** | 降级为 **P1 可选**：用于运营后台逐步替换写死内容；**不接该接口时**，不影响「写死 + 真下单 + 真派单」闭环。 |

---

## 三、模块 2：快捷入口（直约服务商 / 直约技工 / 秒杀 / 领券 / 家事积分商城）

### 3.1 前端现状

| 项目 | 说明 |
|------|------|
| 数据 | `quickActions` 在 `init()` 写死 5 项 |
| 模板 | **所有项** `navigator` 均指向 **`../classify/classify`**（无区分参数） |
| 问题 | 与下方「直约服务商」「直约技工」区块未形成差异化落地；秒杀/领券无独立页 |

### 3.2 后端需要做什么

| 入口 | 后端任务 |
|------|----------|
| 直约服务商 | 无单独首页接口亦可：依赖 **`core/services`** 列表或分类下服务；若做聚合页，可提供 `GET /api/v1/core/services?page=&limit=&category_id=`。 |
| 直约技工 | **`GET /api/v1/core/workers`** 支持分页：`page`、`page_size`；可选 `category` / `keyword`。 |
| 秒杀 | **待产品定稿**：如 `GET /api/v1/promotions/flash-sales` 或给 `services` 增加 `promo_type`、`flash_end_at` 等字段筛选。 |
| 领券 | **待产品定稿**：如 `GET /api/v1/coupons/available`（可领取）、`POST /api/v1/coupons/claim/:id`；或与现有 `wx/user/coupon/:userId` 体系合并设计。 |
| 积分商城 | 积分商品列表 + 兑换规则表（后续迭代）。 |

### 3.3 前端需要调整什么

| 优先级 | 调整项 |
|--------|--------|
| P1 | **`index.wxml`**：五个 `navigator` **不要全部指向 classify**；改为不同 `url` 或 `url` 带 query：`../classify/classify?tab=worker` / `?tab=service` 等（需 `classify.js` `onLoad` 解析并切 Tab / 拉数）。 |
| P1 | **`classify.js`**：当前技工为静态数组；改为 **`util.get('core/workers')`** 分页加载；类目筛选与后端参数对齐。 |
| P2 | 秒杀 / 领券 / 积分：产品定稿前可 **`bindtap` + `wx.showToast({ title: '敬请期待' })`** 或跳转占位页，避免误导进 classify。 |

---

## 四、模块 3：轮播 Banner

### 4.1 前端现状

| 项目 | 说明 |
|------|------|
| 数据 | `banner` 使用本地 `images.bannerHome`、`images.bannerSale` |
| 模板 | `swiper` → `item.imageUrl` |
| 问题 | **未调用** `GET /api/v1/core/banners`；点击 Banner **无跳转** |

### 4.2 后端需要做什么

| 优先级 | 任务 |
|--------|------|
| P0 | **`GET /api/v1/core/banners`**：返回数组，每项至少包含： |

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | int | 可选 |
| `image_url` | string | 全路径或 `/uploads/...` |
| `link_type` | string | 建议枚举：`none` / `service` / `miniprogram` / `h5` |
| `link_value` | string | `service` 时为服务 ID；`h5` 时为 URL；小程序可为 `appId|path` 或分字段 |

| P1 | 管理后台 CRUD Banner、排序、生效时间。 |

**数据表**：已有 `banners` 模型（见第三阶段文档），按上表扩展字段即可。

### 4.3 前端需要调整什么

| 优先级 | 调整项 |
|--------|--------|
| P0 | **`index.js` `init()`**：在 setData 前 **`await util.get('core/banners')`**，映射为 `{ imageUrl: imgUrl(row.image_url), linkType, linkValue }`；失败则保留现有本地图。 |
| P1 | **`index.wxml`**：Banner 包一层 `navigator` 或 **`bindtap`**，按 `link_type` **`wx.navigateTo` 到 `service?id=`** 或复制链接等。 |

---

## 五、模块 4：邻里帮帮

### 5.1 产品定义（地址双端）

**每一项帮帮业务下单时，须采集「起点端地址」与「终点端地址」**（结构相同：联系人、电话、省市区、详细地址、经纬度可选），**前端展示文案**按类型区分（参考 **代取** 的「取货 / 收货」逻辑）：

| `assist_type`（与 `recomm?type=` 一致） | 起点端（字段名统一 `origin_*`） | 终点端（字段名统一 `destination_*`） |
|----------------------------------------|----------------------------------|--------------------------------------|
| `take` 代取 | **取货地址** | **收货地址** |
| `child` 接送小孩 | **接取地址** | **送达地址** |
| `escort` 陪诊 | **汇合/上车地址** | **目的地地址**（如医院门口） |
| `trash` 代扔垃圾 | **上门取件地址** | **投放/交接点地址**（或物业指定点，仍须可填） |
| `pet` 宠物喂养 | **喂养上门地址**（主服务点） | **物资取送/备用地址**（如需代取粮砂等；无则可与起点相同并备注说明） |

**后端**：两条地址均建议 **`JSON 快照`**（`origin_address_snapshot`、`destination_address_snapshot`），结构与用户地址库一致，避免用户改库后订单无法追溯。

### 5.2 前端现状

| 项目 | 说明 |
|------|------|
| 数据 | `knowledgeList` 写死 5 项（代取、接送小孩、陪诊、代扔垃圾、宠物喂养） |
| 跳转 | 各 `url` 指向 `../recomm/recomm?type=...` |
| 标题栏 | 「查看更多」**无** `bindtap`/`navigator` |
| 缺口 | 下单页未统一采集 **双地址**；需在 `recomm` 或独立 **`neighbor-assist-publish`** 表单中按上表展示标签 |

### 5.3 后端需要做什么

| 优先级 | 任务 |
|--------|------|
| P0 | **单一主实体**：**邻里帮帮订单表**（建议 `neighbor_assist_orders`，勿与 `service_orders`、集市订单混用不加类型字段）。 |
| P0 | 字段至少包含：`assist_type`、`user_id`、**`community_id`**（与「小区」维度一致，便于热卖与派单范围）、`origin_address_snapshot`、`destination_address_snapshot`、`amount`、`status`、`assigned_worker_id`（或调度员指派的执行人）、`remark`、`appointment_time`、`createdAt`。 |
| P0 | **下单接口**：`POST /api/v1/neighbor-assist/orders`（或并入统一订单中台路由，但 DB 表独立）；成功后 **推送/可被「九州」管理端拉取**（见 **第十章 B**）。 |
| P1 | 列表：`GET /api/v1/neighbor-assist/orders/my`；管理端：`GET /api/v1/admin/neighbor-assist/orders?status=&community_id=`。 |
| P1 | 与旧 **`orders/publish`**：若历史已用 `publish_orders`，需迁移或 **类型字段** 显式区分 `publish_type=neighbor_assist`，并 **补全双地址**；新开发优先独立表。 |

### 5.4 前端需要调整什么

| 优先级 | 调整项 |
|--------|--------|
| P0 | **下单表单**：根据 `assist_type` 展示 **起点/终点** 两块的标题（上表），均支持选地址簿或地图选点；提交 body 与后端 `origin_*` / `destination_*` 对齐。 |
| P1 | 「查看更多」绑定列表或专题页；`knowledgeList` 可继续写死入口，但 **落地页必须带 type**。 |
| P2 | 可选 `GET /api/v1/core/neighborhood-assist` 下发运营图标与 `assist_type` 枚举。 |

---

## 六、模块 5：小区热卖榜

### 6.1 产品定义（按「小区」统计下单量）

**小区热卖榜**展示的是：**当前用户所属小区（或选定小区）范围内，最近一段时间内被下单最多** 的 **服务 SKU** 和/或 **集市店铺（商店）** 信息，而不是全站 `sales_count` 简单排序。

| 维度 | 说明 |
|------|------|
| **小区** | 须有稳定 `community_id`（与用户绑定小区、或订单收货地址归属小区一致，产品定一种主数据来源）。 |
| **服务榜** | 统计 `service_orders`（及状态满足「有效单」的业务定义，如已支付/已完成）在 `community_id` 下按 **`service_id` 聚合 COUNT** 降序。 |
| **店铺榜**（若首页需展示） | 统计 `market_orders`（或等价）在同一 `community_id` 下按 **`shop_id` 聚合 COUNT** 降序。 |
| **时间窗** | 建议默认近 30 天，可用 Query `days=` 覆盖。 |

### 6.2 前端现状

| 项目 | 说明 |
|------|------|
| 数据 | `config.useCuratedHomeHotList === true` 时用本地假数据；否则请求 **`GET core/services/hot`（当前为全站逻辑）** |
| 二次使用 | `init()` 中 **`core/services/hot` 可能被请求两次**，可合并 |
| 展示 | `hotList`：`id, name, price, image, rank`；跳转服务详情 |
| 筛选 | `hotFilters` 仅展示 |

### 6.3 后端需要做什么

| 优先级 | 任务 |
|--------|------|
| P0 | **新接口（推荐）**：`GET /api/v1/core/community/hot`（或 `GET /api/v1/core/services/hot?community_id=` **且语义改为小区内聚合**）。 |
| P0 | Query：**`community_id` 必填** 或 **从登录用户默认小区解析**；可选 `limit`、`days`、`type=service|shop|all`。 |
| P0 | 返回：至少支持 **服务条目** `{ item_type:'service', id, title, price, cover_image, order_count_in_community, rank }`；若含店铺：`item_type:'shop', id, name, cover_image, order_count_in_community`。 |
| P1 | **数据前提**：`service_orders`、`market_orders`（若统计店铺）须带 **`community_id`** 或与地址解析出的小区一致；历史数据需回填策略。 |
| P1 | `hotFilters` 若可点：带 `category_id` 或 `tag` 在 **本小区聚合结果** 上再筛。 |

**与旧接口关系**：全站 `GET /api/v1/core/services/hot` 可保留给「直约服务商条」等场景；**「小区热卖榜」模块专用小区内聚合接口**，避免混用。

### 6.4 前端需要调整什么

| 优先级 | 调整项 |
|--------|--------|
| P0 | **热卖榜数据源** 改为 **`GET .../community/hot`**（并传 `community_id` 或依赖后端从 token 解析）；`useCuratedHomeHotList` 生产环境 `false`。 |
| P1 | 若返回含 `shop`：增加跳转 `market-shop` 或店铺详情；纯服务则维持 `service/service?id=`。 |
| P1 | 合并重复请求；直约服务商条若仍用全站 hot，与热卖榜 **分开请求** 或后端提供双字段。 |
| P2 | `hot-filter` 点击后带参重拉 **本小区** 热卖。 |

---

## 七、模块 6：直约服务商

### 7.1 前端现状

| 项目 | 说明 |
|------|------|
| 数据 | 与热卖榜共用 `core/services/hot` 的前 **4 条**，映射为 `merchantList`：`id, name, sub, image, url` |
| `sub` | 写死为「服务 + id + 单」，**未用**真实订单数 |
| 跳转 | `item.url` → `../merchant-detail/merchant-detail?id=` **（注意：merchant-detail 页目前几乎静态，未按 id 拉服务商）** |
| 「查看全部」 | 跳转 `merchant-detail`，非列表页 |

### 7.2 后端需要做什么

| 优先级 | 任务 |
|--------|------|
| P0 | **服务 SKU** 与 **`GET /api/v1/core/services/:id`** 一致（同 `service/service` 详情）。 |
| P1 | 若「服务商」指**企业主体**：增加 `service_provider_id` / `shop_name` 等字段，并提供 **`GET /api/v1/core/service-providers/:id`** 或 **`GET /api/v1/core/services/:id` 内嵌主体信息**，供 `merchant-detail` 展示。 |
| P1 | `services` 表增加 **`order_count`** 或实时统计，供首页 `sub` 展示「已服务 x 单」（替代假文案）。 |

### 7.3 前端需要调整什么

| 优先级 | 调整项 |
|--------|--------|
| P1 | **`merchant-detail.js`**：`onLoad(options)` 接收 **`id`**，调用 **`util.get('core/services/' + id)`** 或服务商详情接口，替换静态 `merchants`。若产品确认「卡片=服务 SKU」，则详情即 **`service/service`**，首页卡片应 **`navigator` 直接进 `service/service`**，避免 `merchant-detail` 歧义。 |
| P2 | **`index.js`**：`mapMerchantList` 中 `sub` 改为展示接口字段，如 `已售${sales_count}` 或 `已服务${order_count}单`。 |
| P2 | 「查看全部」改为 **`classify`** 或 **`service` 列表页**（与产品一致）。 |

---

## 八、模块 7：直约技工

### 8.1 前端现状

| 项目 | 说明 |
|------|------|
| 数据 | `GET core/workers`，取前 5 条；`orders` 字段映射为 `服务x单` |
| 头像 | **`workerAvatarUrl(w.id)` 本地占位**，未优先使用接口头像 |
| 跳转 | `worker-detail?id=` |

### 8.2 后端需要做什么

| 优先级 | 任务 |
|--------|------|
| P0 | **`GET /api/v1/core/workers`**：仅返回 **审核通过** 且 **上架** 的技工；默认按 **`sort` / 接单数 / 评分** 排序（需文档化）。 |
| P0 | 单项字段建议（与 `worker-detail.js` `normalizeWorker` 兼容）： |

| 字段 | 说明 |
|------|------|
| `id` | int |
| `name` / `real_name` / `nickname` | 至少一个 |
| `orders` / `service_count` / `serviceCount` | 服务单数（数字） |
| `avatar_url` 或 `avatar` | 可选；有则应用真实头像 |
| `gender`, `region`/`city`, `resume`/`desc`, `tags[]`, `work_years`/`exp` | 详情用 |

| P0 | **`GET /api/v1/core/workers/:id`**：与列表字段一致，避免详情只能靠列表兜底。 |

### 8.3 前端需要调整什么

| 优先级 | 调整项 |
|--------|--------|
| P1 | **`index.js`**：若 `w.avatar_url` 存在，**`avatar: imgUrl(w.avatar_url)`**，否则再用 `workerAvatarUrl(w.id)`。 |
| P2 | **`worker-detail.js`**：技工关联服务列表当前为 **`mockGoods`**；若后端提供 **`GET /api/v1/core/workers/:id/services`**，替换 mock。 |

---

## 九、模块 8：管家精选

### 9.1 前端现状

| 项目 | 说明 |
|------|------|
| 数据 | `GET core/goods/featured`，映射到 `marketList`（命名易与集市混淆） |
| 模板 | 价格写死 **`￥{{price}}/瓶`**，不一定符合真实单位 |
| 跳转 | `goods-detail?id=` |

### 9.2 后端需要做什么

| 优先级 | 任务 |
|--------|------|
| P0 | **`GET /api/v1/core/goods/featured`**：返回精选 **商品** 列表（来源：`shop_products` 或独立 `featured_goods` 挂载表）。 |
| P0 | 字段兼容前端映射： |

| 接口字段 | 前端使用 |
|----------|----------|
| `id` | `item.id` |
| `goodsTitle` / `title` / `name` | 名称 |
| `goodsRealPrice` / `price` | 价格 |
| `mainPicture` / `cover_image` / `image` | 主图 |

| P1 | 增加 **`unit`**（如「瓶」「份」）或 **`spec`**，避免前端写死「/瓶」。 |

### 9.3 前端需要调整什么（重要）

| 优先级 | 调整项 |
|--------|--------|
| P0 | **`pages/goods-detail/goods-detail.js`**：当前文件内容疑似 **错误拷贝**（注释为 service，且走 `api/wx/market/goods_info`）。需 **重写为与 `core/goods/featured` 同源**：按商品 ID 调 **`GET /api/v1/core/goods/:id`** 或 **`GET /api/v1/market/goods/:id`**（与后端统一 **一种** 商品主数据）。 |
| P1 | **`index.wxml`**：价格行改为 **`￥{{item.price}}{{item.unit || ''}}`** 或去掉固定「/瓶」。 |

---

## 十、「我的」与内层首页供给关系（后端闭环 + 前端入口）

### 10.1 技工入驻 → 直约技工列表

| 环节 | 后端 | 前端 |
|------|------|------|
| 提交 | `POST /api/v1/worker/apply`，入库 `worker_applications` | `join-worker.js` 已 `post`，需保证图片先 `upload` | 
| 审核 | 管理端 `PUT .../admin/worker-applications/:id` | 无 |
| 通过后 | 写入 **`workers`**，`core/workers` 可见 | 无 |

**后端额外规则**：拒绝/下架的技工 **不得**出现在 `GET /core/workers`。

### 10.2 服务商入驻 → 热卖榜 / 直约服务商

| 环节 | 后端 | 前端 |
|------|------|------|
| 提交 | `POST /api/v1/service-provider/apply` | `join-service.js` |
| 通过后 | 创建 **`services`** 记录，`sales_count` 参与 `services/hot` 排序 | 服务商后台或接口维护 SKU |
| 未通过 | 禁止对外展示对应 `services` | 无 |

### 10.3 集市商家入驻

| 说明 |
|------|
| **仅影响「本地集市」Tab**，与内层首页模块无直接数据关系；不在此展开。 |

### 10.4 优惠券

| 场景 | 接口 | 前端 |
|------|------|------|
| 我的角标 | `GET /api/v1/wx/user/coupon/:userId` | `user.js` `getMyCoupon` |
| 首页「领券」 | 待后端可领券列表 + 领取接口 | 快捷区改跳转（见模块 3） |

---

## 十 A、到家服务订单：后台「收到订单」与「派单给技工」（九大类写死场景下的必选能力）

> 与 **2.4** 配套：前端类目与价目可写死，**从 `order-confrim` / 下单页提交开始，后端必须完整承接**。

### 10 A.1 业务目标

1. **收到订单**：用户提交服务订单后，平台（后台/管理端/服务层）能 **持久化订单**、查询列表、区分待支付 / 待派单 / 服务中 / 已完成等状态。  
2. **派单给实际技工**：在「已收款（或已确认）待派单」状态下，平台能将订单 **绑定到某一 `Workers` 记录**（`assigned_worker_id`），并支持 **改派、拒单回流待派单池**（规则按产品选做）。

### 10 A.2 建议订单域（与集市订单区分）

| 方案 | 说明 |
|------|------|
| **A. 独立子表** | `service_orders`（推荐）：字段清晰，与 `market_orders` 并行，避免 `orders` 单表多态过重。 |
| **B. 统一 orders** | `orders.order_type = 'service'`，扩展 `service_id`、`assigned_worker_id` 等；需统一状态枚举文档。 |

以下以 **`service_orders`** 为例描述字段与接口。

### 10 A.3 表字段建议（`service_orders`）

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | PK | |
| `user_id` | FK | 下单用户 |
| `community_id` | int NULL FK | **所属小区**；用于 **小区热卖榜** 聚合、派单范围；与用户默认小区或上门地址解析一致 |
| `service_id` | FK → Services.id | 与写死页或库内 SKU 一致 |
| `group_key` | varchar(32) | 可选冗余：`urgent_fix`、`beauty_home` 等，便于统计与派单规则 |
| `amount` | decimal(10,2) | 成交价 |
| `address_id` | int NULL | 用户地址表主键；或 JSON 快照 |
| `address_snapshot` | json / text | 上门地址、联系人、电话（防地址被删后无法追溯） |
| `appointment_time` | datetime NULL | 预约上门时间 |
| `remark` | text | 用户备注 |
| `status` | varchar(32) | 见下表状态机 |
| `assigned_worker_id` | int NULL FK → Workers.id | **派单目标技工**；未派单为空 |
| `dispatch_at` | datetime NULL | 派单时间 |
| `dispatch_by` | int NULL | 操作人（0 表示系统自动规则） |
| `pay_status` | varchar(32) | 若支付独立：`unpaid` / `paid` / `refunded` |
| `createdAt` / `updatedAt` | datetime | |

**状态机（示例，可按产品裁剪）**

| status | 含义 |
|--------|------|
| `pending_pay` | 待支付 |
| `paid_pending_dispatch` | **已支付（或已确认），待派单——平台「收到订单」后应进入可派单队列** |
| `dispatched` | 已指派技工，待技工确认上门 |
| `in_service` | 服务进行中 |
| `completed` | 已完成 |
| `cancelled` | 已取消 |
| `refund_pending` / `refunded` | 退款相关（可选） |

### 10 A.4 派单逻辑（后端必选一种或组合）

| 方式 | 说明 |
|------|------|
| **人工派单** | 管理端展示 `paid_pending_dispatch` 列表；运营选择技工，调用 **指派接口** 写入 `assigned_worker_id`。 |
| **技工抢单** | 订单进入池；符合条件的技工 **抢单** 后写入 `assigned_worker_id`（需并发锁）。 |
| **自动规则** | 按 `Workers` 的 **服务标签 / 区域 / 当前单量** 评分选一人写入（`dispatch_by=0`）。 |

**候选技工筛选**：至少支持按 **`service_id` 对应类目 / `group_key` 与 `Workers.tags` 或扩展表 `worker_skills` 匹配**；可选叠加地理围栏。

### 10 A.5 接口约定（需在主 `API_DOC.md` 同步登记）

| 方法 | 路径（示例） | 鉴权 | 说明 |
|------|----------------|------|------|
| POST | `/api/v1/service-orders` 或 `/api/v1/orders`（`type=service`） | 用户 JWT | **创建订单**；body：`service_id, community_id?（或后端从地址解析）, address_id, appointment_time?, remark?` |
| POST | `/api/v1/service-orders/:id/pay` | 用户 | 调起支付或模拟支付（与现有 `orders/:id/pay` 对齐则合并） |
| GET | `/api/v1/service-orders/my` | 用户 | 我的服务订单 |
| GET | `/api/v1/admin/service-orders?status=paid_pending_dispatch` | 管理员 | **待派单队列** |
| POST | `/api/v1/admin/service-orders/:id/assign` | 管理员 | body：`{ "worker_id": n }` **派单** |
| POST | `/api/v1/worker/service-orders/:id/accept` | 技工 JWT | 接单（若采用确认环节） |
| POST | `/api/v1/worker/service-orders/:id/reject` | 技工 JWT | 拒单回流（可选） |

技工侧若暂无独立端，可先 **仅实现管理端指派**，小程序只展示「已派师傅」信息（`GET /service-orders/:id` 返回 `assigned_worker` 摘要）。

### 10 A.6 与现有文档的关系

- `doc/API_DOC.md` 中 **`POST /api/v1/orders/`** 若为泛化下单，需扩展 **服务单类型** 或与 **`service_orders`** 路由并存并在文档中写清。  
- **邻里帮帮** 使用 **`neighbor_assist_orders`**（见 **模块 4**），与 `service_orders`、集市订单 **分表**；三类订单均须进入 **「九州」管理后台统一收单与派单**（见 **第十章 B**）。

---

## 十 B、「九州」管理后台：统一收单与派单中枢

### 10 B.1 目标

用户在前端（小程序）提交 **到家服务单**、**邻里帮帮单** 等订单后，数据须 **进入后端管理平台「九州」**，由九州 **统一汇聚展示、筛选、派单/改派、状态跟踪**，避免多套后台或 Excel 旁路。

### 10 B.2 接入范围（首期建议）

| 订单类型 | 表 / 实体 | 在九州中的业务名 |
|----------|-----------|------------------|
| 到家服务 SKU | `service_orders` | 服务订单 |
| 邻里帮帮 | `neighbor_assist_orders` | 帮帮订单 |
| 本地集市（可选二期） | `market_orders` | 集市订单（派单逻辑多为商家自配，可做「异常工单」池） |

### 10 B.3 统一能力（九州须支持）

| 能力 | 说明 |
|------|------|
| **统一列表** | 按类型 Tab + 筛选：`status`、`community_id`、时间范围、`assigned_worker_id` 是否为空。 |
| **待派单池** | `paid_pending_dispatch`（服务）/ 帮帮单等价状态：已支付或已接单待指派 **执行人** 的队列。 |
| **派单** | 写入 `assigned_worker_id`（或帮帮单的执行技工/志愿者），记录 `dispatch_at`、`dispatch_by`（操作员账号）。 |
| **改派 / 撤回** | 允许回流待派单池；建议 **`dispatch_logs`** 审计。 |
| **消息/推送（可选）** | 派单成功后通知用户端、技工端（模板消息或订阅消息）。 |

### 10 B.4 接口与权限

- 所有 **`/api/v1/admin/...`** 派单类接口须 **管理员/调度员角色**（RBAC），与 C 端、技工 JWT 分离。  
- 可提供 **聚合接口**：`GET /api/v1/admin/dispatch-queue?types=service,neighbor_assist&community_id=` 便于首页工作台一眼看全站待办（实现细节由后端定，但 **语义上须能覆盖多类型**）。

### 10 B.5 与小区维度

- 列表与统计默认支持按 **`community_id`** 过滤，与 **小区热卖榜**、帮帮双地址中的小区归属一致，便于 **小区运营** 只处理本小区单。

---

## 十一、服务接口契约（约定，前缀统一 `/api/v1`）

以下与小程序 `util.get('core/...')` 路径一致（`config.baseUrl` 已含 `/api/v1` 时则控制器挂载在 `core` 下）。

### 11.1 通用响应

- 成功：`{ "errno": 0, "data": <payload> }`；`payload` 为对象或数组见各节。
- 失败：`{ "errno": <非0>, "errmsg": "string" }`，HTTP 仍建议 **200**（与现有 `util.js` 解析习惯一致）或 **4xx/5xx**（需在 `util.js` 统一兼容）。

### 11.2 `GET /core/banners`

| 项 | 约定 |
|----|------|
| Query | 无，或 `?scene=home`（预留多场景） |
| `data` | `array`，元素字段： |

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `id` | int | 否 | 运营主键 |
| `image_url` | string | 是 | 绝对 URL 或 `/uploads/...` |
| `sort_order` | int | 否 | 升序；缺省按数组顺序 |
| `link_type` | string | 否 | `none` \| `service` \| `h5` \| `page` |
| `link_value` | string | 否 | `service` 时为服务 ID；`page` 时为小程序路径 |

### 11.3 `GET /core/service-groups/:key`

| 项 | 约定 |
|----|------|
| Path | `:key` ∈ `tidy` \| `urgent_fix` \| `appliance_clean` \| `pioneer_clean` \| `mite_remove` \| `furniture_care` \| `baby_home` \| `house_repair` \| `beauty_home` |
| `data` | `{ "title": "整理收纳", "price_unit": "份", "categories": [...], "services": [...] }`（`title`/`price_unit` 可选，便于与 `tidy-service` 对齐） |

**`categories[]`**

| 字段 | 类型 | 说明 |
|------|------|------|
| `name` | string | Tab 名 |
| `icon_url` | string? | Tab 小图标 |
| `sort_order` | int? | 排序 |

**`services[]`**

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | int | 与 `GET /core/services/:id` 一致 |
| `title` | string | |
| `price` | number | 单位：元，小数最多 2 位 |
| `cover_image` | string? | |
| `sales_count` | int | 默认 0 |
| `category` | `{ "name": "衣橱收纳" }` | 用于前端筛选 |

### 11.4 `GET /core/services/hot`

| 项 | 约定 |
|----|------|
| Query | `limit` 默认 10，最大 20；可选 `category_id` |
| `data` | **数组** 或 `{ "list": [] }`（前端已兼容两种） |
| 元素字段 | 同 11.3 `services[]`，且必须含 `id,title,price,cover_image,sales_count` |
| 排序 | `ORDER BY sales_count DESC, id DESC`（写死并在接口文档公示） |

**与「小区热卖榜」区分**：首页 **「小区热卖榜」模块** 须使用 **`GET /core/community/hot`**（**11.10**），按 **`community_id` 下有效订单量** 排序；本接口用于 **全站热卖 / 直约服务商条** 等，二者勿混用。

### 11.5 `GET /core/services/:id`

| 项 | 约定 |
|----|------|
| `data` | 单对象：在 11.4 基础上可增加 `description`、`detail_images`（数组或 JSON 字符串）、`tags`（数组）、`sub_title` |

### 11.6 `GET /core/workers` / `GET /core/workers/:id`

| 项 | 约定 |
|----|------|
| 列表 Query | `page` 默认 1，`page_size` 默认 20，最大 50 |
| 列表 `data` | 数组或 `{ "list":[], "total": n }` |
| 元素字段 | `id`,`name`,`avatar_url?`,`orders` 或 `service_count`,`region`/`city`,`gender`,`resume`/`desc`,`tags`（数组）,`work_years`/`exp` |
| 过滤 | **仅** `status='approved'`（或等价）且 `listed=1`（若有两字段）；未登录不返回隐私字段（如手机号） |

### 11.7 `GET /core/goods/featured` / `GET /core/goods/:id`

| 项 | 约定 |
|----|------|
| featured `data` | 数组；每项至少：`id`,`title`/`goodsTitle`,`price`/`goodsRealPrice`,`mainPicture`/`cover_image`；可选 `unit` |
| 详情 | 与 featured **同一商品主表**；`:id` 必须可逆查 |

### 11.8 入驻（与首页供给闭环，鉴权 JWT）

| 方法 | 路径 | 请求体要点 | 成功后后端动作 |
|------|------|--------------|------------------|
| POST | `/worker/apply` | 见 `API_DOC.md` 8.1 | 写 `worker_applications`；审核通过后 **upsert `workers`** 并关联 `user_id` |
| POST | `/service-provider/apply` | 见 `API_DOC.md` 8.1 | 写 `service_provider_applications`；通过后开通 **`services` 写入权限**（绑定 `provider_id`） |

### 11.9 到家服务订单与派单（鉴权、路径与 body 细则见 **第十章 A**）

| 方法 | 路径（与十四章速查一致） | `data` / body 要点 |
|------|--------------------------|---------------------|
| POST | `/service-orders`（或项目统一命名） | `service_id`（必填）、`community_id?`、`address_id`、`appointment_time`、`remark`；返回 `order_id`、`pay_params?` |
| POST | `/service-orders/:id/pay` | 与支付网关对接；成功后订单 `status` 进入 **待派单** |
| GET | `/service-orders/my` | 分页；列表项含 `status`、`assigned_worker` 摘要 |
| GET | `/admin/service-orders` | Query：`status=paid_pending_dispatch` 等 |
| POST | `/admin/service-orders/:id/assign` | `{ "worker_id": number }`；校验技工可接该 `service_id` / 区域 |

### 11.10 小区热卖与邻里帮帮下单

**`GET /core/community/hot`**

| 项 | 约定 |
|----|------|
| Query | **`community_id`**（必填或从登录用户解析）、`limit`、`days`（默认 30）、`type=service|shop|all` |
| `data` | `{ "services": [ { "item_type":"service", "id", "title", "price", "cover_image", "order_count_in_community", "rank" } ], "shops": [ { "item_type":"shop", ... } ] }`；若仅实现服务榜，`shops` 可空数组 |

**邻里帮帮**

| 方法 | 路径 | body / 说明 |
|------|------|-------------|
| POST | `/neighbor-assist/orders`（或项目前缀统一加 `api/v1`） | `assist_type`, **`origin_address_snapshot`**, **`destination_address_snapshot`**, `community_id`, `appointment_time`, `remark`, `amount?` |
| GET | `/neighbor-assist/orders/my` | 我的帮帮单 |
| GET | `/admin/neighbor-assist/orders` | 九州：筛选、派单 |
| POST | `/admin/neighbor-assist/orders/:id/assign` | `{ "worker_id": n }` 或执行人 ID |

---

## 十二、数据库表构建（DDL 建议）

> 表名可与 Sequelize 模型一致；以下为 **逻辑必备字段**。若已有表结构，用 `ALTER` 补字段而非重复建表。  
> **与主站迁移对齐**：若仓库已有 `Categories`/`Services`/`Banners` 迁移文件，以迁移为准，本节作 **字段核对清单**；新建环境可据此生成迁移。

### 12.0 关系总览（一句话）

**`Communities`（小区）** 1:N **`service_orders`**（按 `community_id`）；1:N **`neighbor_assist_orders`**；**`market_orders`** 若带 `community_id` 可参与 **小区内店铺热卖** 聚合。  
**`Categories(group_type=key)` 1:N `Services`**；**`ServiceProviders` 1:N `Services`**（`provider_id`）；**`Users` 1:1 `Workers`**（展示用）；**`Services` 1:N `service_orders`**；**`Workers` 1:N `service_orders` / `neighbor_assist_orders`**（`assigned_worker_id`，派单后）；**`shop_products` 或挂载表** 支撑 **`core/goods/*`**；**`Banners`** 独立列表。

### 12.1 `Categories`（服务类目 / Tab）

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | PK | |
| `name` | varchar(100) | 类目名；可与同组其它分组重名，**与 `group_type` 组合唯一** |
| `group_type` | varchar(50) | **九大分组 slug**，与 `service-groups/:key` 一致 |
| `icon_url` | varchar(512) | 可空 |
| `sort_order` | int | 默认 0 |
| `createdAt` / `updatedAt` | datetime | |

**唯一索引建议**：`UNIQUE (name, group_type)`。

### 12.2 `Services`（可下单服务 SKU）

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | PK | |
| `category_id` | FK → Categories.id | 必填 |
| `provider_id` | int NULL FK | 关联服务商主体（见 12.5）；无则平台自营 |
| `title` | varchar(200) | |
| `description` | text | |
| `price` | decimal(10,2) | |
| `cover_image` | varchar(512) | |
| `detail_images` | json / text | 可空 |
| `tags` | json | 可空 |
| `sales_count` | int | 默认 0，**热卖榜排序依赖** |
| `is_published` | tinyint | 1 展示；0 隐藏 |
| `createdAt` / `updatedAt` | datetime | |

**索引**：`INDEX (category_id)`，`INDEX (sales_count, id)`。

### 12.3 `Banners`

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | PK | |
| `image_url` | varchar(512) | |
| `sort_order` | int | |
| `link_type` | varchar(32) | |
| `link_value` | varchar(512) | 可空 |
| `scene` | varchar(32) | 可空，默认 `home` |
| `createdAt` / `updatedAt` | datetime | |

### 12.4 `Workers`（已通过审核的技工档案）

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | PK | |
| `user_id` | FK → Users.id | 与账号绑定 |
| `name` | varchar(50) | |
| `avatar_url` | varchar(512) | 可空 |
| `service_count` | int | 展示「服务 n 单」 |
| `city` / `region` | varchar(100) | 可空 |
| `gender` | varchar(8) | 可空 |
| `resume` | text | |
| `tags` | json | 可空 |
| `work_years` | int | 可空 |
| `status` | enum | `approved` / `suspended` |
| `createdAt` / `updatedAt` | datetime | |

**索引**：`INDEX (status)`。

### 12.5 `ServiceProviders`（服务商主体，可选独立表）

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | PK | |
| `user_id` | FK | 负责人账号 |
| `shop_name` | varchar(200) | |
| `status` | enum | `approved` 后允许挂 `Services` |

**关系**：`Services.provider_id` → `ServiceProviders.id`（无外键亦可，但需在应用层校验）。

### 12.6 精选商品（二选一）

**方案 A**：`shop_products` 增加 `is_featured`、`featured_sort`。  
**方案 B**：挂载表 `featured_goods`：`id`, `product_id`, `sort_order`, `status`。

**约束**：`GET /core/goods/featured` 与 `GET /core/goods/:id` 必须指向 **同一 `product_id` 空间**。

### 12.7 申请类表

沿用 `doc/API_DOC.md` 中 **`worker_applications`**、**`service_provider_applications`** DDL；不在此重复粘贴，**审批流与正式表关系**见下一节。

### 12.8 可选：`home_nav_tiles`（九宫格若改接口下发）

| 字段 | 类型 | 说明 |
|------|------|------|
| `slug` | varchar(32) PK 或 UNIQUE | 同 `key` |
| `name`,`icon_url`,`path`,`sort_order`,`status` | | |

### 12.9 可选：`neighborhood_assist_items`（邻里帮帮）

| 字段 | 类型 | 说明 |
|------|------|------|
| `recomm_type` | varchar(32) UNIQUE | take/child/… |
| `name`,`icon_url`,`url`,`sort_order` | | |

### 12.10 `service_orders`（到家服务订单，与 **十 A** 一致）

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | PK | |
| `user_id` | FK | |
| `community_id` | int NULL FK | **小区内热卖统计、九州按小区筛单** |
| `service_id` | FK → Services.id | |
| `group_key` | varchar(32) NULL | 与九宫格 `key` 一致，便于统计 |
| `amount` | decimal(10,2) | |
| `address_id` | int NULL | |
| `address_snapshot` | json | 建议必填快照 |
| `appointment_time` | datetime NULL | |
| `remark` | text | |
| `status` | varchar(32) | 见 **10 A.3** 状态机 |
| `pay_status` | varchar(32) | 若与 `status` 拆分 |
| `assigned_worker_id` | int NULL FK → Workers.id | **派单结果** |
| `dispatch_at` | datetime NULL | |
| `dispatch_by` | int NULL | |
| `createdAt` / `updatedAt` | datetime | |

**索引**：`INDEX (status, createdAt)`，`INDEX (assigned_worker_id)`，`INDEX (user_id)`，**`INDEX (community_id, status, createdAt)`**（支撑小区热卖聚合）。

### 12.11 `neighbor_assist_orders`（邻里帮帮订单，与 **模块 4、十 B** 一致）

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | PK | |
| `assist_type` | varchar(32) | `take`/`child`/`escort`/`trash`/`pet` |
| `user_id` | FK | |
| `community_id` | FK | 小区 |
| `origin_address_snapshot` | json **必填** | 取货/接取/汇合等 **起点** |
| `destination_address_snapshot` | json **必填** | 收货/送达/目的地等 **终点** |
| `amount` | decimal(10,2) NULL | |
| `appointment_time` | datetime NULL | |
| `remark` | text | |
| `status` | varchar(32) | 含待支付、待派单、已派单、完成、取消等（与业务对齐） |
| `assigned_worker_id` | int NULL FK → Workers.id | 九州派单 |
| `dispatch_at` / `dispatch_by` | datetime / int NULL | 同 `service_orders` |
| `createdAt` / `updatedAt` | datetime | |

**索引**：`INDEX (community_id, status)`，`INDEX (assist_type, status)`。

---

## 十三、后端如何建立「对应关系」（实现规则）

### 13.1 九大分组 ↔ 类目 ↔ 服务

1. **类目**：`Categories.group_type = :key`（如 `tidy`），`name` 为 Tab 文案。  
2. **服务**：`Services.category_id` 指向该组下的类目行。  
3. **`GET /core/service-groups/:key`**：  
   - `SELECT * FROM Categories WHERE group_type=? ORDER BY sort_order` → `categories`；  
   - `SELECT s.*, c.name AS cat_name FROM Services s JOIN Categories c ON s.category_id=c.id WHERE c.group_type=? AND s.is_published=1 ORDER BY s.sales_count DESC` → 映射为 `services[].category.name`。  
4. **禁止**在 `Services` 上冗余存 `group_type` 字符串（易不一致）；若查询性能需要，可加生成列或视图。

### 13.2 全站热卖（直约服务商条）与「小区热卖榜」

- **直约服务商条 / 全站热卖**：`GET /core/services/hot`；前端截取前 4 / 前 6；仅 `is_published=1`。若需「首页横条」与全站热卖区隔，增加 `home_strip_priority` 或运营位表。  
- **小区热卖榜**：**不得**仅用 `Services.sales_count` 全站字段；须 **`GET /core/community/hot`**，在 **`community_id` + 时间窗** 内对 **`service_orders`**（有效状态）按 `service_id` **COUNT** 降序；店铺榜对 **`market_orders`** 按 `shop_id` **COUNT**（订单表须带 `community_id` 或可解析）。

### 13.3 技工入驻 → 列表

1. `POST /worker/apply` → `worker_applications`（`status=pending`）。  
2. 管理端审核通过 → **插入或更新** `Workers`：`user_id` 来自申请人，`status=approved`，同步 `name/resume/...`。  
3. `GET /core/workers`：**只查** `Workers.status='approved'`；与 `Users` 左连接取头像兜底策略由后端统一。

### 13.4 服务商入驻 → 服务 SKU

1. `POST /service-provider/apply` → `service_provider_applications`。  
2. 审核通过 → 创建 **`ServiceProviders`**（或等价）记录，并给运营账号 **`provider_id`**。  
3. 创建 **`Services`** 时写入 `provider_id`、`is_published=0`，运营上架后改为 `1`。  
4. **`GET /core/services/hot`** 仅包含 `is_published=1` 且（可选）`provider.status=approved`。

### 13.5 Banner 跳转

- Controller 根据 `link_type` 组装小程序端可识别结构；若仅返回原始字段，由前端 `switch(link_type)` 跳转（见模块 4 前端调整项）。

### 13.6 与种子脚本的关系

- `seed_service_groups.js` 写入的 `Categories.group_type` + `Services` 即实现 **13.1** 的物理数据；执行后 `service-groups/:key` 应与脚本逻辑一致。

### 13.7 服务订单 ↔ `Services` ↔ `Workers`（派单）

1. **下单**：`service_orders.service_id` 必须指向 **`Services` 已发布行**；前端写死展示时，须保证该 `id` 已在库中（种子或后台录入）。  
2. **收到订单**：支付成功（或业务确认）后 `status → paid_pending_dispatch`，进入待派单池。  
3. **派单**：`assigned_worker_id` 只能指向 **`Workers.status=approved`** 且具备 **技能/区域与服务匹配**（应用层校验）；写入后 `status → dispatched`（或等价）。  
4. **改派**：清空或更换 `assigned_worker_id` 时记录日志表（建议 `service_order_dispatch_logs`：`order_id, old_worker_id, new_worker_id, operator_id, created_at`）。

### 13.8 邻里帮帮订单 ↔ 双地址 ↔ 九州

1. **下单**：校验 `origin_address_snapshot`、`destination_address_snapshot` 非空；`assist_type` 合法。  
2. **九州**：`neighbor_assist_orders` 进入 **统一待派单池**（**十 B**），与 `service_orders` 并列展示或分 Tab。  
3. **派单**：同服务单，写入 `assigned_worker_id`；候选池可按 **小区 + assist_type + 技工标签** 过滤。

### 13.9 `community_id` 来源（约定）

- **用户表**绑定默认 `community_id`，或  
- **订单提交时**由用户所选地址关联小区主数据，或  
- 运营在后台为用户打标小区。  
三者选一并写死文档，保证 **热卖聚合** 与 **九州筛单** 同源。

---

## 十四、接口总表（速查）

| 方法 | 路径 | 用途 | 鉴权 |
|------|------|------|------|
| GET | `/api/v1/core/banners` | Banner | 无 |
| GET | `/api/v1/core/service-groups/:key` | 九类落地页 | 无 |
| GET | `/api/v1/core/services/hot` | **全站**热卖 + 直约服务商条 | 无 |
| GET | `/api/v1/core/community/hot` | **小区内**热卖（服务/店铺） | 无或登录 |
| GET | `/api/v1/core/services/:id` | 服务详情 | 无 |
| GET | `/api/v1/core/workers` | 技工横滑 | 无 |
| GET | `/api/v1/core/workers/:id` | 技工详情 | 无 |
| GET | `/api/v1/core/goods/featured` | 管家精选 | 无 |
| GET | `/api/v1/core/goods/:id` | 精选商品详情（建议） | 按商品域 |
| POST | `/api/v1/worker/apply` | 技工入驻 | 登录 |
| POST | `/api/v1/service-provider/apply` | 服务商入驻 | 登录 |
| GET | `/api/v1/user/profile` | 我的余额 | 登录 |
| GET | `/api/v1/wx/user/coupon/:id` | 优惠券数量 | 登录 |
| POST | `/api/v1/service-orders`（或约定路径） | 创建到家服务订单 | 登录 |
| GET | `/api/v1/service-orders/my` | 我的服务订单 | 登录 |
| GET | `/api/v1/admin/service-orders` | 待派单等列表 | 管理员 |
| POST | `/api/v1/admin/service-orders/:id/assign` | 指派技工 | 管理员 |
| POST | `/api/v1/neighbor-assist/orders` | 邻里帮帮下单（双地址） | 登录 |
| GET | `/api/v1/neighbor-assist/orders/my` | 我的帮帮单 | 登录 |
| GET | `/api/v1/admin/neighbor-assist/orders` | 九州：帮帮单列表 | 管理员 |
| POST | `/api/v1/admin/neighbor-assist/orders/:id/assign` | 九州：帮帮派单 | 管理员 |
| GET | `/api/v1/admin/dispatch-queue` | 九州：多类型待派单聚合（可选） | 管理员 |

---

## 十五、联调自检清单（建议）

**后端**

- [ ] `services/hot` 返回条数、字段与排序与文档一致  
- [ ] `service-groups` 九个 key 均可 200，且 `services[].id` 在 `services/:id` 可访问  
- [ ] `workers` 仅返回可展示技工；`workers/:id` 与列表字段一致  
- [ ] `goods/featured` 与 `goods/:id`（若提供）ID 空间一致  
- [ ] `banners` 图片可访问，`link_type` 枚举与前端约定一致  
- [ ] **到家服务订单**：创建 → 支付（或确认）→ `paid_pending_dispatch` → 管理端 **assign** → `assigned_worker_id` 合法且技工侧/用户侧可查询  
- [ ] **`community/hot`**：仅统计带 **`community_id`** 的有效订单；与首页小区一致  
- [ ] **邻里帮帮**：双地址入库；九州可列表 + 派单  
- [ ] **九州**：`service_orders` 与 `neighbor_assist_orders` 至少一种方式 **统一待办**（分 Tab 或聚合接口）  

**前端**

- [ ] `useCuratedHomeHotList` 生产为 `false`  
- [ ] `core/services/hot` 只请求一次并复用  
- [ ] `tidy-service` 使用 `util.get` 调 `service-groups`  
- [ ] 技工头像优先接口 `avatar_url`  
- [ ] `goods-detail` 与精选商品接口对齐，非错误 `market` 拷贝逻辑  
- [ ] 快捷入口与 `classify` 区分参数或独立路径  

---

## 十六、修订记录

| 日期 | 说明 |
|------|------|
| 2026-04-15 | 初版、收窄 Tab 范围 |
| 2026-04-15 | 详版：分模块补充后端任务、字段表、前端调整项与联调清单 |
| 2026-04-15 | 增补第十一～十三章：接口契约、DDL 建议、后端关系与查询规则；原十一十二顺延为十四、十五 |
| 2026-04-15 | 增补 2.4、第十章 A、`service_orders` 表 12.10、13.7：九大类可写死展示，下单后接单与派单必选 |
| 2026-04-15 | 第五、六章：小区热卖按 `community_id` 聚合服务/店铺；邻里帮帮双地址与 `neighbor_assist_orders`；第十章 B 九州统一收单派单；11.10、12.11、13.8～13.9、十四章增补接口 |
