/**
 * 小程序全局配置文件
 */
const config = {
    // 当前使用的 API 基础地址
    // 开发环境可以切换为 http://127.0.0.1:3000/api/v1/
    // 公网环境使用 Cpolar 提供的地址
    baseUrl: 'http://6b6b1bb4.r3.cpolar.cn/api/v1/',

    // 如果需要 https 可以取消注释下面这行（注意 Cpolar 免费版通常为 http）
    // baseUrl: 'https://6f567ef2.r3.cpolar.cn/api/v1/',
};

module.exports = config;
