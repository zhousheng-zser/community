# 本地集市图片展示异常 — 前端对后端排查说明（可转发）

> 口吻：前端联调  
> 背景：本地集市列表、店铺详情「点单 / 商家」Tab 中，多处出现**灰色占位**或**空白图**，需确认**接口字段是否有值**以及**最终可访问的静态 URL**，便于区分是「字段为空」还是「地址错误 / 权限 / 跨域」。

---

## 1. 前端实际读取的字段（按页面位置）

### 1.1 店铺列表 `GET /api/v1/market/shops`（每条 list 项）

| 截图位置 | 前端用途 | 读取字段（按优先级） |
|----------|----------|----------------------|
| 列表左侧**店铺封面/缩略图** | `normalizeMarketShop` → `coverUrl` | `cover_url` → `cover_image` → `list_cover_url` → `logo_url`（任一有值则经 `imgUrl()` 拼接域名） |

说明：**四个都为空或 null** 时，前端 **`coverUrl` 为空字符串**，`<image>` 不渲染有效地址，表现为**灰块**。

---

### 1.2 店铺详情 `GET /api/v1/market/shops/:shopId`

| 截图位置 | 前端用途 | 读取字段 |
|----------|----------|----------|
| 顶部**大横幅/封面** | `shop.cover` | `cover_url` 或 `cover` |
| 卡片左侧**店铺 Logo** | `shop.logo` | `logo_url` 或 `logo` |
| 「商家」Tab → **店铺照片**左图 | `shop.facadeImage` | `facade_image` |
| 「商家」Tab → **店铺照片**右图 | `shop.interiorImage` | `interior_image` |
| 「商家」Tab → **资质证照**大图 | `shop.licenseImage` | `license_image` |

以上字段在 `market-shop.js` 的 `loadShopFromApi` 中映射；若接口无值，会落到本地占位路径，但若占位仍异常，请一并核对。

---

### 1.3 店内商品 `GET /api/v1/market/shops/:shopId/goods`

| 截图位置 | 前端用途 | 读取字段（按优先级） |
|----------|----------|----------------------|
| 商品行**小图** | `buildGoodsGroups` 每条商品 | `main_image` → `image` |

---

## 2. 请后端协助的排查方式（建议）

1. **对当前测试店铺**（如「母婴生活馆官方店」）在**响应 JSON 里**确认上述字段是否存在、是否为**非空字符串**。
2. 请在**服务端日志或管理端**把下列 **最终静态地址**打印出来（或导出一张表），方便前端在浏览器直接打开核对是否为预期图片：  
   - 列表：`cover_url`（及实际用于列表的那一项）  
   - 详情：`cover_url` / `logo_url`（或 `cover` / `logo`）  
   - 详情：`facade_image`、`interior_image`、`license_image`  
   - 商品：`main_image`（或 `image`）
3. 若字段有值但仍是灰块，请核对：  
   - URL 是否 **https**、是否需 **登录态** 才能访问；  
   - 路径是否为**相对路径**（前端会拼 `imageBaseUrl`，需与线上资源一致）；  
   - 小程序**下载域名白名单**是否已配置该域名。

---

## 3. 前端拼接说明（便于后端对照）

相对路径会经 `utils/util.js` 的 `imgUrl()` 与 `config.imageBaseUrl` 拼接；**空值不会拼接**，列表封面会直接为空。

---

## 4. 接口文档索引

详见 `doc/API_DOC.md` 第 12 节本地集市相关、`doc/本地集市店铺化_FE-BE沟通纪要_07_BE.md` 中 `cover_url`、门面/内景/证照等字段约定。

---

## 5. 后端确认（2026-03-14）

| 接口 | 库表真实字段 | 接口补充别名（与库值一致，便于前端兜底） |
|------|----------------|----------------------------------------|
| `GET /market/shops` 列表项 | `cover_url`、`logo_url` | `cover`、`logo`；若 `cover_url` 非空则 `cover_image`、`list_cover_url` 同 `cover_url` |
| `GET /market/shops/:id` | `cover_url`、`logo_url`、`facade_image`、`interior_image`、`license_image` | `cover`、`logo` |
| `GET /market/shops/:id/goods` 每项 | `main_image` | `image` |

- **静态 URL**：接口一般为**相对路径** `/uploads/...`；服务端映射为 `backend/data/uploads/images/...`（见 `src/index.js` 的 `express.static`）。完整地址 = 小程序 **`imageBaseUrl`**（或浏览器）+ 相对路径。
- **联调脚本**（在 `backend` 目录）：`PUBLIC_API_BASE=https://你的域名:端口 node scripts/print_market_shop_image_urls.js "母婴生活馆官方店"` — 会打印每条字段的完整 URL，并检查本地 `data/uploads/images` 下文件是否**真的存在**；若库内有路径但文件缺失，浏览器打开会 **404**，表现为灰块/裂图。
- **静态目录分层**（2026-03）：`/uploads/market/{店铺分类}/{shop_no}/shop_media/`（Logo/封面/门面/内景/证照）、`/uploads/market/{店铺分类}/{shop_no}/goods/{category_key}/`（商品图）。迁移命令：`node scripts/restructure_market_images.js`（`--dry-run` 仅预览）。
- **详情调试日志**：`.env` 中 `LOG_MARKET_IMAGE_DEBUG=1` 后请求店铺详情，服务端日志会打印图片字段与拼接预览。

*维护：前端 · 用于联调截图中「箭头所指」各图对应字段排查*
