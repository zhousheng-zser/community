# 社区生活服务系统全栈深度设计手册

本手册旨在提供一份详尽的、可落地的全栈系统设计方案，涵盖从前端交互细节到后端 API 规范，再到管理后台操作逻辑的全方位指导。

---

## 1. 核心身份与入驻闭环 (User & Join)

### 1.1 技工/服务商入驻 (Worker Join)
*   **前端功能细节**：
    *   **入口**：个人中心 -> 技工入驻 [join-worker.js](file:///d:/CODE/project/community/pages/join-worker/join-worker.js)。
    *   **表单字段**：姓名、性别、实名手机号（自动获取）、身份证号、居住地址、接单社区（下拉选择，如：阳光社区）、意向行业（家政/水电等）、最高学历、工作年限、个人简介、工作经历、资质证明（身份证正反面、职业资格证、工作照上传）。
    *   **交互逻辑**：
        *   点击“提交”：前端校验必填项 -> 异步上传图片至服务器获取路径 -> 调用 `POST /worker/apply` -> 跳转至“申请中”状态页面。
*   **后端 API 需求**：
    *   `POST /worker/apply`：接收完整表单，存储于 `Apply` 表，状态设为 `pending`。
    *   `GET /user/profile`：需返回 `worker_status`（pending/approved/rejected）以供前端展示不同状态。
*   **管理后台需求**：
    *   **模块**：入驻审批 -> 技工审批。
    *   **功能详情**：
        *   **按钮：查看详情**：弹出层显示所有资质证明图片（大图预览）。
        *   **按钮：通过**：点击后 -> 1. 更新 `Apply` 表状态；2. 在 `Workers` 表创建记录并关联 `UserId`；3. 修改 `Users` 表 `role` 字段增加 `worker` 标签；4. 发送微信订阅消息。
        *   **按钮：驳回**：弹出输入框要求填写理由 -> 理由存入 `Apply` 表 `remark` 字段 -> 下发通知。
*   **模块联动效应**：
    *   **数据流向**：`审核通过` -> `该技工被归类至“阳光社区”`。
    *   **前端表现**：当用户在个人资料中绑定小区 **A**（`user.community_id` / `communityId`）后，首页 [index.js](file:///d:/CODE/project/community/pages/index/index.js) 调用 `GET core/workers?community_id=A`。**仅展示**已入驻且审核通过、且 `Workers.community_id`（或关联表）等于 **A** 的技工；卡片字段见 **§1.2**。

### 1.2 直约技工：首页列表 → 详情 → 购买 → 到家订单 → 消息同步

#### 1.2.1 产品逻辑（摘要）

1. **入驻**：技工在「技工入驻」中选择接单小区 **A**，审核通过后写入 `Workers`（或 `worker_communities`）与 **A** 的关联。
2. **用户选小区**：用户在「我的/资料」或地址流程中绑定 **同一小区 A**（`GET/PUT user/profile` 返回并保存 `community_id`）。首页拉取技工列表时**必传** `community_id=A`，后端只返回该小区可接单技工。
3. **首页「直约技工」**：展示头像、姓名、**性别（男/女）**、**简介摘要**、**主要方向**（如衣橱整理）、服务单量；点击进入技工详情。
4. **技工详情**：完整简介、标签、**客户评价列表**、**该技工绑定的服务项目**（如「衣橱整理」），数据来自后端；用户点「立即购买」进入服务页并携带 `worker_id`。
5. **下单**：确认订单页提交 `POST service-orders`， body 含 `worker_id`、`service_id`、地址、联系人、金额等；成功后订单出现在用户 **「我的 → 到家订单」**（`GET service-orders/my`）。
6. **推送技工侧**：订单落库后，**推送到对应技工**的技工端 **订单列表**（`GET /api/v1/worker/service-orders`）与 **消息会话/系统通知**（见下）。
7. **接单通知用户**：技工在技工端 **接单** 后，后端向用户侧写入 **消息**（推荐：`messages/conversations` 更新或新增一条会话摘要 + `user/notifications` 一条可读记录）；用户在 **「消息」Tab** 可看到更新，点击跳转 **订单详情**（`/pages/order-detail/order-detail?id=` 或与到家订单详情统一路由）。

#### 1.2.2 前端已实现对接（小程序）

| 环节 | 文件 | 行为 |
|------|------|------|
| 首页列表 | [index.js](file:///d:/CODE/project/community/pages/index/index.js) | `init` 与 `onShow` 中 `refreshWorkerListForCommunity`：`GET core/workers`，Query 带当前用户 `community_id`；[workerApiMap.mapWorkerForHomeCard](file:///d:/CODE/project/community/utils/workerApiMap.js) 映射 `genderLabel`、`intro`、`mainDirection` |
| 首页卡片 | [index.wxml](file:///d:/CODE/project/community/pages/index/index.wxml) | 展示头像、姓名、性别标签、主营、简介、服务单数 |
| 技工详情 | [worker-detail.js](file:///d:/CODE/project/community/pages/worker-detail/worker-detail.js) | `GET core/workers/:id`、`GET core/workers/:id/services`、`GET core/workers/:id/reviews`（失败则用本地 mock） |
| 服务购买 | [service.js](file:///d:/CODE/project/community/pages/service/service.js) | 接收 `worker_id`，跳转确认页时带上 `serviceId`、`workerId` |
| 提交订单 | [order-confrim.js](file:///d:/CODE/project/community/pages/order-confrim/order-confrim.js) | 优先 `POST service-orders`，失败回退 `POST api/order/save` |
| 我的订单 | [service-orders-my.js](file:///d:/CODE/project/community/pages/service-orders-my/service-orders-my.js) | `GET service-orders/my` |
| 消息列表 | [message.js](file:///d:/CODE/project/community/pages/message/message.js) | `GET messages/conversations`（技工接单后由后端更新/推送会话，前端无需改路径即可展示） |

#### 1.2.3 后端需实现的接口与字段（详细）

**A. 技工列表（按小区过滤）**

*   `GET /api/v1/core/workers`
*   **Query**：`community_id`（必填，用于首页/分类页）、`page`、`limit`。
*   **规则**：仅返回 `status=approved`（或等价）且绑定该 `community_id` 的技工。
*   **响应项字段（建议）**：`id`, `name`/`real_name`, `avatar_url`, `gender`（`male`/`female` 或 `M`/`F`，前端已做男/女映射）, `desc`/`resume`, `tags[]`, `main_direction` 或 `specialty`, `service_count`, `work_years`。

**B. 技工详情**

*   `GET /api/v1/core/workers/:id`  
    *   返回上述字段 + 可选 `introduction` 长文、`rating_avg`、`order_count`。

**C. 技工关联的服务项目（钓鱼/平台商品库）**

*   `GET /api/v1/core/workers/:id/services`  
    *   返回该技工可提供的 `core/services` 子集：`id`, `title`, `price`, `cover_image`, `description`（与 `GET core/services/:id` 字段对齐）。  
*   **数据关系（存储）**：关联表 `worker_service`（`worker_id`, `service_id`, `enabled`, `sort`），后台可配置每位技工上架的服务。

**D. 客户评价**

*   `GET /api/v1/core/workers/:id/reviews`  
    *   Query：`page`, `limit`。  
    *   项：`id`, `score`（1–5）, `content`, `created_at`, `user.nickname`（脱敏）。  
*   **来源**：用户完成到家订单后的评价写入 `service_order_reviews`，按 `worker_id` 聚合展示。

**E. 用户下单（到家服务订单）**

*   `POST /api/v1/service-orders`（用户端，需登录）  
*   **Body（JSON）**：
    *   `worker_id`（必填）：指派技工。  
    *   `service_id`（必填）：`core/services.id`。  
    *   `address`（完整服务地址）、`contact_name`, `contact_phone`, `goods_name`, `goods_price`, `qty`, `remark`（可选）。  
*   **业务**：生成订单 `service_orders`，状态 `pending_worker_accept`（或等价）；计算应付金额（可接支付中台）；记录 `user_id`（当前登录用户）。
*   **推送技工**：写入技工可见订单池；可选 `POST` 内部事件：通知推送、短信（策略）。
*   **推送消息**：创建或更新与用户/技工相关的 `conversation` 一条系统消息，或写入 `notifications` 表，供 **消息页** 展示。

**F. 用户订单列表**

*   `GET /api/v1/service-orders/my`（已实现前端调用）  
    *   当前用户维度的到家订单，字段含 `status_text`, `service_title`, `created_at`, `amount`。

**G. 技工端（已实现路由，需与用户单联动）**

*   `GET /api/v1/worker/service-orders`、`POST .../accept` 等（见 `backend/src/routes/workerPortal.js`）。  
*   **接单后**：将订单状态改为「已接单/待上门」；**必须**向用户侧触发 **§1.2.1 第 7 条**（会话更新 + 可选模板消息 `requestSubscribeMessage`）。

**H. 消息页**

*   `GET /api/v1/messages/conversations`：会话列表中可包含「订单助手」类会话，`last_message` 展示「技工已接单」等摘要；或  
*   `GET /api/v1/user/notifications?type=service_order`：系统 Tab 合并展示（前端可后续对接 `localPrefs` 之外的云端已读）。

#### 1.2.4 数据表建议（补充）

*   `workers`：已有基础上保证 `community_id` 或 `worker_communities(worker_id, community_id)`。  
*   `worker_service`：技工与服务项目多对多。  
*   `service_orders`：`id`, `user_id`, `worker_id`, `service_id`, `status`, `amount`, `address_json`, `created_at`, …  
*   `service_order_reviews`：`order_id`, `worker_id`, `user_id`, `score`, `content`。  
*   `notifications`（可选）：`user_id`, `type`, `ref_type`, `ref_id`, `title`, `body`, `read_at`。

### 1.3 分类到家服务（整理收纳、家修急事…上门美业）：下单 → 服务商接单 → 消息 → 服务订单 → 聊天/投诉

#### 1.3.1 业务说明

面向首页金刚区/分类进入的 **标准化服务项目**（如 [tidy-service](file:///d:/CODE/project/community/pages/tidy-service/tidy-service.js) 中 `tidy`、`urgent_fix`、`beauty_home` 等 `group_key`），用户选服务 → 填写地址 → 提交订单。订单由**平台指派或服务商抢单/接单**（具体策略由运营定）。**服务商在 Web 管理端或小程序服务商端的「订单管理」中接单**后，系统应将 **接单成功** 同步到客户侧 **「消息」Tab**（会话列表摘要 + 可选系统通知），与 §1.2 一致。客户可在 **「我的 → 服务订单」** 查看列表，点击进 **订单详情**；也可在消息栏点对应会话：若后端返回 `service_order_id`，可先进 **订单详情** 再点「与服务商沟通」进入聊天。

**聊天**：复用 `messages/order-conversation/ensure`（`channel: worker_customer`）与 [chat](file:///d:/CODE/project/community/pages/chat/chat.js)，支持 **文字、图片**（及已扩展的语音，与 §2 一致）。

**投诉**：订单详情页提供 **投诉/售后** 入口，调用 `POST service-orders/:id/complaint`。

#### 1.3.2 前端接口与页面（已实现）

| 步骤 | 路径 / 接口 | 说明 |
|------|-------------|------|
| 分类列表 | `pages/tidy-service/tidy-service` | `onLoad` 接收 `key`（如 `beauty_home`）；跳转服务详情时带 `group_key` |
| 服务详情 | [service.js](file:///d:/CODE/project/community/pages/service/service.js) | `GET core/services/:id`；`group_key` 透传到确认页 |
| 提交订单 | [order-confrim.js](file:///d:/CODE/project/community/pages/order-confrim/order-confrim.js) | `POST service-orders`，body 含 `service_id`、`group_key`（及可选 `worker_id` 直约场景） |
| 我的列表 | [service-orders-my](file:///d:/CODE/project/community/pages/service-orders-my/service-orders-my.js) | 菜单文案为 **「服务订单」**；`GET service-orders/my` |
| 订单详情 | [service-order-detail](file:///d:/CODE/project/community/pages/service-order-detail/service-order-detail.js) | `GET service-orders/:id` 或 `GET service-orders/detail?order_no=`；**沟通** → `messages/order-conversation/ensure`；**投诉** → `POST service-orders/:id/complaint` |
| 消息列表 | [message.js](file:///d:/CODE/project/community/pages/message/message.js) | `GET messages/conversations`；若单项含 `service_order_id`，点击先进 **服务订单详情**（否则仍进聊天） |

#### 1.3.3 后端需实现的接口（汇总）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/core/service-groups/:groupKey` | 分类下服务列表（与 tidy-service 已请求的 `wx.request` 一致，可逐步统一为 `util.get`） |
| GET | `/core/services/:id` | 服务详情（已有） |
| POST | `/service-orders` | 创建订单；body 含 `service_id`, `group_key`, `address`, `contact_*`, `goods_name`, `goods_price`, `qty`, `remark`；可选 `worker_id`（直约） |
| GET | `/service-orders/my` | 当前用户订单列表；项含 `id`, `order_no`, `status_text`, `worker_user_id`, `amount`, `service_title` |
| GET | `/service-orders/:id` | 客户查看订单详情 |
| GET | `/service-orders/detail` | Query：`order_no`（与单号查询互为补充） |
| POST | `/service-orders/:id/complaint` | body：`content`, 可选 `images[]` |
| POST | `/worker/service-orders/:id/accept` 等 | 服务商/技工端接单（见 `workerPortal`）；**接单后必须** 更新 `messages/conversations` 中 `last_message` 或写入 `notifications`，并建议在会话列表项中附带 `service_order_id` 供前端跳转详情 |
| POST | `/messages/order-conversation/ensure` | 已存在；`worker_customer` 渠道下绑定 `order_no` 与 `worker_user_id`、`customer_user_id` |

**会话列表扩展字段（建议）**：`GET messages/conversations` 每条增加 `service_order_id`（或 `ref_type` + `ref_id`），便于消息页点击进入 `service-order-detail` 而非仅进聊天。

#### 1.3.4 管理后台（服务商端）

*   **订单管理**：按状态筛选（待接单、进行中、已完成）；展示 `group_key` / 服务名称、客户、地址、金额；操作 **接单 / 拒单**（调用与 `worker` 端一致 API）。
*   **与消息一致**：接单后触发客户侧消息同步（见上表）。

#### 1.3.5 数据字段补充

*   `service_orders` 表建议增加 `group_key`（或关联 `service_group_id`），便于统计「整理收纳」「上门美业」等业务线报表。
*   `service_order_complaints`：`order_id`, `user_id`, `content`, `status`, `created_at`。

### 1.4 服务商工作台与首页「直约服务商」：主体店铺 → 多服务打包 → 接单履约 → 消息与打卡

#### 1.4.1 业务说明（与 §1.2 技工链路对齐）

1. **身份**：已通过 **服务商/商家入驻**（与集市商家可共用同一主体，或独立 `service_provider` 主体）的用户，在 **「我的 → 工作台 → 服务商工作台」** 进入分包 [merchant-home](file:///d:/CODE/project/community/package-merchant/pages/merchant-home/merchant-home.js)（与店铺经营同一入口，文案侧重「直约到家 + 店铺」）。
2. **用户端入口**：首页 **「直约服务商」** 横向列表展示 **服务商主体**（非单个 SKU）。数据源优先 `GET core/service-providers`（按 `community_id` 过滤）；无接口时回退为 `core/services/hot` 映射的旧逻辑。
3. **选购**：用户进入 **服务商店铺页** [service-provider-shop](file:///d:/CODE/project/community/pages/service-provider-shop/service-provider-shop.js)，拉取 `GET core/service-providers/:id` 与 `GET core/service-providers/:id/catalog`，在 **整理收纳 / 家修急事 / … / 上门美业** 等分组下勾选 **一个或多个子服务 SKU**（如「衣橱整理收纳」「新房开荒保洁」），合并为 **打包单**。
4. **下单**：确认订单页 `mode=sp_bundle`，读取本地 `sp_bundle_checkout`（`provider_id`、`items[]`：`service_id`、`group_key`、`qty`、`title`、`price`），提交 **`POST service-orders/bundle`**；成功后订单归属该 **服务商账号**（后端 `merchant_user_id` / `provider_user_id` 与 `user_id` 买家）。
5. **服务商侧**：**「经营中心 → 直约服务订单」** 进入 `merchant-orders?scene=direct_service`，列表 **`GET merchant/service-orders`**（与集市 `market/merchant/orders` 区分）；详情复用 [worker-order-detail](file:///d:/CODE/project/community/package-worker/pages/worker-order-detail/worker-order-detail.js) 并传 **`portal=merchant`**，请求前缀为 **`merchant/service-orders`**：**接单 / 拒单 / 上门打卡 / 到场与完工多图证据 / 服务完成**（与技工订单一致的状态机）。
6. **通知用户**：服务商 **接单** 及关键状态变更后，写入 **`messages/conversations`** 摘要（或 `user/notifications`），用户 **「消息」** 与 **「我的 → 服务订单」** 均可感知（与 §1.2.1 第 7 条、§1.3 一致）。
7. **沟通**：用户侧 **服务订单详情** [service-order-detail](file:///d:/CODE/project/community/pages/service-order-detail/service-order-detail.js) 在存在 **`merchant_user_id`**（或后端约定的服务商用户 ID）时，`messages/order-conversation/ensure` 使用 **`channel: merchant_customer`**；否则仍走 **`worker_customer`**（技工单）。
8. **闭环**：服务商在详情点击 **订单完成** 后，订单进入 **待用户确认**；用户侧展示 **确认完成**，调用 **`POST service-orders/:id/confirm-complete`**（或 **`POST .../confirm`**，与后端统一即可）。

#### 1.4.2 订单状态机（建议枚举）

| 阶段 | status（示例） | 服务商端 | 用户端 |
|------|----------------|----------|--------|
| 已下单 | `pending_accept` | 可接单/拒单 | 等待接单 |
| 已接单 | `pending_visit` / `assigned` | 上门打卡、到场拍照 | 消息通知 |
| 服务中 | `in_service` | 完工拍照、点「完成」 | 沟通、查看进度 |
| 待用户确认 | `pending_user_confirm` | 等待确认 | **确认订单完成** |
| 已完成 | `completed` | 归档 | 评价（可选） |

证据字段建议：`evidence_before[]`、`evidence_after[]`（或 `before_photos` / `after_photos`），与技工单 §1.2 扩展一致。

#### 1.4.3 前端对接一览（小程序）

| 环节 | 文件 / 参数 | 行为 |
|------|----------------|------|
| 首页列表 | [index.js](file:///d:/CODE/project/community/pages/index/index.js) | 优先 `GET core/service-providers` → 跳转 `service-provider-shop?provider_id=` |
| 服务商店铺 | [service-provider-shop](file:///d:/CODE/project/community/pages/service-provider-shop/service-provider-shop.js) | 目录 `GET .../catalog`；写入 `sp_bundle_checkout` → `order-confrim?mode=sp_bundle&provider_id=` |
| 打包下单 | [order-confrim.js](file:///d:/CODE/project/community/pages/order-confrim/order-confrim.js) | `POST service-orders/bundle` |
| 我的订单 | [service-orders-my.js](file:///d:/CODE/project/community/pages/service-orders-my/service-orders-my.js) | `GET service-orders/my`（含打包单） |
| 订单详情（用户） | [service-order-detail.js](file:///d:/CODE/project/community/pages/service-order-detail/service-order-detail.js) | 沟通（`merchant_customer` / `worker_customer`）；**确认完成** `POST .../confirm-complete` |
| 服务商首页 | [merchant-home](file:///d:/CODE/project/community/package-merchant/pages/merchant-home/merchant-home.js) | 入口「直约服务订单」→ `merchant-orders?scene=direct_service` |
| 服务商订单列表 | [merchant-orders.js](file:///d:/CODE/project/community/package-merchant/pages/merchant-orders/merchant-orders.js) | `scene=direct_service` 时 `GET merchant/service-orders`；详情 → `worker-order-detail?portal=merchant` |
| 服务商订单详情 | [worker-order-detail.js](file:///d:/CODE/project/community/package-worker/pages/worker-order-detail/worker-order-detail.js) | `portal=merchant` 时 API 前缀 `merchant/service-orders`；聊天 `channel=merchant_customer` |
| 我的入口文案 | [user.wxml](file:///d:/CODE/project/community/pages/user/user.wxml) | 「服务商工作台」与技工并列 |

#### 1.4.4 后端需实现的接口（汇总，前缀 `/api/v1`）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/core/service-providers` | Query：`community_id`（可选）、`page`、`limit`。项：`id`, `name`, `avatar_url`, `cover_image`, `subtitle`, `service_count` |
| GET | `/core/service-providers/:id` | 服务商主体详情 |
| GET | `/core/service-providers/:id/catalog` | 分组 + SKU：`group_key`, `group_label`, `items[]`（`service_id`, `title`, `price`, `cover_image`, `description`） |
| POST | `/service-orders/bundle` | Body：`provider_id`, `items[]`（`service_id`, `group_key`, `qty`, 可选 `title` 快照）, `address`, `contact_name`, `contact_phone`, `remark`；生成一单多行或 JSON `lines` |
| GET | `/service-orders/my` | 已存在；返回项需区分 **技工指派单** 与 **服务商打包单**（`provider_id` / `bundle_id`） |
| GET | `/service-orders/:id` | 已存在；增加 `merchant_user_id` 或 `provider_user_id`、`status`、`evidence_*` |
| POST | `/service-orders/:id/confirm-complete` | 用户确认服务完成（仅 `pending_user_confirm`） |
| GET | `/merchant/service-orders` | 当前登录 **服务商** 可见的直约到家订单列表（与 `market/merchant/orders` 区分） |
| GET | `/merchant/service-orders/:id` | 详情 |
| POST | `/merchant/service-orders/:id/accept` | 接单 |
| POST | `/merchant/service-orders/:id/reject` | 拒单 |
| POST | `/merchant/service-orders/:id/check-in` | 上门定位打卡（body 同技工） |
| POST | `/merchant/service-orders/:id/evidence` | body：`kind: before|after`, `urls[]` 多图 |
| POST | `/merchant/service-orders/:id/complete` | 服务商提交完成 → 置 `pending_user_confirm` 并通知用户 |
| POST | `/messages/order-conversation/ensure` | 扩展：`channel: merchant_customer`，`merchant_user_id` + `customer_user_id` + `order_no` |
| （可选） | `/service-orders/:id/complaint` | 与 §1.3 共用 |

**权限**：`/merchant/service-orders*` 仅绑定该服务商主体的账号可访问；**接单后**必须写会话/通知（§1.2.3 G、§1.3.3）。

#### 1.4.5 数据表建议

*   `service_providers`：`id`, `user_id`（负责人）, `shop_id`（可选关联集市店）, `community_id`, `status`, `name`, `logo_url`。
*   `service_provider_catalog` 或复用 **`core/services` + `service_provider_service`（`provider_id`, `service_id`, `enabled`, `sort`）** 表达「该服务商可卖的 SKU」。
*   `service_order_lines`：`order_id`, `service_id`, `group_key`, `qty`, `unit_price`（打包拆行对账）。
*   `service_orders` 增加：`provider_id`, `bundle_key`（可选）, `merchant_user_id`，状态含 `pending_user_confirm`。

### 1.5 首页「小区热卖榜」与「管家精选」

#### 1.5.1 小区热卖榜 — 产品定义

*   **统计范围**：**当前用户绑定小区**（`community_id`，与资料/地址一致）。切换小区或绑定变化后，首页应 **重新请求** 热卖接口。
*   **排序含义**：在该小区内，**最近一段时间**（如 30 天，由后端配置）内 **被用户购买次数最多** 的 **服务 SKU**（`core/services.id`）。购买来源包括：
    *   **直约技工** 订单：`service_orders` 中 `worker_id` 非空，且订单归属本小区（`community_id` 或上门地址解析为同小区，**以后端统一规则为准**）；
    *   **直约服务商** 订单（含打包单）：`service_orders` 中 `provider_id` / `merchant_user_id` 非空，同上须计入本小区。
*   **聚合对象**：按 **`service_id`** 汇总有效订单行（可对 `qty` 求和）；**不得**仅用全站字段 `Services.sales_count` 代替小区内真实成交。
*   **与「全站热卖」区分**：`GET core/services/hot` 可用于「直约服务商条」等 **全站/运营位**；**「小区热卖榜」模块** 必须使用 **`GET core/community/hot`**，二者勿混用。

#### 1.5.2 小区热卖榜 — 前端实现（小程序）

| 项目 | 说明 |
|------|------|
| 配置 | [config.js](file:///d:/CODE/project/community/utils/config.js)：`useCuratedHomeHotList` 为 `false` 时走接口；为 `true` 时仅用本地写死列表（开发预览）。 |
| 拉取 | [index.js](file:///d:/CODE/project/community/pages/index/index.js) `init`：优先 `GET core/community/hot`，Query：`limit`（如 10）、**`community_id`**（取自 `app.globalData.user.communityId`）；解析 `commRes.services` / `service_list`，否则 `unwrapList(commRes)`。失败则回退 `GET core/services/hot`（同传 `community_id` 可选）。 |
| 映射 | 本地函数 `mapHotRows`：每项 → `id`, `itemType`（`service` / `shop`）, `name`, `price`, `image`, `rank`（或接口返回 `rank`）。 |
| 展示 | [index.wxml](file:///d:/CODE/project/community/pages/index/index.wxml)「小区热卖榜」横向列表：`itemType === 'shop'` → `market-shop`；否则 → `service/service?id=`。 |
| 筛选条 | `hotFilters` 为静态文案；若产品要求按分类筛热卖，应对 `core/community/hot` 增加 Query（如 `group_key`）并 **重新请求**。 |

#### 1.5.3 小区热卖榜 — 后端实现要点

*   **接口**：`GET /api/v1/core/community/hot`
*   **Query（建议）**：
    *   `community_id`（**必填**或从登录用户默认小区解析，与文档一致即可）；
    *   `limit`（默认 10，最大 20）；
    *   `window_days`（可选，默认 30）：统计时间窗；
    *   `group_key`（可选）：仅统计某一大类下的服务。
*   **统计逻辑（示例）**：
    1.  从 `service_orders` 筛选：`community_id = :cid`（若订单仅地址无 `community_id`，需与地址库/小区围栏一致规则补全），状态属于 **已计入销量** 的集合（如已支付、已完成，**不含已取消/全额退款**，具体由财务规则定）。
    2.  按 `service_id` 分组，`SUM(qty)` 或 `COUNT(*)` 降序，取 Top N。
    3.  Join `services`（或等价表）取 `title`, `price`, `cover_image`。
*   **响应项（每项）**：至少 `id`（即 `service_id`）, `title`/`name`, `price`, `cover_image`, `rank`（1-based 或 `NO.1` 字符串）, `item_type`（默认 `service`）；可选 `order_count_community`（本小区成交笔数或件数，便于展示「月销 xxx」）。
*   **扩展（可选）**：若同一榜单需混入 **小区内集市店铺** 热卖，可对 `market_orders` 按 `shop_id` + `community_id` 聚合，返回 `item_type: shop`，与前端 `mapHotRows` 已支持的 `shop` 分支一致。
*   **性能**：对 `service_orders(community_id, status, created_at)`、`service_id` 建索引；热卖结果可 **短时缓存**（如 5～15 分钟）按小区键。

#### 1.5.4 管家精选 — 产品定义

*   **含义**：**小区管家、物业或平台运营** 在管理后台为 **指定小区** 勾选、排序的 **集市商品**（非算法排序），用于首页「管家精选」模块展示信任背书与主推 SKU。
*   **与热卖榜关系**：热卖榜为 **成交数据驱动**；管家精选为 **人工配置**，二者数据源独立。

#### 1.5.5 管家精选 — 前端实现（小程序）

| 项目 | 说明 |
|------|------|
| 拉取 | [index.js](file:///d:/CODE/project/community/pages/index/index.js)：`GET core/goods/featured`，Query 建议带 **`community_id`**（与当前用户小区一致）；`unwrapList` 后取前 6 条映射为 `marketList`。 |
| 映射字段 | `id`, `name` ← `goodsTitle`/`title`/`name`, `price` ← `goodsRealPrice`/`price`, `image` ← `mainPicture`/`cover_image`/`image`。 |
| 展示 | [index.wxml](file:///d:/CODE/project/community/pages/index/index.wxml)「管家精选」→ `goods-detail/goods-detail?id=`。 |
| 详情同源 | 详情页须与精选 **同一商品 ID 空间**，调用 `GET core/goods/:id` 或项目已统一的集市商品详情接口（与 [首页与我的_前端配合及测试说明](file:///d:/CODE/project/community/doc/首页与我的_前端配合及测试说明.md) 一致）。 |

#### 1.5.6 管家精选 — 后端实现要点

*   **接口**：`GET /api/v1/core/goods/featured`
*   **Query（建议）**：
    *   `community_id`（**可选**但推荐）：传入时 **仅返回该小区配置的精选**；不传时可返回全站默认精选或空（由产品定）。
    *   `limit`（可选，默认 6～20）。
*   **数据层（任选其一）**：
    *   **方案 A**：表 `community_featured_goods`：`id`, `community_id`, `product_id`（或 `shop_product_id`）, `sort_order`, `status`, `created_by`（管家/管理员用户 ID）, `created_at`。
    *   **方案 B**：在 `shop_products` 上增加 `is_featured`, `featured_community_id`, `featured_sort`（适合「每商品只挂一个小区精选」的简单场景）。
*   **响应**：与现有 `unwrapList` 兼容的数组或 `{ list: [] }`；每项含商品卡片所需字段，且 **`id` 与 `GET core/goods/:id` 一致**。
*   **管理后台**：小区运营 → **管家精选**：按小区筛选，从可售商品中勾选、拖拽排序、上下线；保存即影响对应 `community_id` 的 `featured` 结果。

### 1.6 惠民卡（流量联盟 · 京东 / 拼多多 / 大牌餐饮栏目）

#### 1.6.1 产品定位

*   **入口**：首页顶栏 Tab **「惠民卡」**（与「本地商城」「首页」「本地集市」并列），见 [index.wxml](file:///d:/CODE/project/community/pages/index/index.wxml) `activeTab === '惠民卡'`。
*   **价值**：聚合 **京东联盟（京挑客）**、**拼多多多多进宝** 等 CPS/联盟商品，用户点击后跳转 **微信京东购物小程序** / **拼多多小程序** 或复制推广链；订单佣金按联盟规则结算（平台与推客分润，具体以商务与联盟后台为准）。
*   **扩展**：横向子 Tab 除「京东联盟」「拼多多」外，可配置 **肯德基 / 麦当劳 / 星巴克** 等 **大牌餐饮栏目**（当前前端为关键词复制 + 跳转惠民专区入口，后续可挂载 **按栏目筛选的联盟 SKU**，见 **§1.6.5**）。

#### 1.6.2 前端逻辑设计（小程序）

| 环节 | 行为 |
|------|------|
| Tab 切换 | `topTabs` 含「惠民卡」；`switchTopTab` 将 `activeTab` 设为 `惠民卡`，仅渲染惠民卡页 `benefit-card-page`。 |
| 子栏目（联盟 / 大牌） | `benefitAllianceTabs`：`jd`、`pdd`、`kfc`、`mcd`、`starbucks`；`switchBenefitAllianceTab` 更新 `activeBenefitAlliance`。横向 `scroll-view` 展示胶囊 Tab。 |
| 京东区块 | `activeBenefitAlliance === 'jd'`：顶栏头图 `jdBanner` + 文案 `jdHeroTitle` / `jdHeroSubtitle`；列表 `jdGoods`（网格）；底部 `jdEntry` 用于「去京东领券购买」大按钮。 |
| 拼多多区块 | `activeBenefitAlliance === 'pdd'`：同理 `pddBanner`、`pddHeroTitle`、`pddGoods`、`pddEntry`。 |
| 大牌餐饮区块 | `kfc` / `mcd` / `starbucks`：`benefitBrandColumns` 配置标题、副文案、搜索关键词、说明；`copyBrandKeyword` 复制关键词；「进入京东惠民专区」「进入拼多多精选」复用 `jdEntry` / `pddEntry`。 |
| 跳转京东 | `goToJDMiniprogram`：`wx.navigateToMiniProgram` 至京东联盟小程序（`config.benefitAlliance.jdUnionAppId`），path 为联盟 proxy：`/pages/union/proxy/proxy?spreadUrl=` + `encodeURIComponent(推广链)`；优先用商品自带 `spreadUrl`，否则 `GET jd/promotion/spread-url?sku_id=&scene=benefit_card`。 |
| 跳转拼多多 | `goToPddBenefit`：有 `miniPath` 则 `navigateToMiniProgram` 拼多多 AppId；否则可拉 `GET pdd/promotion/spread-url` 或复制 H5 `spreadUrl`。 |
| 本地优先 | `config.benefitAlliancePreferLocal === true` 时，优先 [benefitAllianceLocal.js](file:///d:/CODE/project/community/utils/benefitAllianceLocal.js) `getLocalBenefitCardPayload()`，**不覆盖**已有本地京东/拼多多列表时再请求接口（见 `init` 中 `useLocalJd` / `useLocalPdd` 逻辑）。 |
| 远程拉取顺序 | 1）本地清单；2）`GET benefit/display?scene=benefit_card` 补头图与标题（仅当本地未提供对应块）；3）`GET jd/benefit/goods`、`GET pdd/benefit/goods` 补列表。 |

#### 1.6.3 前端调用的后端接口（与现网一致，前缀 `/api/v1`）

| 方法 | 路径 | 用途 |
|------|------|------|
| GET | `/benefit/display` | Query：`scene=benefit_card`。返回京东/拼多多 **顶栏运营配置**：头图 URL、主副标题（可选），与商品列表分离。 |
| GET | `/jd/benefit/goods` | Query：`scene=benefit_card`。返回京东联盟 **商品列表**（用于 `jdGoods`）：`skuId`/`sku_id`、`title`、`image`、`price`、`rebateAmount`、`spreadUrl` 等。 |
| GET | `/pdd/benefit/goods` | Query：`scene=benefit_card`。返回拼多多进宝 **商品列表**：`goodsId`、`title`、`image`、`price`、`couponPrice`、`spreadUrl`、`miniPath` 等。 |
| GET | `/jd/promotion/spread-url` | Query：`sku_id`、`scene=benefit_card`。点击商品时若列表未带完整推广链，动态换取 `spreadUrl`（短链）。 |
| GET | `/pdd/promotion/spread-url` | Query：`goods_id`、`scene=benefit_card`。换取拼多多推广链与可选 `miniPath`。 |

**说明**：小程序 [util.get](file:///d:/CODE/project/community/utils/util.js) 已带 `baseUrl`，实际请求为 `https://{host}/api/v1/...`。

#### 1.6.4 后端应提供的能力（数据与接口契约）

*   **商品列表**：京东、拼多多 **分表或分接口** 维护（如 `jd_benefit_goods`、`pdd_benefit_goods`），字段需与前端映射一致；`scene` 支持多场景扩展（如仅 `benefit_card` 展示）。
*   **展示配置**：`benefit_alliance_config`（或等价表）按 `scene` 存储京东/拼多多 **头图、标题、副标题**，供 `benefit/display` 返回，避免把运营文案写死在代码里。
*   **推广链**：`spread-url` 类接口可对接联盟开放能力或预置短链；需处理 **失效、风控、限流**，失败时前端可降级为复制链接。
*   **可选扩展接口**（大牌栏目商品化）：`GET /benefit/brand-goods?brand=kfc|mcd|starbucks&scene=benefit_card` 返回与 `jdGoods`/`pddGoods` 同构的列表，前端在大牌 Tab 内嵌套网格即可；未上线前沿用 **关键词复制 + 惠民专区入口**。

#### 1.6.5 管理后台：数据管理与上架服务

| 模块 | 功能 |
|------|------|
| **京东联盟商品** | 列表：SKU、标题、主图、展示价、佣金提示、推广短链、排序、`enabled`、所属 `scene`。操作：新增/编辑/下架/批量导入（与仓库「流量联盟」目录内京东联盟物料、短链说明对齐）。 |
| **拼多多进宝商品** | 同上，字段对齐 `link_key`/`goods_id`、券后价等；支持从进宝后台同步或手工录入。 |
| **惠民卡顶栏运营位** | 配置 `benefit_card` 场景下京东、拼多多 **头图 URL、主标题、副标题**；支持预览小程序效果。 |
| **大牌餐饮栏目（可选）** | 配置 `kfc`/`mcd`/`starbucks` 的 **展示文案、默认搜索词、挂载商品 ID 列表**（京东/拼多多二选一或双列）；未配置商品时与小程序「仅关键词」模式一致。 |
| **合规与审计** | 联盟物料需符合平台规范；后台记录 **操作人、上下架时间**；敏感类目按平台规则限制展示。 |

#### 1.6.6 数据表建议（与仓库后端脚本对齐）

*   **`jd_benefit_goods`**：`sku_id`（与 u.jd.com 短链 path 对应）、`title`、`image_url`、`price`、`rebate_amount`、`spread_url`、`scene`、`sort_order`、`status`。  
*   **`pdd_benefit_goods`**：`link_key` 或 `goods_id`、`title`、`image_url`、`price`、`coupon_price`、`spread_url`、`mini_path`、`scene`、`sort_order`、`status`。  
*   **`benefit_alliance_config`**：`scene`、`platform`（`jd`|`pdd`）、`hero_image_url`、`hero_title`、`hero_subtitle`、`updated_at`。  

**种子与脚本**：参考 `backend/scripts/seed-jd-benefit.js`、`seed-benefit-alliance-config.js`、`backend/sql/` 下表结构；路由挂载见 [mountBenefitAlliance.js](file:///d:/CODE/project/community/backend/src/mountBenefitAlliance.js)。

---

## 2. 社区互动与邻里帮帮 (Community & Assist)

### 2.1 邻里互动 (Community)
*   **前端功能细节**：
    *   **列表展示**：按“热门话题”、“热门活动”、“邻里互动”分类分页加载 [community.js](file:///d:/CODE/project/community/pages/community/community.js)。
    *   **发布动态**：[community-publish.js](file:///d:/CODE/project/community/pages/community-publish/community-publish.js) 支持文字 + 最多 9 张图片 + 当前定位。
    *   **交互反馈**：
        *   **点赞**：点击红心按钮 -> 立即改变 UI 颜色（乐观更新） -> `POST posts/{id}/like` -> 后端记录并增加计数。
        *   **评论**：点击评论按钮 -> 弹出底部输入框（避开键盘覆盖） -> `POST posts/{id}/comments` -> 成功后列表追加显示。
*   **管理后台需求**：
    *   **模块**：社区运营 -> 动态管理。
    *   **功能详情**：
        *   **敏感词过滤**：后端集成敏感词库，自动标记存疑帖子。
        *   **按钮：屏蔽/下架**：点击后动态在前端消失，但保留在后台库中供追溯。
        *   **按钮：置顶**：设置动态权重值，影响 `GET /posts` 接口的排序逻辑。

### 2.2 邻里帮帮：业务闭环与数据同步

**目标**：居民发布帮帮需求后，数据进入**后端**并可在**运营后台**查看；同一批公开数据推送到 **首页「邻里帮帮」横向滚动条** 与 **社区 Tab「邻里互动」** 下的帮帮卡片区；参与双方在 **「我的 → 帮帮订单」** 中分别查看「我发布的 / 我接的单」，并进入订单详情完成履约与评价。**理论上，有人接单及后续关键状态变更时，对应提示应同步进入小程序「消息」Tab 的会话列表/系统通知**（与 §1.2 到家订单的接单通知一致，见下「消息栏同步」）。

**主流程（状态机建议）**

| 状态 | 说明 | 发布方 | 接单方 |
|------|------|--------|--------|
| `pending_accept` | 待接单 | 可查看、可聊天（若已建会话）、可取消（策略） | 可接单、可查看地址 |
| `in_service` | 服务中（已接单） | 联系对方、聊天、电话 | **打卡**、**路线规划**、提交「服务完成」 |
| `pending_confirm` | 待发布方确认 | **确认订单完成**、不满意可**投诉** | 等待确认 |
| `completed` | 已完成 | **评价** | 查看评价 |
| `cancelled` / `disputed` | 已取消 / 纠纷中 | 按规则展示 | 按规则展示 |

**前端已实现页面（小程序）**

| 模块 | 路径 | 说明 |
|------|------|------|
| 发布 | [order-publish](file:///d:/CODE/project/community/pages/order-publish/order-publish.js) | 优先 `POST neighbor-assist/orders`，失败回退 `POST orders/publish` |
| 首页滚动 | [index](file:///d:/CODE/project/community/pages/index/index.js) | `GET neighbor-assist/orders/feed` 填充横向 chip；「查看更多」→ 社区「邻里互动」 |
| 社区列表 | [community](file:///d:/CODE/project/community/pages/community/community.js) | 「邻里互动」下 `GET neighbor-assist/orders/public` 展示帮帮卡片 |
| 我的列表 | [neighbor-assist-orders-my](file:///d:/CODE/project/community/pages/neighbor-assist-orders-my/neighbor-assist-orders-my.js) | Tab：我发布的 / 我接的单 → `GET neighbor-assist/orders/my?role=publisher|helper` |
| 订单详情 | [neighbor-assist-order-detail](file:///d:/CODE/project/community/pages/neighbor-assist-order-detail/neighbor-assist-order-detail.js) | 打卡、导航、完成、确认、投诉弹窗、评价弹窗、聊天入口 |
| 聊天 | [chat](file:///d:/CODE/project/community/pages/chat/chat.js) | 文字、图片（原有）；**语音**：按住说话 → 上传 → `msgType: audio`；`orderScene=neighbor_assist` 时「查看订单」跳转帮帮详情 |

**即时通讯与电话**

*   **聊天**：`POST neighbor-assist/conversations/ensure` 返回 `conversation_id`，再进入 `pages/chat/chat`；消息沿用 `messages/history/{id}`、`messages/send`、`messages/upload`（语音 `msgType: audio`，内容为音频 URL）。
*   **电话**：详情页展示对方脱敏手机号，点击 `wx.makePhoneCall`；后端可按订单状态决定是否返回**完整号码**（仅双方接单后可见等策略）。

**消息栏（「消息」Tab）同步 — 接单与状态变更**

*   **原则**：邻里帮帮订单在**用户侧**不仅出现在「我的 → 帮帮订单」，**接单成功及后续关键节点**（如已接单、对方已打卡、待您确认、已完成）应**写入与「消息」页拉取的数据源一致**，使用户在 **会话列表**（`GET messages/conversations`）或 **系统通知**（`GET user/notifications` 等，若已对接）中可见，无需只依赖主动打开「帮帮订单」。
*   **接单瞬间（必做）**：`POST /neighbor-assist/orders/:id/accept` 成功后，后端应至少：
    1.  **发布方（下单/发布人）**：在 `conversations` 中更新或创建 `biz_type=neighbor_assist` 的会话，更新 `last_message` 摘要（如「邻居已接单，点击查看」）、`last_message_at`、未读数；或插入一条 **系统类通知** 指向该 `order_id`。
    2.  **接单方**：可写入对称会话或仅在「帮帮订单」列表展示，按产品二选一。
    3.  **可选**：模板消息 / 订阅消息（`requestSubscribeMessage`）提醒发布人打开消息页查看。
*   **其他状态**：服务完成待确认、已确认、已取消、投诉受理等，可同样通过 **会话最后一条摘要** 或 **通知表** 推送，保证消息栏与订单状态一致。
*   **前端**：[`message.js`](file:///d:/CODE/project/community/pages/message/message.js) 已调用 `messages/conversations`；后端补齐上述写入后，**消息栏即可展示接单与动态**，无需额外前端路径；若仅走系统 Tab，需前端合并 `user/notifications`（可后续迭代）。

### 2.3 后端 API 清单（前缀：`/api/v1`，与现有 `util` 一致）

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/neighbor-assist/orders` | 发布：body 含 `category, address, time, content, images[], reward_amount?`, `lat/lng?` |
| GET | `/neighbor-assist/orders/feed` | 首页滚动条：仅**待接单**或运营配置的曝光单，按时间倒序 |
| GET | `/neighbor-assist/orders/public` | 社区「邻里互动」：本小区/附近公开单，分页 |
| GET | `/neighbor-assist/orders/my` | Query：`role=publisher|helper`，`page`, `limit` |
| GET | `/neighbor-assist/orders/:id` | 详情：返回 `publisher_id`, `helper_id`, `my_role`, 双方脱敏手机号、地址、坐标、状态、`conversation_id` |
| POST | `/neighbor-assist/orders/:id/accept` | 接单（当前用户非发布人且状态允许）；**成功后必须触发消息栏同步**（见 §2.2「消息栏同步」）：更新 `conversations` 或写入 `notifications` |
| POST | `/neighbor-assist/orders/:id/check-in` | body：`latitude, longitude` |
| POST | `/neighbor-assist/orders/:id/complete-service` | 接单方提交服务完成 |
| POST | `/neighbor-assist/orders/:id/confirm` | 发布方确认结单 |
| POST | `/neighbor-assist/orders/:id/complaint` | body：`content`, 可选 `images[]` |
| POST | `/neighbor-assist/orders/:id/review` | body：`score` 1–5, `content?` |
| POST | `/neighbor-assist/conversations/ensure` | body：`order_id` → 返回 `conversation_id`（会话类型标记为 `neighbor_assist`） |

**消息服务扩展**：`messages/send` 支持 `msgType: audio|text|image`；`messages/upload` 支持音频文件（`formData.type=audio`）。

### 2.4 管理后台功能

*   **模块**：社区运营 → **邻里帮帮订单**（列表/筛选：小区、状态、时间、关键词）。
*   **操作**：查看详情、冻结/下架异常单、查看投诉与处理记录、必要时人工改状态（仅运维角色）。
*   **与首页/社区同步**：后台修改「是否上首页滚动」「是否在社区展示」等开关时，影响 `feed` / `public` 接口结果。

### 2.5 数据存储建议

**表：`neighbor_assist_orders`**

*   `id`, `order_no`, `community_id`, `publisher_user_id`, `helper_user_id`（可空）
*   `category`, `content`, `address`, `lat`, `lng`, `service_time`, `reward_amount_cent`
*   `status`（枚举同上）, `check_in_lat`, `check_in_lng`, `check_in_at`
*   `completed_at`, `confirmed_at`, `cancel_reason`, `created_at`, `updated_at`
*   `flags`（JSON：是否上首页、是否社区展示等）

**表：`neighbor_assist_complaints`**

*   `id`, `order_id`, `user_id`, `content`, `images_json`, `status`, `admin_reply`, `created_at`

**表：`neighbor_assist_reviews`**

*   `id`, `order_id`, `from_user_id`, `to_user_id`, `score`, `content`, `created_at`

**会话**：复用现有 `conversations` 表，增加 `biz_type='neighbor_assist'`, `biz_order_id` 便于 `ensure` 接口幂等创建；**接单及状态变更时更新该会话的 `last_message` / 未读**，以便「消息」Tab 列表与帮帮订单状态一致。

**通知（可选表 `notifications`）**：若会话列表不足以承载纯系统提示，可单独写入 `type=neighbor_assist_order`，`ref_id=order_id`，供消息页「通知」子 Tab 与 `conversations` 双轨展示。

---

## 3. 角色权限与工作台隔离 (Portal Isolation)

### 3.1 身份隔离设计
*   **前端逻辑**：
    *   使用 [rolePortals.js](file:///d:/CODE/project/community/utils/rolePortals.js) 进行路由控制。
    *   **技工工作台**：仅限 `role` 包含 `worker` 的用户访问，提供：今日订单、待接单池、收入统计、位置上报开关。
    *   **服务商工作台**：与 **集市商家** 共用分包入口（[merchant-home](file:///d:/CODE/project/community/package-merchant/pages/merchant-home/merchant-home.js)），在具备 `merchant` / `shop_id` / 审核通过等条件时可进入；除 **店铺订单** 外，通过 **直约服务订单**（`merchant-orders?scene=direct_service`）处理 **打包到家单**，详情与履约与技工端共用 [worker-order-detail](file:///d:/CODE/project/community/package-worker/pages/worker-order-detail/worker-order-detail.js)（`portal=merchant`，见 **§1.4**）。
*   **后端 API 需求**：
    *   `GET /worker/orders`：技工专属订单流，支持状态过滤（待上门、进行中、已完成）。
    *   `POST /worker/report-location`：技工端定时上报坐标，以便在前端“骑手位置”地图上展示。

---

## 4. 管理后台（Web 端）全量功能清单

### 4.1 运营看板
*   **实时地图**：基于 `Geo` 坐标，动态展示当前全城/全小区正在进行中的服务订单、活跃技工位置、热点服务区域。

### 4.2 财务中心
*   **对账系统**：每日自动生成各小区、各服务商的营收报表。
*   **提现审核**：
    *   **按钮：核对流水**：展示该用户所有待结算订单。
    *   **按钮：确认转账**：调用“微信支付-转账到零钱”接口，自动完成打款。

### 4.3 系统配置（全局开关）
*   **Banner 管理**：控制 [index.js](file:///d:/CODE/project/community/pages/index/index.js) 中的轮播图，支持设置跳转路径（分包页面或 H5）。
*   **金刚区管理**：动态配置首页的分类图标、名称及跳转 URL。
*   **分润规则设置**：设置平台抽成比例（如：技工订单抽 10%）。

---

## 5. 系统设计原则 (UX & Logic)

1.  **乐观更新 (Optimistic UI)**：所有涉及社交交互（点赞、收藏）的操作，前端优先变更状态，API 报错时再回滚。
2.  **状态机同步**：订单状态（待支付 -> 待接单 -> 待上门 -> 服务中 -> 已完成 -> 评价）必须在前后端严格对齐，任何不合法状态跳转（如：直接从未接单跳到已完成）后端需拦截并抛错。
3.  **LBS 优先**：凡是涉及“展示”的列表（技工、活动），后端必须默认按距离升序排列，将最贴近用户的资源放在首位。
