# 「首页 / 我的」服务域：前端配合说明与测试注意

> 对应后端实现与契约见仓库内 `首页与我的_服务域前后端联调需求.md` 第十一～十四章；本文只写**前端要不要改**、**怎么对接**、**测什么**。

---

## 一、是否需要前端修改迭代？

**需要。** 后端已落地新路由、统一 `errno` 响应、部分列表结构变化；小程序若仍按旧假数据或旧路径，会出现联调失败或展示不一致。

| 优先级 | 前端动作概要 |
|--------|----------------|
| P0 | `util` 解析：成功以 `errno === 0` 或兼容无 `errno`；取数用 `res.data`（数组或对象视接口而定）。 |
| P0 | `GET core/workers` 列表形态改为 **`{ list, total, page, page_size }`**，禁止再当纯数组遍历。 |
| P0 | 小区热卖改用 **`GET core/community/hot`**，传 **`community_id`** 或由后端从 Token 解析（需用户绑定默认小区）。 |
| P0 | 商品详情与精选同源：**`GET core/goods/:id`**（与 `goods/featured` 同一 ID 空间）。 |
| P1 | Banner：**`GET core/banners`**，字段 `image_url`、`link_type`、`link_value`；失败可降级本地图。 |
| P1 | `tidy-service`：**`util.get('core/service-groups/' + key)`**，与全局 baseUrl/Token 一致。 |
| P1 | 到家下单 / 帮帮下单：对接 **`POST /service-orders`**、**`POST /neighbor-assist/orders`**（JWT）及模拟/真实支付流程（与产品一致）。 |
| P2 | 快捷入口、classify 分页技工等与需求文档第三章、第八章一致。 |

不要求前端改动的部分：九宫格仍可本地配置展示（与需求 2.4 一致），但**下单后**必须走后端订单与派单链路。

---

## 二、后端契约要点（前端对接）

### 2.1 成功 / 失败

- 成功建议形态：`{ "errno": 0, "data": ... }`。
- 失败：`{ "errno": 非0, "errmsg": "..." }`；HTTP 可能仍为 200，**必须以 `errno` 判断业务成败**（与 `utils/util.js` 约定对齐）。

### 2.2 已提供或强相关的接口（前缀均为 `/api/v1`，小程序 `util.get` 一般为去掉前缀后的路径）

| 路径 | 说明 |
|------|------|
| `GET core/banners` | 轮播；`link_type`：`none` / `service` / `h5` / `page` 等与运营约定 |
| `GET core/service-groups/:key` | 九类 slug，返回 `title`、`categories`、`services` |
| `GET core/services/hot` | **全站**热卖；`limit` 默认 10、最大 20 |
| `GET core/community/hot` | **小区内**热卖；`community_id` 必填或登录用户有默认小区 |
| `GET core/services` | 分页列表；`page`、`limit`、`category_id` |
| `GET core/services/:id` | 服务详情；未上架返回业务失败 |
| `GET core/workers` | **`data.list` + `data.total`** |
| `GET core/workers/:id` | 技工详情 |
| `GET core/goods/featured` | 精选；可含 `unit` |
| `GET core/goods/:id` | 商品详情 |
| `POST service-orders` | 创建到家订单（JWT） |
| `POST service-orders/:id/pay` | 支付/模拟支付（JWT） |
| `GET service-orders/my` | 我的到家订单（JWT） |
| `POST neighbor-assist/orders` | 帮帮下单，双地址 JSON（JWT） |
| `POST neighbor-assist/orders/:id/pay` | 帮帮支付（JWT） |
| `GET neighbor-assist/orders/my` | 我的帮帮单（JWT） |

管理端（Vue 运营后台）已对接：`/admin/dispatch-queue`、`/admin/service-orders`、`/admin/neighbor-assist/orders` 及 assign；小程序不涉及。

### 2.3 字段与兼容

- 精选商品：`title` / `goodsTitle`、`price` / `goodsRealPrice`、`cover_image` / `mainPicture` 后端会多形态兼容，前端择一主字段即可。
- 技工头像：优先 **`avatar_url`**，无则再走本地占位。
- 图片：相对路径需继续走 `imgUrl()` + `imageBaseUrl`。

---

## 三、前端测试注意文档（联调 / 回归清单）

### 3.1 环境与配置

- [ ] `config.baseUrl`（或等价）指向**含 `/api/v1` 的正确网关**；勿写死旧 IP 未更新。
- [ ] 测试号已执行**数据库迁移**（含 `service_orders`、`neighbor_assist_orders`、Banner 扩展字段等），否则新接口 500。
- [ ] 小程序体验版 / 开发工具打开「不校验合法域名」仅用于开发；**正式环境**域名必须在微信公众平台配置。

### 3.2 未登录 / 已登录

- [ ] Core 只读接口（banners、hot、workers、goods 等）在**游客**下可访问（与产品一致）。
- [ ] 下单、我的订单、`community/hot` 若依赖用户默认小区，需**登录**且用户表有 **`community_id`**（或每次传 `community_id`）。
- [ ] Token 过期或缺失时：接口应 401 或业务 `errno`；前端要有统一跳转登录或提示，避免白屏。

### 3.3 响应解析（易错）

- [ ] 判断成功：**先看 `errno === 0`（或无 errno 的老接口）**，再取 `data`。
- [ ] `GET core/workers`：**使用 `data.list`**，不要 `data` 直接当数组。
- [ ] `GET service-orders/my`：后端 `data` 为 `{ list, total, page, limit }`，与 workers 类似，列表取 **`data.list`**。

### 3.4 小区热卖

- [ ] 生产环境 **`useCuratedHomeHotList === false`**，必须请求 **`core/community/hot`**。
- [ ] 传参：`community_id` 与当前展示小区一致；切换小区后**重新请求**热卖。
- [ ] 返回含 `shops` 时，跳转路径与「仅服务」区分（按产品设计）。

### 3.5 订单与支付

- [ ] 创建订单后记录 **`order_id`**，支付页 URL 带对 id。
- [ ] 支付失败 / 取消：订单状态仍为待支付时，可再次发起支付（与后端状态机一致）。
- [ ] 帮帮单：**起点 / 终点**两个 JSON 与后端字段名一致，且为对象（非字符串）。

### 3.6 边界与异常

- [ ] 弱网、超时：`util` 超时提示；可重试的接口（列表）要有防抖或 loading。
- [ ] `service-groups` 非法 key：应提示错误，不崩溃。
- [ ] 空列表：热卖、技工、Banner 均为空时的占位 UI。

### 3.7 管理端（若你们也测运营后台）

- [ ] 仅管理员 JWT 可调 `admin/*`。
- [ ] 「九州派单」页：待派单列表刷新、指派技工 ID 可选下拉、指派成功后队列减少。
- [ ] 对已派单订单再次指派：应提示业务错误（后端会拒绝）。

### 3.8 回归范围建议

- [ ] 首页内层 Tab「首页」：Banner、九宫格进落地页、热卖、技工、精选、快捷入口（按已改范围）。
- [ ] 「我的」：订单入口、优惠券角标（若有）、登录态。

---

## 四、与主需求文档的关系

- **产品级模块说明、字段表、九州与订单域**：仍以 `首页与我的_服务域前后端联调需求.md` 为准。
- **本文**：侧重**前端要不要改、对接什么、测什么**；后端接口速查见该文档**第十四章**。

---

## 五、修订记录

| 日期 | 说明 |
|------|------|
| 2026-04-15 | 初版：前端配合要点 + 测试清单 |
