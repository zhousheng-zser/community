# 技工后台 & 服务商后台 — Web 管理端需求说明

> **文档性质**：产品/研发共用的需求基线，用于立项、排期与验收。  
> **范围**：两套**相互独立**的浏览器端管理界面（各含**独立登录页**），分别面向 **C 端已入驻技工** 与 **已入驻服务商主体**，**不包含**平台总运营后台（调度/审核全站数据的 Admin），也不替代小程序内用户端功能。

**版本**：v3.1 · 2026-04-20（增补：本仓库后端实现索引、接口缺口、Web 管理端开发指引）  

---

## 1. 背景与目标

### 1.1 业务背景

- 小程序侧已实现用户端，并通过分包提供 **技工工作台 / 商家工作台** 的轻量入口；复杂操作（大屏看单、批量处理、财务对账、团队子账号等）在移动端体验差，需要 **独立 Web**。
- 「技工」与「服务商」在本项目中为两类主体：**技工**偏个人接单执行；**服务商**偏机构/团队提供到家服务 SKU 与履约（与「集市商家」不同，后者见小程序 `join-market` 等业务）。

### 1.2 建设目标

| 系统 | 目标 |
|------|------|
| **技工后台 Web** | 技工在 PC/平板浏览器上完成登录与实名认证、订单全链路履约、现场留证与加项、收益与提现，以及接单设置与消息辅助能力。 |
| **服务商后台 Web** | 服务商在浏览器中管理**本机构名下每一个商品**的上架/下架、库存与销量数据、补货预警，接收并处理 **C 端用户下单**，并具备与小程序前端配合的常见经营能力。 |
| **服务商端小程序** | 分包 `package-merchant`：店铺入驻与资质、商品与库存、订单处理、经营数据与辅助能力（与 Web **数据同源**）；复杂配置与大屏操作优先 Web。 |

### 1.3 非目标（边界）

- 不替代平台运营人员的「总后台」审核与全站配置（可与现有 `admin/*` 能力并存，权限边界需区分）。
- 与小程序 **账号同源、数据同源**；Web 独有配置须在接口层与 C 端展示规则一致，避免「后台改了用户看不见」。

---

## 2. 术语与关系

| 术语 | 含义 |
|------|------|
| **技工** | 已通过入驻、可执行上门服务的个人；关联 `user_id`、技工档案（如 `workers`）。 |
| **服务商** | 提供标准化到家服务的主体（企业/门店），名下有 **多个商品（SKU）**、库存与订单；可关联多名技工/员工。 |
| **商品** | 本文「服务商后台」中的商品指 **该服务商可售卖的到家服务 SKU**（与 C 端 `core/services`、下单页一致）；**非**集市店铺实物商品（除非产品明确合并）。 |
| **客户端** | 小程序/用户端；用户下单后订单进入服务商或平台订单池，服务商后台须能接收并处理。 |

---

## 3. 总体架构要求

### 3.1 部署与入口

- 两套 **独立部署 URL**（或同域不同 path，如 `/worker/*`、`/provider/*`），**各自独立登录页**，禁止共用同一套登录表单混用身份。
- 支持 HTTPS；兼容 Chrome / Edge / Safari 近两个大版本；**响应式**：最低宽度建议 1280px 为舒适布局，平板可横向滚动或简化布局。

### 3.2 身份与鉴权（需求级）

- **技工后台**：仅允许「已具备技工身份且账号有效」的账号登录；会话/Token **角色限定为技工 Web**，后端强校验。
- **服务商后台**：仅允许「已绑定服务商主体且具备后台权限」的账号登录；**与技工后台应用标识、Token 签发分离**。
- 登出、会话过期、强制下线与后端一致；前端统一跳转登录页。

### 3.3 与小程序 / 前端的配合原则

- **状态机唯一来源**：订单状态、商品上下架、库存扣减以后端为准；Web 与小程序 **同接口模型**，仅权限与菜单不同。
- **实时性**：新订单、库存预警、审核结果等，支持 **WebSocket / 轮询 / 浏览器通知（可选）** 中的一种或组合，具体见非功能需求。
- **字段对齐**：商品 ID、订单号、状态枚举与 `utils/config.js` 所在环境的 `api/v1` 文档一致；变更需同步前端（小程序）发版说明。

---

## 4. 技工后台 Web — 详细功能需求

### 4.1 登录与账号安全

| 需求项 | 说明 |
|--------|------|
| **手机号快捷登录** | 支持 **手机号 + 短信验证码** 登录（主路径）；可选扩展 **手机号 + 密码** 作为辅助。 |
| **风控** | 验证码发送频控、连续失败锁定、异常设备/ IP 提示（策略由后端定）。 |
| **会话** | 登录态保持、主动登出、Token 过期刷新；与小程序可同时在线或互踢策略由产品定，须在文档或帮助中心说明。 |

### 4.2 实名认证

| 需求项 | 说明 |
|--------|------|
| **信息采集** | 真实姓名、身份证号（或按监管要求的证件类型）；与公安/三方实名接口对接由后端实现，前端提供表单与 OCR 上传（若需要）。 |
| **状态展示** | 明确展示：**未认证 / 认证中 / 已通过 / 已驳回**；驳回原因可读。 |
| **权限** | 未完成实名认证前，可限制「接单」「提现」等关键操作（规则可配置：仅警告或硬拦截）。 |

### 4.3 技能与资质

| 需求项 | 说明 |
|--------|------|
| **技能** | 维护可接单技能类目/标签（与平台类目体系对齐）；支持多选；**变更后若需重新审核**，展示审核状态。 |
| **资质上传** | 证书/资格证照片上传（多张）、有效期；**提交审核**后展示平台审核状态（待审/通过/驳回及原因）。 |
| **与接单关系** | 仅 **已通过审核** 的技能/资质可在 C 端展示或参与派单（与平台规则一致）。 |

### 4.4 订单管理（核心）

#### 4.4.1 订单状态与列表

- 提供 **全状态** 列表视图，至少覆盖以下业务语义（具体枚举名与后端对齐）：  
  **待接单** → **待上门**（已接、未到达）→ **服务中** → **待确认完成**（若需要用户确认）→ **已完成**；以及 **已取消**、**已拒单**、**异常/售后中** 等扩展状态。
- **筛选**：按状态、时间范围、服务类型、订单号/用户手机后四位搜索、小区或区域（若字段存在）。
- **排序**：默认按下单时间或预约时间倒序；支持按预约时间正序（便于当日路线规划）。
- **分页**：标准分页或虚拟滚动；导出（可选，见辅助功能）。

#### 4.4.2 接单与拒单

- **待接单** 订单：提供 **接单**、**拒单**；拒单需 **原因**（选填或必选由产品定），提交后状态与 C 端、平台同步。
- **权限**：仅能操作 **已指派给自己** 或 **抢单池内可抢且已抢到自己名下** 的订单（以后端校验为准）。

#### 4.4.3 新订单提醒

- **站内**：顶部或订单模块 **红点/数字角标**；新单进入待接单时刷新列表高亮或置顶（可配置短时间）。
- **站外（可选）**：浏览器通知（需用户授权）、短信/语音由平台侧模板消息能力决定，Web 提供「提醒设置」开关。

### 4.5 服务过程履约（订单详情内）

以下能力均在 **单订单详情** 或 **履约子页** 中提供，且 **操作留痕**（操作人、时间、定位或图片 URL）：

| 能力 | 说明 |
|------|------|
| **上门定位打卡** | 调用浏览器地理定位（需用户授权）或对接小程序同款定位策略；**到达打卡** 记录时间戳与坐标（后端校验与订单地址距离阈值可配置）。 |
| **导航到客户地址** | 一键跳转第三方地图（高德/腾讯/百度）导航，地址取自订单；若浏览器限制，提供 **复制地址** 与 **二维码推送到手机**（可选）。 |
| **一键联系用户** | 点击拨打订单预留手机号（脱敏展示规则与法务一致）；可记录「已联系」操作（可选）。 |
| **服务前后照片留证** | 上传 **服务前**、**服务后** 照片（多张）；支持压缩与进度；**必填策略** 可按服务类目配置。 |
| **现场加项加价申请** | 填写加项说明、加价金额，提交后 **待用户或平台确认**（流程与后端一致）；未确认前不得直接改订单实付（除非业务允许「线下补款」并留痕）。 |
| **服务完成确认** | 技工端点击 **完成服务**；若需用户确认，则进入「待用户确认」直至超时规则触发。 |

### 4.6 收益统计、佣金明细与提现

| 需求项 | 说明 |
|--------|------|
| **收益统计** | 按日/周/月汇总 **订单收入、佣金、补贴、扣款** 等（字段以后端为准）；支持简单图表（折线/柱状，可选）。 |
| **佣金明细** | 列表展示每笔订单对应的 **佣金基数、佣金比例、实发佣金、结算状态**；支持筛选与导出（CSV/Excel，异步任务亦可）。 |
| **在线提现** | 绑定提现账户（银行卡实名一致或微信零钱，与平台支付能力对齐）；**发起提现**、查看 **处理中/成功/失败** 及失败原因；遵守最低提现额与手续费规则（展示在页面）。 |

### 4.7 个人资料与接单设置

| 需求项 | 说明 |
|--------|------|
| **个人资料** | 头像、昵称、简介等可编辑项；与实名信息 **敏感字段** 修改规则分离（改实名可能需重新审核）。 |
| **服务区域** | 地图或行政区选择接单范围；保存后生效规则与派单过滤一致。 |
| **接单技能** | 与 §4.3 技能数据一致，可在此快捷调整（仍受审核约束）。 |
| **接单开关** | **全局开关**：关闭后不再派单/抢单（已有订单仍须履约）；状态在 C 端是否展示「休息中」由产品定。 |

### 4.8 消息通知、平台公告、客服反馈、服务规范

| 需求项 | 说明 |
|--------|------|
| **消息通知** | 站内信列表：订单节点、审核结果、提现结果、系统通知；**未读数** 与标记已读。 |
| **平台公告** | 公告列表与详情（只读）；重要公告可强提示一次（可选）。 |
| **客服反馈** | 提交问题/截图；查看处理状态与历史记录（与工单或客服系统对接）。 |
| **服务规范** | 展示平台发布的 **服务标准、红线、计价规则** 等富文本；版本更新可读历史（可选）。 |

---

## 5. 服务商后台 Web — 详细功能需求

### 5.0 服务商端小程序（`package-merchant`）能力基线

以下与 **§5.1～5.8 Web 能力** 对齐，**状态机、商品、订单字段唯一来源为后端**；小程序侧重移动场景下的 **接单、提醒、简单改价与库存、联系用户**，复杂营销活动、大批量导出、子账号 RBAC 等以 **Web** 为主（可分期）。

#### 5.0.1 店铺入驻与资质

- **店铺信息**：名称、Logo、简介、联系方式、经营类目；**资质认证**：主体资质、行业许可等上传与审核状态展示（与 `pages/join-market` 及后台审核一致）。
- **营业时间**：可配置每日时段/休息日（规则与配送、用户端展示一致）。
- **配送范围**：地图圈选、半径或行政区；**超区提示**与下单拦截以后端为准。
- **商品上下架管理**：见 §5.2；小程序提供 **列表、上架开关、补货**（与 `merchant-goods` 一致）。

#### 5.0.2 商品管理（分类、定价、库存、图文）

- **分类**：归属店铺类目/平台类目，支持多规格 SKU（若业务启用）。
- **定价**：售价、划线价、会员价等（字段与后端模型一致）。
- **库存**：可售库存、安全库存阈值、补货入库；与订单扣减、退款回补 **同一套数据**。
- **图文编辑**：主图、详情图、富文本描述；移动端可 **简编**，完整排版可 Web 完成。

#### 5.0.3 订单实时处理（核心）

- **实时性**：新订单 **推送/轮询/WebSocket** 至少一种；列表与角标刷新。
- **新订单语音提醒**：可选开启 **系统提示音 / 语音播报**（需用户授权与平台策略）。
- **接单 / 拒单**：待接单订单可操作，拒单原因留痕；与 C 端、骑手/配送侧 **状态同步**。
- **打印小票 / 备货标记**：蓝牙或网络打印机对接（小程序提供 **打印指令或跳转**）；订单 **备货中/已拣货** 等标记与后端一致。
- **用户联系**：一键拨号、在线消息入口（若接入）。
- **配送进度**：对接物流单号或自配送节点，用户端同步展示。
- **异常订单**：缺货、退款中、地址异常等 **工单化** 或状态流转，与售后 §5.6 衔接。

#### 5.0.4 营业数据统计

- **今日 / 月度营收、订单量、客单价**（字段以后端为准）。
- **热销商品**、**类目占比**（可选图表）。
- **收支明细**：入账、退款、平台佣金、提现；与财务 §5.7 一致。

#### 5.0.5 营销与售后

- **营销活动**：满减、折扣、优惠券（创建与编辑权限可仅 Web；小程序 **查看与核销** 或简易开关）。
- **退款与售后**：售后单列表、同意/拒绝（权限内）、与 C 端进度同步。

#### 5.0.6 店员与权限

- **店员管理**：子账号绑定、角色（店长/店员/拣货/客服等）；**权限分配** 控制菜单与数据范围（与 Web RBAC 一致）。

#### 5.0.7 消息、规则与客服

- **消息通知**：订单、审核、系统公告；**未读数**与已读。
- **平台规则**：服务标准、红线、计价规则等 **只读** 展示。
- **客服对接**：工单、反馈入口与处理状态；与平台客服系统可对接。

#### 5.0.8 UI 与体验

- 服务商端小程序 **视觉主题**与项目约定：**黄褐色系**主色（与技工端并列统一），保证 **店铺日常运营高效、订单履约顺畅**。

#### 5.0.9 分期建议（小程序）

| 阶段 | 建议范围 |
|------|----------|
| **一期** | 入驻与店铺资料、商品列表与上下架/库存/补货、订单列表与接单拒单、用户联系、基础统计摘要、消息与规则入口 |
| **二期** | 语音提醒、打印小票、备货标记、配送节点、营销活动配置、售后闭环、店员子账号 |

---

### 5.1 登录与账号

- 面向 **机构主体**：主账号登录；**子账号/多门店（可选）** 需 **角色与数据范围**（仅本店/全部门店）。
- 登录方式：**手机号 + 验证码** 或 **账号密码**（与技工后台入口、样式、域名区分）；风控同 §4.1 原则。

### 5.2 商品管理 — 上架、下架（单品粒度）

| 需求项 | 说明 |
|--------|------|
| **商品列表** | 展示本服务商名下 **每一个商品（SKU）** 一行：名称、主图、标价、**上架状态**、库存、销量摘要等。 |
| **上架 / 下架** | 对 **单个商品** 独立操作 **立即上架** / **立即下架**；操作二次确认（可选）；**状态与 C 端同步**，用户端不可再买已下架商品。 |
| **编辑** | 标题、副标题、图文详情、价格、服务时长、服务说明等（字段与 `core/services` 或后台商品模型一致）；**敏感字段**是否需平台审核由业务定，若需审核则展示「待审核」中间态。 |

### 5.3 数据管理 — 商品维度经营数据

| 需求项 | 说明 |
|--------|------|
| **商品维度统计** | 每个商品：**累计销量（件/单）**、**销售额（可选）**、**浏览量（若 C 端有埋点）**；支持按时间筛选（今日/7 日/30 日/自定义）。 |
| **库存数量** | 展示 **当前可售库存**；下单扣减、退款回补规则以后端为准，前端展示 **实时或准实时** 库存。 |
| **库存不足与补货** | 设置 **安全库存阈值**（按商品可配置）；当 **当前库存 ≤ 阈值** 时：列表 **醒目标识**、**补货提醒**（列表筛选「需补货」）；支持 **补货入库** 操作（录入增加数量与备注）或 **仅提醒** 由运营线下处理（两种模式可选其一为 MVP）。 |

### 5.4 订单中心 — 接收客户端下单

| 需求项 | 说明 |
|--------|------|
| **订单来源** | 展示订单来自 **小程序用户下单**；订单内包含：商品信息、规格、数量、实付、用户信息（脱敏）、预约时间、服务地址等。 |
| **列表与筛选** | 按状态、时间、商品、订单号等筛选；与平台订单状态机对齐。 |
| **详情与处理** | 查看详情、备注；**发货/指派技工/核销** 等动作以后端与业务模式为准（服务商是否可派自有技工见 §5.5）。 |
| **与 C 端一致** | 订单状态变更后，用户在小程序侧 **订单详情** 同步展示；Web 操作不得绕过支付与退款规则。 |

### 5.5 技工/员工与派工（与机构关系）

- 维护本机构关联技工：列表、绑定/解绑、是否可接单。
- **派单**：若业务允许，从详情将订单 **指派给某技工**；技工侧在技工后台可见（与 §4 联动）。

### 5.6 与前端（小程序）配合的常见能力（建议一期或二期）

以下均为 B 端常见合理能力，**在排期中可分阶段**，但需求文档一次性列全以便研发预留接口：

| 模块 | 能力 |
|------|------|
| **售后** | 查看退款/售后单、同意/拒绝（权限内）、与 C 端退款进度同步。 |
| **评价** | 查看用户对订单/商品的评价、回复（若平台开放）。 |
| **营销** | 优惠券/活动由平台或服务商配置（权限待定）；列表只读或简易配置。 |
| **导出** | 订单、商品、库存流水 **导出 Excel**（异步任务 + 下载中心）。 |
| **打印** | 订单小票/拣货单浏览器打印（可选）。 |
| **多门店** | 门店切换、按门店维度的商品与订单（若启用）。 |
| **通知** | 新订单站内提醒、可选短信/订阅消息触发配置说明。 |

### 5.7 财务与对账

- 账单汇总、分账状态、开票信息（若业务涉及）；与 §4.6 类似，主体为 **服务商账户** 而非技工个人。

### 5.8 企业信息与设置

- 主体名称、统一社会信用代码、联系人、客服电话；**合同/结算信息**只读或受限编辑。

---

## 6. 非功能需求（两套后台通用）

| 类别 | 要求 |
|------|------|
| **安全** | 全站 HTTPS；HttpOnly Cookie 或安全存储 Token；关键操作二次确认；**操作审计日志**（谁、何时、对哪张单、旧值/新值摘要）。 |
| **权限** | 后端强校验；越权 403；前端路由守卫。 |
| **实时性** | 新订单、库存预警：至少 **轮询** 或 **WebSocket** 之一；指标见产品 SLA。 |
| **性能** | 列表首屏可接受（建议 2s 内）；大表导出异步。 |
| **可用性** | 空状态、加载中、错误重试；文案中文、符合业务习惯。 |
| **兼容性** | 见 §3.1；地图跳转不依赖单一厂商时可配置。 |
| **隐私** | 用户手机号等 **脱敏展示**；日志脱敏。 |

---

## 7. 接口与数据依赖（研发对齐）

### 7.1 总原则

- 技工 Web、服务商 Web 使用 **独立应用前缀**（建议如 `/api/v1/worker-portal/*`、`/api/v1/service-provider-portal/*`），与 C 端小程序、平台 Admin 区分；**鉴权主体与角色**在后端强校验。
- 订单状态枚举、商品字段、库存字段须输出 **与小程序一致的 OpenAPI/表格**（变更需同步发版说明）。

### 7.2 技工端：小程序分包与后端接口对应

小程序技工分包根目录：`package-worker/`。下列路径均相对 **`/api/v1`**（与 `utils/config.js` 中 baseUrl 一致；具体拼接以 `utils/util.js` 为准）。

| 页面（分包内） | 功能摘要 | 主要 HTTP 接口 | 备注 |
|----------------|----------|----------------|------|
| `worker-home` | 工作台首页、订单数统计 | `GET worker/service-orders`（`page`、`limit`） | 用于桶统计；接口未就绪时前端可降级为空统计 |
| `worker-orders` | 订单列表、Tab 筛选 | 同上 | 状态分组由前端 `utils/workerOrderUi.js` 根据 `status`/`status_text` 推断 |
| `worker-order-detail` | 详情与履约 | `GET worker/service-orders/:id`；若 404/501 可降级 `GET service-orders/:id` | 详情 `data` 须含地址、联系人、金额、状态等字段 |
| `worker-order-detail` | 接单 / 拒单 | `POST worker/service-orders/:id/accept`、`.../reject` | `reject` 建议 body 含 `reason` |
| `worker-order-detail` | 上门打卡 | `POST worker/service-orders/:id/check-in` | body：`latitude`、`longitude`、`accuracy`（与 §4.5 一致） |
| `worker-order-detail` | 前后照片留证 | `POST worker/service-orders/:id/evidence` | body：`kind`（before/after）、`urls` 数组 |
| `worker-order-detail` | 加项加价申请 | `POST worker/service-orders/:id/addon-request` | body：`remark` 或 `content` |
| `worker-order-detail` | 服务完成 | `POST worker/service-orders/:id/complete` | 与状态机衔接 |
| `worker-order-detail` | 与客户会话（文字/图片） | `POST messages/order-conversation/ensure` | body：`channel: 'worker_customer'`、`order_no`、`worker_user_id`、`customer_user_id`（或 `buyer_user_id`）、`buyer_name`；会话消息走 `messages/send`、`messages/upload`，聊天页见 `pages/chat/chat` |
| `worker-home` | 余额摘要（可选） | `GET user/profile` | **本仓库 Node 未实现**；失败时前端将余额显示为「—」，不影响订单接口 |
| `worker-mine` | 接单开关等 | 与 profile/技工档案相关接口对齐（具体路径以后端清单为准） | 与 §4.7 接单开关一致 |

**演示模式**：`worker-order-detail?mock=1` 使用 `utils/workerOrderMock.js` 展示 UI，**不调用**上述 POST；后端联调时请使用真实订单 ID。

**后端开发优先级建议（技工履约）**：先实现 **详情 GET** 与 **列表 GET**（含状态字段），再实现 **accept / reject / check-in / evidence / complete**，**addon-request** 可与用户确认流程一并上线。

**本仓库服务端对齐**：`backend/src` 已挂载 **`/api/v1/worker`** 下与上表一致的内存演示实现（`routes/workerPortal.js`、`controllers/workerPortalController.js`、`workerPortalStore.js`）。请求须带 **`Authorization: Bearer <任意非空 token>`**（开发阶段不校验 JWT，仅作登录态占位）。部署生产前须替换为数据库、真实 JWT 与技工身份校验。本地联调可将小程序 `utils/config.js` 的 `baseUrl` 指向本机 Node 服务（如 `http://127.0.0.1:3000/api/v1`）。

### 7.3 技工后台 Web 与上述接口的关系

- **订单列表、详情、履约动作**（打卡、照片、完成、拒单等）与小程序 **共用同一套业务接口**（路径建议仍为 `worker/service-orders/*`），通过 **Web 登录态 Token** 调用；后端在网关或控制器层区分 `Authorization` 来源，但 **订单数据模型与状态机唯一**。
- **Web 独有** 能力（如批量导出、日历视图、大屏看板）可在 `worker-portal/*` 下 **增量** 提供，**不得**另造一套互斥的订单状态。
- 管理端（若指 **平台运营 Admin**）与 **技工自用 Web** 权限边界见 §1.3：运营侧可全站审核；技工 Web 仅本人数据。

### 7.4 商家端（集市）：商品与库存接口（小程序 `package-merchant`）

与 **§5.0**（小程序能力总览）、**§5.2、§5.3** 对应；小程序页面 **`merchant-goods`**、首页统计拉取商品列表。路径相对 **`/api/v1`**。

| 能力 | 方法 | 路径（优先 / 降级） | 说明 |
|------|------|----------------------|------|
| 商品列表 | GET | `market/merchant/goods`；失败时可试 `market/shop/goods` | Query：`page`、`limit`、`shop_id`（见 `utils/merchantShopContext.js`）；`data.list` 为数组 |
| 商品详情（编辑页） | GET | `market/merchant/goods/:id`；降级 `market/shop/goods/:id` | 返回 `data.goods`；含 `title`、`main_image`、`price`、`description`、`stock`、`safe_stock` 等 |
| 更新商品资料 | PATCH | `market/merchant/goods/:id`；降级 `market/shop/goods/:id` | body：可选 `title`、`main_image`、`price`、`stock`、`safe_stock`、`description` 等（与 `merchant-goods-edit` 一致） |
| 补货入库 | POST | `market/merchant/goods/:id/restock`；降级 `market/shop/goods/:id/restock` | body：`quantity` 或 `qty`（正整数） |
| 上架下架 | POST | `market/merchant/goods/:id/shelf`；降级 `market/shop/goods/:id/shelf` | body：`published` 或 `is_published` |
| 主图上传 | POST | `messages/upload`（`wx.uploadFile`） | 返回图片 URL/路径后写入 `main_image` 再 PATCH 保存 |

列表项字段须兼容（与前端 `merchant-goods.js` 一致）：`id`、`title`/`name`、`stock`/`inventory`、`sales_count`/`sales`、`safe_stock`/`low_stock_threshold`、`is_published`/`published`/`on_shelf`、`main_image`/`image`、`price`、`description` 等。

**订单列表（缺口）**：`merchant-home`、`merchant-orders` 会请求 **`GET market/merchant/orders`**，失败降级 **`GET market/shop/orders`**。**本仓库 Node 未实现**该路由；需线上网关或其它服务提供，否则首页/订单页统计与列表为空或提示「接口待上线」。

**本仓库服务端对齐**：`backend/src` 已在 **`/api/v1/market/merchant`** 与 **`/api/v1/market/shop`** 挂载相同路由（`routes/marketMerchantGoods.js`、`merchantGoodsController.js`、`merchantGoodsStore.js`），内存演示 SKU 含库存、安全库存、价格、描述、主图字段；**须带 `Authorization: Bearer <token>`**（开发阶段仅校验存在）。生产环境对接真实店铺商品表与权限。

### 7.5 参考文档

- `doc/首页与我的_服务域前后端联调需求.md`
- `doc/直约服务商与直约技工_接口规范.md`（首页名单/直约展示；**订单履约以 §7.2 为准**）
- `doc/首页与我的_后端404与待上线接口修复说明.md`（排障与待上线项）

### 7.6 消息与会话（技工 / 服务商 / 用户共用）

小程序统一走 **`/api/v1/messages/*`**（见 `backend/src/routes/messagesRoutes.js`）。与订单相关的会话通过 **`POST messages/order-conversation/ensure`** 建立；`channel` 区分业务（如集市 `shop_buyer` / `shop_rider`、到家 **`worker_customer`** 等）。列表、历史、发消息、发图分别对应 `conversations`、`history/:id`、`send`、`upload`。

**本仓库**：内存会话存储见 `orderConversationStore.js`；上传图片落盘至 `data/uploads/images/chat`，静态访问前缀 **`/uploads/chat/...`**（与 `app.js` 中 `express.static` 一致）。

### 7.7 本仓库后端文件索引（研发拆任务）

| 领域 | 挂载路径 | 路由文件 | 控制器 / 存储 |
|------|----------|----------|----------------|
| 技工订单履约 | `/api/v1/worker` | `backend/src/routes/workerPortal.js` | `controllers/workerPortalController.js`、`workerPortalStore.js` |
| 集市商家商品 | `/api/v1/market/merchant`、`/api/v1/market/shop` | `backend/src/routes/marketMerchantGoods.js` | `controllers/merchantGoodsController.js`、`merchantGoodsStore.js` |
| 消息与会话 | `/api/v1/messages` | `backend/src/routes/messagesRoutes.js` | `controllers/messagesController.js`、`orderConversationStore.js` 等 |

入口：`backend/src/app.js` 注册上述 `app.use`；进程监听见 `backend/src/index.js`（默认端口 **3000**，健康检查 **`GET /health`**）。

**鉴权约定（开发态）**：上述业务路由普遍要求请求头 **`Authorization: Bearer <token>`**，且 **token 字符串长度 ≥ 12**（与 `requireBearer` 实现一致）；**未校验 JWT 内容与技工/店铺身份**，上线前必须替换为真实鉴权与数据权限。

### 7.8 接口缺口与线上依赖（排期参考）

| 接口/能力 | 前端调用位置 | 本仓库 Node |
|-----------|----------------|-------------|
| `GET user/profile` | `package-worker` 技工首页余额 | 无 |
| `GET market/merchant/orders`（及 shop 降级） | `merchant-home`、`merchant-orders` | 无 |
| 技工 Web / 服务商 Web 独立登录、RBAC | 浏览器端（未在本仓库） | 无（需业务网关或新服务） |

以上项在 **`utils/config.js` 的 `baseUrl`** 指向线上时，若线上已部署则可联调通过；仅跑本仓库后端时按 **§7.7** 已有能力验收。

---

## 8. 分期建议（可随合同调整）

| 阶段 | 技工后台 | 服务商后台 |
|------|----------|------------|
| **一期** | 手机验证码登录、实名与资质流程、订单全状态列表与接单拒单、履约打卡/照片/完成、收益与佣金明细、提现、接单开关、消息与公告 | 登录、商品列表、**单品上架下架**、库存与销量展示、**补货阈值与提醒**、订单接收与处理、基础数据筛选 |
| **二期** | 浏览器新单通知、加项加价完整闭环、日历视图、导出 | 售后与评价、多门店、导出中心、打印、子账号 RBAC |

---

## 9. 验收要点（摘要）

- 技工：**实名/资质状态**与操作限制符合规则；订单全链路动作与 C 端状态一致；履约留证（定位、照片）可追溯；提现与佣金字段准确。
- 服务商：**每个商品可独立上下架**；**库存与销量、补货提醒**准确；客户端下单在后台可见且状态同步。
- 两套后台 **权限隔离**、**无串权**；关键操作 **可审计**。

---

## 10. 待产品确认项（评审前填写）

- [ ] 技工：未完成实名是否 **禁止接单**（硬拦截）  
- [ ] 加项加价：用户确认方式（小程序内确认 / 短信链接 / 仅记录线下）  
- [ ] 服务商：补货是 **系统入库** 还是 **仅提醒**  
- [ ] 订单：平台指派 / 服务商指派 / 技工抢单 的合法组合  
- [ ] 服务商商品是否 **必须经过平台审核** 后才能上架  

---

**文档维护**：需求变更请更新版本号与日期，并同步后端接口清单（建议另维护 `api/v1` 路由表或与 OpenAPI 仓库双向链接）。

---

## 11. 后端开发说明（对齐本仓库 `backend/src`）

### 11.1 职责边界

- **技工侧**：到家服务订单的列表、详情、接单拒单、打卡、留证、加项申请、完成；数据模型以 `workerPortalStore` 内存演示为准，替换为数据库时需保持 **与 §7.2 路径、字段语义一致**。
- **服务商（集市商家）侧**：店铺维度商品的 **列表、详情、PATCH 编辑、补货、上下架**；与小程序 `package-merchant` 共用接口。
- **消息侧**：订单会话 ensure、发消息、图片上传；`worker_customer` 通道与技工—客户沟通对齐（见 `orderConversationStore.js` 中 `CHANNEL`）。

### 11.2 本地运行与联调

1. 安装依赖：`cd backend && npm install`
2. 启动：`node src/index.js`（或项目约定的 `npm start`）
3. 小程序 `utils/config.js` 将 **`baseUrl`** 设为 `http://<主机>:<端口>/api/v1`（真机调试需可访问的局域网 IP / HTTPS 隧道）
4. 小程序登录后需写入 **`token`** 至 `wx.setStorageSync('token', ...)`，长度满足 Bearer 校验即可联调

### 11.3 扩展生产环境时的要点

- 将 `workerPortalStore`、`merchantGoodsStore`、内存消息存储替换为 **数据库 + 事务**；`restock`、`shelf`、订单状态变更需 **幂等** 与 **审计日志**。
- **`GET/PATCH market/merchant/goods/:id`** 与列表 **`GET .../goods`** 的字段必须一致，避免编辑页与列表展示冲突。
- 实现 **`market/merchant/orders`** 与 **`user/profile`** 时，响应形状需与小程序 `unwrapList`、页面字段兼容（见各页 `*.js`）。

---

## 12. Web 管理端（浏览器）开发说明

> **范围**：§3、§4、§5 所描述的 **技工后台 Web** 与 **服务商后台 Web**；**代码仓库**：可与小程序分离（独立前端工程），通过同一 **`/api/v1`** 业务接口交互（与 §7 一致）。

### 12.1 与小程序的关系

| 维度 | 说明 |
|------|------|
| **数据与状态机** | 与小程序分包 `package-worker`、`package-merchant` **同一套后端接口**（§7.2、§7.4、§7.6），不得复制一套互斥状态 |
| **鉴权** | 浏览器侧独立登录页；Token 存 HttpOnly Cookie 或安全存储；请求头仍 **`Authorization: Bearer`**（或与网关约定等价头） |
| **能力差异** | Web 适合大屏列表、批量导出、子账号 RBAC、复杂表单；移动端已实现的履约动作 Web 应能调用 **相同 POST 路径** |

### 12.2 建议模块与接口映射（最小对照）

| Web 模块（需求 §4 / §5） | 主要接口（相对 `/api/v1`） |
|--------------------------|----------------------------|
| 技工 — 订单列表/详情/接单拒单/打卡/照片/完成 | `worker/service-orders`、`worker/service-orders/:id`、各 `POST .../accept|reject|check-in|evidence|complete` |
| 技工 — 与客户沟通 | `messages/order-conversation/ensure`（`worker_customer`）、`messages/send`、`messages/upload` |
| 服务商 — 商品列表/编辑/上下架/补货 | `market/merchant/goods`、`market/merchant/goods/:id`（GET/PATCH）、`.../shelf`、`.../restock` |
| 服务商 — 订单中心 | `market/merchant/orders`（**待后端实现**，见 §7.8） |

### 12.3 前端工程建议（非强制）

- 技术栈：Vue 3 / React 18 + 路由 + 组件库（Ant Design / Element Plus 等），**HTTPS 部署**
- **路由守卫**：未登录跳转登录页；角色与 **技工 / 服务商** 应用标识分离（§3.2）
- **上传**：图片走 `messages/upload` 或与后端统一的上传域名，再回填 URL 到表单

### 12.4 本仓库不包含的内容

- **浏览器端 Web 源码**不在小程序仓库内；本文档仅规定 **接口与需求对齐方式**。实现 Web 时请以 §4、§5、§7、§11 为验收基线。

---

## 附录 A：技工端后端交付物清单（供研发拆任务）

| 序号 | 交付物 | 说明 |
|------|--------|------|
| A1 | 路由与鉴权 | `worker/*` 或 `worker-portal/*` 路由注册；技工身份与 `user_id` 绑定校验（开发可参考 `backend/src/routes/workerPortal.js`） |
| A2 | 订单列表/详情 | 字段与 §7.2 表对齐；支持分页与筛选（后续扩展） |
| A3 | 履约 POST | check-in、evidence、accept、reject、complete、addon-request 幂等与审计日志 |
| A4 | Web 登录 | 短信验证码会话；Token 与小程序可并存或互踢（产品定） |
| A5 | 与 Admin | 运营审核技工资质时，数据来源与技工 Web 展示一致，避免两套状态 |

### 附录 B：商家集市商品接口（后端交付参考）

| 序号 | 交付物 | 说明 |
|------|--------|------|
| B1 | 路由 | `GET/PATCH .../market/merchant/goods/:id`、`GET .../goods`、`POST .../restock`、`POST .../shelf`（与 `market/shop` 降级路径行为一致） |
| B2 | 库存 | `restock` 原子增加 `stock`/`inventory`；与订单扣减、退款回补规则一致 |
| B3 | 上下架 | `shelf` 与 C 端可购状态同步 |
| B4 | 商品资料 | `PATCH` 支持 `title`、`main_image`、`price`、`description`、`safe_stock` 等，与小程序 `merchant-goods-edit` 一致 |
| B5 | 开发参考 | `backend/src/routes/marketMerchantGoods.js`、`merchantGoodsController.js` |
