# 第一阶段：本地基础环境与用户认证

## 1. 当前完成进度
- [x] 初始化 Node.js 项目 (`backend/package.json`)
- [x] 安装核心依赖 (`express`, `cors`, `dotenv`, `mysql2`, `sequelize`, `jsonwebtoken`, `axios`, `nodemon`)
- [x] 搭建基础目录结构 (`src/config`, `src/controllers`, `src/models`, `src/routes`, `src/utils`, `src/middlewares`)
- [x] 创建环境变量文件 (`.env`)
- [x] 创建 Express 主入口文件 (`src/index.js`)
- [x] 初始化 Sequelize ORM 配置 (`src/config/config.js`)

## 2. 云端环境同步状态 (更新于今日)
- [x] **远程数据库迁移**：数据库已在云服务器成功创建并联通。
- [x] **后端环境配置**：Node.js 程序已在远程服务器运行，`.env` 已配置。
- [x] **前后端联通**：小程序已能成功向 `jshsp1.eds-tech.cn` 发起请求。

## 3. 第一阶段“收官”核对
- [x] **UserModel 同步**：Sequelize 模型与云端数据库同步正常。
- [x] **微信登录闭环**：云端 WX_APP 配置正确，小程序登录逻辑已验证跑通。
- [x] **Token 校验**：JWT 鉴权体系在远程环境运行正常。

> [!NOTE]
> 第一阶段所有任务已于今日全部达成。项目已具备完整的基础环境和用户准入能力。

## 4. 生产环境合规性（第一阶段额外建议）
如果是正式上线前，还需要完成：
1. **域名与 HTTPS**：微信小程序正式环境**必须**通过域名访问（不能用 IP），且必须配置 **SSL 证书 (HTTPS)**。
2. **白名单配置**：在微信小程序管理后台，将服务器域名加入“request合法域名”列表。

## 3. 下一步开发计划
当数据库创建完毕且配置文件 `.env` 修改正确后，我们将开始：
1. 编写 Sequelize UserModel 数据模型代码。
2. 编写与微信服务器换取 `openid` 的授权逻辑。
3. 签发登录态 JWT Token 的完整接口。
