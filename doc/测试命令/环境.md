二、测试环境准备

### 2.1 环境信息

```
后端地址:     http://120.27.239.244:3001:3001
数据库:       MySQL@127.0.0.1 (community_db)
数据库账号:   root / CommunityPwd123!
服务器SSH:    cw@120.27.239.244:3001 (已配置免密)
后端路径:     /home/cw/a/community-backend/backend/
```

### 2.2 环境检查清单

```bash
# 1. 检查后端运行状态
ssh cw@120.27.239.244:3001 "ps aux | grep node"
ssh cw@120.27.239.244:3001 "netstat -tlnp | grep 3001"

# 2. 检查数据库连接
ssh cw@120.27.239.244:3001 "mysql -u root -p'CommunityPwd123!' community_db -e 'SELECT 1;'"

# 3. 检查现有数据量
ssh cw@120.27.239.244:3001 "mysql -u root -p'CommunityPwd123!' community_db -e '
SELECT 
  (SELECT COUNT(*) FROM Users) as users,
  (SELECT COUNT(*) FROM neighbor_assist_orders) as assist_orders,
  (SELECT COUNT(*) FROM market_orders) as market_orders,
  (SELECT COUNT(*) FROM service_orders) as service_orders,
  (SELECT COUNT(*) FROM posts) as posts,
  (SELECT COUNT(*) FROM messages) as messages;
'"

# 4. 健康检查
curl http://120.27.239.244:3001:3001/
```

### 2.3 测试工具

| 工具 | 用途 | 说明 |
|-----|------|------|
| Node.js | 运行测试脚本 | 已安装 |
| sequelize | 数据库操作 | `npm install sequelize mysql2` |
| curl | 快速接口测试 | 系统自带 |
| 微信开发者工具 | 前端页面测试 | 下载安装 |
