例如有些问题是 用户 商家  后台管理员 服务商配合解决的。我举个例子。用户下单一个商品，那么是不是应该商家也会有对应的接单的逻辑，有对应的出货配送的逻辑。如果一个商品有问题。用户要退单，是不是商家需要有退款的逻辑。如果用户不愿意，是否有介入管理员的逻辑。参考这种思路苦战，发现更多的问题。

我需要你模拟用户点击前端小程序的各个按钮的方式，然后充当不同角色去进行业务测试(例如点击后是否正常获取到了对应的图像和内容，下单后是否有对应的订单生成)，由于你无法直接点击前端按钮，你可以通过模拟点击按钮(直接调用后端接口，然后分析返回的数据结果是否合理，完整的方式，同时测试需要是完整链条的，有时需要在不同角色之间切换，例如"例如有些问题是 用户 商家  后台管理员 服务商配合解决的。我举个例子。用户下单一个商品，那么是不是应该商家也会有对应的接单的逻辑，有对应的出货配送的逻辑。如果一个商品有问题。用户要退单，是不是商家需要有退款的逻辑。如果用户不愿意，是否有介入管理员的逻辑。参考这种思路苦战，发现更多的问题。")来测试。后端可以通过"ssh cw@8.136.29.208" 已配置ssh免密，你可以去对应目录下/home/cw/a/community-backend 修复后端问题后继续测试



无数据的部分 需要你模拟添加数据去测试(例如技工订单无订单数据，你可以模拟用户下单，然后检查是否正常，有数据，确认链条正常的情况下测试技工订单部分，对于帮帮订单和下单流程(需要下单的地方)，可以造一个用户，补充数据)，完成"后续建议'的修改


覆盖面 完全不够:例如我是一个用户，我刚刚在我的定位的社区发布了一个邻里帮帮订单，发布成功后，这个订单应该能被同步到数据库中，同步到对应小区的邻里帮帮订单池，然后理论上我在点击"我的"页面的"帮帮订单"，会获取对应后端获取对应数据库，会发现同一小区的不同帮帮订单，包括我发布的，我可以接别人的订单，别人可以接我的订单，订单被接单后应该是有消息通知的(通知接单详情，可跳转)同时会创建订单。然后类似于技工工作台里的演示订单，有对应的订单管理功能。这是需要前后端同步做的，不只是前端也不只是后端


我需要你将当前的处理帮帮订单的测试过程(全部链条的业务逻辑测试整理出来)，以及出事的业务逻辑检测整理出来
