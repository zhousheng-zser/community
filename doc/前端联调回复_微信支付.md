# 前端联调回复：微信小程序真实支付（API v3）

> 回复日期：2026-06-05  
> 联调域名：`https://jshsp1.eds-tech.cn/api/v1`  
> 详细接口说明见：[后端对接_微信支付联调.md](./后端对接_微信支付联调.md)

---

## 1. 后端已完成项

| 项 | 状态 | 说明 |
|----|------|------|
| 微信 V3 工具 `wechatPayV3.js` | ✅ | 统一下单、RSA 五参、回调验签解密 |
| 本地集市真实支付路由 | ✅ | `POST /market/payments/create` 已接入真实 Controller（不再返回 MD5 mock） |
| 支付状态查询 | ✅ | `GET /market/payments/status?order_no=...` |
| 支付回调 | ✅ | `POST /market/pay/callback`（无 JWT，微信服务器调用） |
| 到家服务支付 | ✅ | `POST /service-orders/payments/create` 共用同一套 V3 逻辑 |
| 支付流水表 | ✅ | `market_pay_transactions` 已存在 |
| 商户证书部署 | ✅ | `backend/certs/wechat/apiclient_key.pem` |
| 虚拟支付关闭 | ✅ | `MARKET_PAY_VIRTUAL_SUCCESS=false` |
| 支付成功后业务 | ✅ | 订单状态更新 + 积分发放 + 分佣（与 mock-success 对齐） |

---

## 2. 接口契约（与原文档一致，字段名请勿改）

### 2.1 本地集市

#### POST `/api/v1/market/payments/create`

- **鉴权**：`Authorization: Bearer <用户 token>`
- **Body**：`{ "order_no": "MK..." }`

**成功响应示例**（`code: 0`）：

```json
{
  "code": 0,
  "msg": "ok",
  "data": {
    "order_no": "MK202605221515186453",
    "out_trade_no": "1780625780477445148",
    "amount": "12.50",
    "pay_mode": "wechat",
    "virtual_pay": false,
    "payment_mode": "wechat",
    "wx_pay_params": {
      "timeStamp": "1710000000",
      "nonceStr": "随机串",
      "package": "prepay_id=wx...",
      "signType": "RSA",
      "paySign": "Base64签名"
    },
    "timeStamp": "1710000000",
    "nonceStr": "随机串",
    "package": "prepay_id=wx...",
    "signType": "RSA",
    "paySign": "Base64签名"
  }
}
```

**前端判定是否调起收银台**：

- ✅ 真支付：`virtual_pay !== true` 且 `signType === "RSA"` 且 `package` 以 `prepay_id=wx` 开头
- ❌ 虚拟支付：`virtual_pay === true`（当前生产已关闭，不应再出现）

#### GET `/api/v1/market/payments/status?order_no=MK...`

```json
{
  "code": 0,
  "msg": "ok",
  "data": {
    "order_no": "MK...",
    "pay_status": "paid",
    "order_status": "pending_accept"
  }
}
```

前端轮询时，以下均视为已支付：`paid` / `success` / `paid_success`。

### 2.2 到家服务

- `POST /api/v1/service-orders/payments/create` — Body/响应格式与集市相同
- `GET /api/v1/service-orders/payments/status?order_no=SO...`

### 2.3 支付回调（微信 → 后端，前端无需调用）

- `POST /api/v1/market/pay/callback`
- 成功应答：`{ "code": "SUCCESS", "message": "成功" }`

---

## 3. 错误码（HTTP 200，`code` 非 0）

| code | 含义 | 前端建议 |
|------|------|----------|
| 20031 | 订单状态不允许支付 | 提示用户刷新订单页 |
| 20043 | 用户未绑定 openid | 引导重新微信登录 |
| 20044 | 未配置微信支付 | 联系后端（运维问题） |
| 20045 | 微信统一下单失败 | 展示 msg，可重试 create |
| 20046 | 订单金额为 0 | 联系运营确认免支付流程 |

---

## 4. 联调步骤

```
1. 小程序微信登录（确保 users.openid 有值）
2. 创建集市订单（order_status=pending_payment, pay_status=unpaid）
3. POST /market/payments/create { order_no }
4. 解析 wx_pay_params（或顶层五参），调用 wx.requestPayment
5. 轮询 GET /market/payments/status 直到 pay_status=paid
6. 跳转支付成功页
```

---

## 5. 自测 curl 示例

```bash
# 替换为真实用户 token（须含 id 字段，微信登录获得）
TOKEN="<用户JWT>"

# 创建支付
curl -s -X POST "https://jshsp1.eds-tech.cn/api/v1/market/payments/create" \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"order_no":"MK202605221515186453"}'

# 查询支付状态
curl -s "https://jshsp1.eds-tech.cn/api/v1/market/payments/status?order_no=MK202605221515186453" \
  -H "Authorization: Bearer $TOKEN"
```

---

## 6. 当前待运营确认项（影响真机收银台）

后端代码与配置已就绪，自测已能成功调用微信统一下单 API。当前微信返回：

> **appid 和 mch_id 不匹配，请检查后再试**（code: 20045）

请在 [微信支付商户平台](https://pay.weixin.qq.com) 确认：

1. 商户号 `1113234936` 已与小程序 AppID `wx563a4fb90c87e40d` **绑定**
2. 已开通 **JSAPI/小程序支付**
3. 支付回调 URL 已登记：`https://jshsp1.eds-tech.cn/api/v1/market/pay/callback`

上述完成后，`payments/create` 将返回真实 `prepay_id=wx...` 五参数，小程序即可拉起收银台。

---

## 7. 常见问题

| 问题 | 原因 | 处理 |
|------|------|------|
| 仍看到 `signType: MD5` | 旧缓存或未部署 | 强刷 / 确认后端已重启 |
| `virtual_pay: true` | 虚拟支付未关或缺配置 | 后端设 `MARKET_PAY_VIRTUAL_SUCCESS=false` |
| 20043 无 openid | 非微信登录或 openid 未入库 | 重新 wx.login + 后端登录 |
| prepay_id 过期 | 超过约 2 小时 | 重新调用 payments/create |
| 支付成功但 status 仍 unpaid | 回调未达或验签失败 | 查后端日志 `pay/callback` |

---

## 8. 相关代码索引

| 说明 | 路径 |
|------|------|
| 集市支付 Controller | `backend/src/controllers/marketPaymentController.js` |
| 到家服务支付 | `backend/src/controllers/servicePaymentController.js` |
| 微信 V3 工具 | `backend/src/utils/wechatPayV3.js` |
| 集市路由 | `backend/src/modules/market/routes.js` |
| 回调 raw body 中间件 | `backend/src/index.js` |
